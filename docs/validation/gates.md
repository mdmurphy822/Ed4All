# Active Validation Gates

> Source of truth: `config/workflows.yaml::validation_gates`. This file is a human-readable mirror with per-validator Wave-reference paragraphs. Per-flag rows below show the phase at which each gate fires; severity in parentheses (`critical` when unmarked).


| Workflow | Phase | Gate | Validator |
|----------|-------|------|-----------|
| `course_generation` | `content_generation` | `content_structure` | ContentStructureValidator |
| `course_generation` | `content_generation` | `content_authorship` | ContentAuthorshipValidator (critical — anti-silent-template guard) |
| `course_generation` | `content_generation` | `manifest_completeness` | ManifestCompletenessValidator (**W3 — warning/SHADOW day-1**; single-pass mirror — see the `textbook_to_course::post_rewrite_validation` row for the full contract. The legacy single-pass path materializes no Block list, so the validator's graceful-pass arm keeps this binding inert until the two-pass rewrite tier emits the sidecar) |
| `course_generation` | `inter_tier_validation` | `outline_curie_anchoring` | BlockCurieAnchoringValidator |
| `course_generation` | `inter_tier_validation` | `outline_content_type` | BlockContentTypeValidator |
| `course_generation` | `inter_tier_validation` | `outline_page_objectives` | BlockPageObjectivesValidator |
| `course_generation` | `inter_tier_validation` | `outline_assessment_item_payload` | BlockAssessmentItemPayloadValidator |
| `course_generation` | `post_rewrite_validation` | `rewrite_curie_anchoring` | BlockCurieAnchoringValidator |
| `course_generation` | `post_rewrite_validation` | `rewrite_content_type` | BlockContentTypeValidator |
| `course_generation` | `post_rewrite_validation` | `rewrite_page_objectives` | BlockPageObjectivesValidator |
| `course_generation` | `post_rewrite_validation` | `rewrite_source_refs` | BlockSourceRefValidator |
| `course_generation` | `post_rewrite_validation` | `rewrite_assessment_item_payload` | BlockAssessmentItemPayloadValidator |
| `course_generation` | `post_rewrite_validation` | `rewrite_html_shape` | RewriteHtmlShapeValidator |
| `course_generation` | `post_rewrite_validation` | `rewrite_source_grounding` | RewriteSourceGroundingValidator (**W4: DEMOTED critical → warning** — cosine pre-filter; the NLI `block_prose_entailment` gate is the critical text-trace enforcer after calibration) |
| `course_generation` | `post_rewrite_validation` | `block_prose_entailment` | BlockProseEntailmentValidator (**W4 — warning/SHADOW day-1**; two-pass mirror of the `textbook_to_course::post_rewrite_validation` gate. Full-prose NLI entailment vs cited chunks; `config.shadow: true`. Critical flip is calibration-gated + DEFERRED — see the `# TODO(calibration)` marker in `config/workflows.yaml`) |
| `course_generation` | `post_rewrite_validation` | `manifest_completeness` | ManifestCompletenessValidator (**W3 — warning/SHADOW day-1**; two-pass mirror. Per-block synthesis-manifest RESOLUTION gate — see the `textbook_to_course::post_rewrite_validation` row below for the full contract. Day-1 warning until a live run proves the rewrite-tier emission; `# TODO(integration)` critical flip in `config/workflows.yaml`) |
| `course_generation` | `packaging` | `imscc_structure` | IMSCCValidator |
| `course_generation` | `packaging` | `page_objectives` | PageObjectivesValidator |
| `course_generation` | `packaging` | `page_objectives_shacl` | PageObjectivesShaclValidator (warning) |
| `course_generation` | `validation` | `wcag_compliance` | WCAGValidator |
| `course_generation` | `validation` | `oscqr_score` | OSCQRValidator (warning) |
| `intake_remediation` | `parsing` | `imscc_parse` | IMSCCParseValidator |
| `intake_remediation` | `validation` | `wcag_compliance` | WCAGValidator |
| `batch_dart` | `multi_source_synthesis` | `dart_markers` | DartMarkersValidator |
| `batch_dart` | `validation` | `wcag_aa_compliance` | WCAGValidator |
| `textbook_to_course` | `dart_conversion` | `dart_markers` | DartMarkersValidator |
| `textbook_to_course` | `chunking` | `chunkset_manifest` | ChunksetManifestValidator (warning) |
| `textbook_to_course` | `objective_extraction` | `textbook_outline_enrichment` | TextbookOutlineValidator (**critical** — three-stage textbook synthesis Stage-1 gate; audits the `semantic_outline` + `draft_terminal_objectives` keys the `TextbookSynthesisProvider` folds into `textbook_structure.json` when `TEXTBOOK_SYNTHESIS_PROVIDER` is set. Floor checks: `semantic_outline.themes[]` non-empty; every theme `chapter_ids[]` resolves against `chapters[].id`; `draft_terminal_objectives[]` non-empty with each draft TO carrying a non-empty `statement` + a valid `bloom_level`. GateIssue codes: `OUTLINE_THEMES_EMPTY`, `OUTLINE_THEME_CHAPTER_UNRESOLVED`, `OUTLINE_DRAFT_TO_EMPTY`, `OUTLINE_DRAFT_TO_MALFORMED`. Skips-with-pass when the enrichment keys are absent — default-off runs (`TEXTBOOK_SYNTHESIS_PROVIDER` unset) are unaffected, mirroring the `ABCD_MISSING` graceful-degrade contract. **2026-06-10 PROMOTED critical** (was warning-severity day-1): the "promote after a clean-corpus calibration run" condition is met — passes on 5 recent clean-corpus calibration runs (2026-06); skip-with-pass keeps synthesis-off/legacy runs unaffected) |
| `textbook_to_course` | `concept_extraction` | `concept_graph` | ConceptGraphValidator (warning) |
| `textbook_to_course` | `concept_extraction` | `domain_concept_vocabulary` | DomainConceptVocabularyValidator (**critical** — three-stage textbook synthesis Stage-3 gate; audits the `domain_concept_vocabulary.json` sibling of `concept_graph_semantic.json` produced by the per-chapter `TextbookSynthesisProvider.synthesize_concepts` pass when `TEXTBOOK_SYNTHESIS_PROVIDER` is set. Floor checks: `>= _MIN_CONCEPTS` (default 8 — matches `ConceptGraphValidator`'s ≥10-node floor with co-occurrence-filter headroom); every concept carries a non-empty `canonical`; `concept_count == len(concepts)`. The `chapter_synthesis_failures` count is surfaced informationally (per-chapter failure isolation, not a fail). GateIssue codes: `VOCAB_THIN`, `VOCAB_CONCEPT_NO_CANONICAL`, `VOCAB_COUNT_MISMATCH`, `VOCAB_PARTIAL_COVERAGE` (warning-only). Skips-with-pass when the vocabulary artifact is absent — default-off runs are unaffected. **2026-06-10 PROMOTED critical** (was warning-severity day-1 per the calibration-debt pattern): passes on 5 recent clean-corpus calibration runs (2026-06); skip-with-pass keeps synthesis-off/legacy runs unaffected) |
| `textbook_to_course` | `course_planning` | `abcd_verb_alignment` | AbcdObjectiveValidator (**2026-06-10 PROMOTED critical**, was warning-severity day-1 — "Phase 7+ promotes to critical once corpus calibration confirms safe"; condition met: passes on every recent clean-corpus calibration run (2026-06) where it ran. Graceful-degrades to warning-severity `ABCD_MISSING` (passed=True) for legacy LOs without an `abcd` block, so promoting severity does NOT fail-close legacy corpora — only a real `ABCD_VERB_BLOOM_MISMATCH`/`ABCD_MALFORMED` on an ABCD-authored LO blocks) |
| `textbook_to_course` | `course_planning` | `chapter_objective_coverage` | ChapterObjectiveCoverageValidator (**critical** — three-stage textbook synthesis Stage-2 gate; audits the per-chapter `CO-NN` objectives + reconciled `terminal_objectives[]` produced by `TextbookSynthesisProvider.synthesize_chapter_objectives` + `reconcile_terminal_objectives` when `TEXTBOOK_SYNTHESIS_PROVIDER` is set. Floor checks: every `chapters[].id` with non-empty text produced ≥1 CO (cross-checks `chapter_synthesis_failures`); reconciled `terminal_objectives[]` non-empty. GateIssue codes: `CHAPTER_OBJ_MISSING`, `CHAPTER_OBJ_PARTIAL_COVERAGE`, `RECONCILED_TO_EMPTY`. Skips-with-pass when the synthesis enrichment is absent — default-off runs fall through to the deterministic `synthesize_objectives_from_topics` path and are unaffected. **2026-06-10 PROMOTED critical** (was warning-severity day-1 per the calibration-debt pattern): passes on 4 recent clean-corpus calibration runs (2026-06); surfaces per-chapter degradation so a partial LLM outage is never silent; skip-with-pass keeps synthesis-off/legacy runs unaffected. **Calibration caveat:** the clean-corpus evidence exercised only the warning-severity `CHAPTER_OBJ_MISSING` / `CHAPTER_OBJ_PARTIAL_COVERAGE` profile; this gate's critical malformed-shape code `RECONCILED_TO_EMPTY` was not exercised by those runs, so the promotion rests on the missing-coverage profile alone) |
| `textbook_to_course` | `course_planning` | `objective_source_refs` | ObjectiveSourceRefValidator (warning — Wave 1.6 W1.6.C; resolves each synthesized LO's `source_refs[]` against the union of `textbook_structure.chapters[*].id` ∪ `chapters[*].sections[*].id` ∪ DART chunkset universe; `require_to_attribution=false` day-1) |
| `textbook_to_course` | `course_planning` | `objective_entailment` | ObjectiveEntailmentValidator (**W4 — warning/SHADOW day-1**; per-LO NLI entailment — scores each synthesized LO `statement` as a single hypothesis against the text of its structured `source_refs[].chunk_ids[]` (resolved id→text from the sibling `chunks.jsonl`, keyed on both the chunk top-level `id` AND each `source.source_references[].sourceId`) via `score_groundedness`. The GATE counterpart to W2's in-synthesis NLI-grounding filter (W2 filters at generation time; this gates the LANDED `synthesized_objectives.json`), sitting ALONGSIDE the resolution-only `objective_source_refs` gate — a valid-resolving `chunk_ids[]` on a fabricated-on-topic objective passes resolution but fails this entailment gate. Fires `OBJECTIVE_UNENTAILED` (rate < `objective_entailment_rate_floor`, default 1.0) + `OBJECTIVE_CONTRADICTED` + warning `OBJECTIVE_NO_GROUNDING_SOURCE`. Skips legacy `List[str]`/empty `source_refs` (resolution gate owns those); TO empty-refs honors `require_to_entailment` (default false). Graceful-degrade: NLI deps absent → warning `NLI_DEPS_MISSING` passed=True; strict → raise; no chunk universe → warning `OBJECTIVE_NO_GROUNDING_SOURCE` passed=True. `config.shadow: true` → warning-only + no regenerate. **Critical flip is calibration-gated + DEFERRED** — see the `# TODO(calibration)` marker at the gate in `config/workflows.yaml` and §4 of `plans/finegrain/w4-nli-grounding-gate.md`) |
| `textbook_to_course` | `course_planning` | `terminal_objective_coverage` | TerminalObjectiveCoverageValidator (**critical / block** — fail-fast guarantee against the `UNCOVERED_TERMINAL_OUTCOME` archival failure. Catches orphan terminal objectives at planning instead of letting them flow to the `libv2_archival::packet_integrity_strict` gate. Two shapes via `lib/ontology/terminal_coverage.py`: a CO-bearing course with a terminal that no chapter objective rolls up to (`parent_to`/`parent_terminal`/`terminal_id`) → `ORPHAN_TERMINAL_NO_CHAPTER_OBJECTIVE`; a CO-less course whose terminal's `chapter` back-pointer (normalised — `"6"`/`"ch6"`/`"Chapter 6"` reconcile) does not resolve to a chapter in `textbook_structure` → `ORPHAN_TERMINAL_NO_CHAPTER`, or no chapter ref at all → `ORPHAN_TERMINAL_NO_CHAPTER_REF`. Pairs with the synthesis-side Layer-A prune in `_plan_course_structure` (`prune_orphan_terminals`, CO-bearing only). Graceful-degrades to `passed=True` when objectives are empty/absent (other gates own that) or when a CO-less course has no `textbook_structure` to adjudicate (emits warning-severity `TERMINAL_COVERAGE_UNVERIFIED`; the archival gate stays the terminal backstop — preserves reuse/legacy runs). Backward-compat verified against the RDF/SHACL calibration corpus in its CO-bearing shape (consistent → pass) and CO-less corpora (pass / pass-with-warning)) |
| `textbook_to_course` | `content_generation` | `content_structure` | ContentStructureValidator (warning) |
| `textbook_to_course` | `content_generation` | `source_refs` | PageSourceRefValidator |
| `textbook_to_course` | `content_generation` | `content_grounding` | ContentGroundingValidator |
| `textbook_to_course` | `content_generation` | `content_authorship` | ContentAuthorshipValidator (critical — blocks the deterministic `generate_week` template emitter from passing as LLM-authored content when LLM authoring was intended via `COURSEFORGE_PROVIDER` / `COURSEFORGE_TWO_PASS` / `ED4ALL_AGENT_DISPATCH`; warns when template fires with no LLM intent; opt out with `COURSEFORGE_ALLOW_TEMPLATE_EMITTER=1`. Reads `<export>/content_generation_provenance.json` stamped by `_generate_course_content`.) |
| `textbook_to_course` | `content_generation` | `manifest_completeness` | ManifestCompletenessValidator (**W3 — warning/SHADOW day-1**; single-pass mirror of the `post_rewrite_validation` gate below. The legacy single-pass path materializes no Block list, so the validator's graceful-pass arm keeps this binding inert until the two-pass rewrite tier emits the sidecar) |
| `textbook_to_course` | `inter_tier_validation` | `outline_curie_anchoring` | BlockCurieAnchoringValidator |
| `textbook_to_course` | `inter_tier_validation` | `outline_content_type` | BlockContentTypeValidator |
| `textbook_to_course` | `inter_tier_validation` | `outline_page_objectives` | BlockPageObjectivesValidator |
| `textbook_to_course` | `inter_tier_validation` | `outline_source_refs` | BlockSourceRefValidator |
| `textbook_to_course` | `inter_tier_validation` | `outline_assessment_item_payload` | BlockAssessmentItemPayloadValidator |
| `textbook_to_course` | `inter_tier_validation` | `outline_shacl` | CourseforgeOutlineShaclValidator (warning) |
| `textbook_to_course` | `inter_tier_validation` | `outline_objective_assessment_similarity` | ObjectiveAssessmentSimilarityValidator (warning) |
| `textbook_to_course` | `inter_tier_validation` | `outline_concept_example_similarity` | ConceptExampleSimilarityValidator (warning) |
| `textbook_to_course` | `inter_tier_validation` | `outline_objective_roundtrip_similarity` | ObjectiveRoundtripSimilarityValidator (warning) |
| `textbook_to_course` | `inter_tier_validation` | `outline_bloom_classifier_disagreement` | BloomClassifierDisagreementValidator (warning) |
| `textbook_to_course` | `inter_tier_validation` | `outline_block_objective_delivery` | BlockObjectiveDeliveryValidator (warning — Wave 1.7 W1.7.C tri-axis NLI / Bloom-gap / verb check; stamps `objectiveAlignment[]` per Block per `$defs.ObjectiveAlignment` in `schemas/knowledge/courseforge_jsonld_v1.schema.json`) |
| `textbook_to_course` | `inter_tier_validation` | `outline_assessment_retrieval_grounding` | AssessmentRetrievalGroundingValidator (warning; calibration-gated severity flip — see `lib/governance/calibration_gate.py::resolve_severity_flip`) |
| `textbook_to_course` | `inter_tier_validation` | `outline_distractor_plausibility` | DistractorPlausibilityValidator (warning) |
| `textbook_to_course` | `inter_tier_validation` | `outline_distractor_misconception_alignment` | DistractorMisconceptionAlignmentValidator (warning) |
| `textbook_to_course` | `inter_tier_validation` | `outline_distractor_structural` | DistractorStructuralValidator (warning) |
| `textbook_to_course` | `inter_tier_validation` | `outline_padded_distractor` | PaddedDistractorValidator |
| `textbook_to_course` | `inter_tier_validation` | `outline_instructional_depth` | InstructionalDepthValidator (warning) |
| `textbook_to_course` | `inter_tier_validation` | `outline_bloom_structural_enforcement` | BloomStructuralEnforcementValidator (warning) |
| `textbook_to_course` | `content_generation_rewrite` | `content_grounding` | ContentGroundingValidator (warning — 2026-05-14 TEMP critical→warning so an objective-only corpus of short `<li>` blocks doesn't trip the `AGGREGATE_EMPTY_PAGES` paragraph-shape check. Revert deferred (2026-06-10): unlike the sibling Block validators, this gate consumes rendered HTML `page_paths`, NOT the Block list, so the "honor escalation_marker" hook does not reach it; the original revert condition stands — promote to critical only when the calibration corpus carries concept/example/explanation blocks with substantive (≥ NON_TRIVIAL_WORD_FLOOR) prose) |
| `textbook_to_course` | `post_rewrite_validation` | `rewrite_curie_anchoring` | BlockCurieAnchoringValidator |
| `textbook_to_course` | `post_rewrite_validation` | `rewrite_content_type` | BlockContentTypeValidator |
| `textbook_to_course` | `post_rewrite_validation` | `rewrite_page_objectives` | BlockPageObjectivesValidator |
| `textbook_to_course` | `post_rewrite_validation` | `rewrite_source_refs` | BlockSourceRefValidator |
| `textbook_to_course` | `post_rewrite_validation` | `rewrite_assessment_item_payload` | BlockAssessmentItemPayloadValidator |
| `textbook_to_course` | `post_rewrite_validation` | `rewrite_html_shape` | RewriteHtmlShapeValidator |
| `textbook_to_course` | `post_rewrite_validation` | `rewrite_source_grounding` | RewriteSourceGroundingValidator (**W4: DEMOTED critical → warning** — the per-sentence cosine ≥ 0.45 gate is now a cheap pre-filter that catches gross off-topic drift without an NLI load; on-topic fabrication scores 0.5+ cosine and passes here. The critical text-trace enforcer is now `block_prose_entailment` (NLI). `on_fail: warn`, `on_error: warn`) |
| `textbook_to_course` | `post_rewrite_validation` | `block_prose_entailment` | BlockProseEntailmentValidator (**W4 — warning/SHADOW day-1**; full-block-prose NLI entailment vs the block's cited source chunk(s) via `lib/retrieval/groundedness.py::score_groundedness` windowed scorer-v2. The text-trace counterpart to the demoted `rewrite_source_grounding` cosine gate AND the broadened counterpart to `claim_support` (which scores only curated `key_claims[]`). Fires `BLOCK_PROSE_UNGROUNDED` (< 60% entailment rate) + `BLOCK_PROSE_CONTRADICTED` (> 5% contradicted rate, standalone) + warning `BLOCK_PROSE_NO_GROUNDING_SOURCE`. Skip set = `claim_support`'s (`assessment_item`/`objective`/`chrome`/`recap`) — deliberately NOT skipping `example`/`self_check_question` (NLI ≠ cosine). Graceful-degrade: NLI deps absent → warning `NLI_DEPS_MISSING` passed=True; `TRAINFORGE_REQUIRE_EMBEDDINGS=true` → fail-closed. `config.shadow: true` forces emitted issues to warning-severity (compute + capture only). **Critical flip is calibration-gated + DEFERRED** — see the `# TODO(calibration)` marker at the gate in `config/workflows.yaml` and §4 of `plans/finegrain/w4-nli-grounding-gate.md`; promotes to critical (`shadow: false`, calibrated `min_block_entailment_rate`) only after `state/calibration/block_prose_entailment_calibration.json` records `decision: promote_critical`, in the SAME flip that promotes `claim_support` to critical) |
| `textbook_to_course` | `post_rewrite_validation` | `manifest_completeness` | ManifestCompletenessValidator (**W3 — warning/SHADOW day-1**; per-block synthesis-manifest RESOLUTION gate. Reads the `<export>/03_content_development/block_synthesis_manifest.jsonl` sidecar written by `pipeline_tools._emit_block_synthesis_manifest` at the rewrite-tier emit (gated by `COURSEFORGE_EMIT_BLOCKS`); resolves each `source_chunk_ids[]` id against the DART chunkset `chunks.jsonl` (chunk `id` ∪ `source.source_references[].sourceId`). Asserts non-empty resolved `source_chunk_ids` + `template_id` + `concept_tags` on SUBSTANTIVE block types (the §1.4 skip set exempts `objective`/`chrome`/`recap`) and that every declared id RESOLVES. Fires `MANIFEST_UNGROUNDED` (empty ids → `action=regenerate`), `MANIFEST_SOURCE_ID_UNRESOLVED`, `MANIFEST_INCOMPLETE`/`MANIFEST_SCHEMA_INVALID`/`MANIFEST_NO_TEMPLATE`, and anti-silent-degradation `MANIFEST_FILE_MISSING` (populated `03_content_development/` but no sidecar) / `MANIFEST_RESOLUTION_UNIVERSE_EMPTY`. `concept_tags` non-empty is `MANIFEST_NO_CONCEPT_TAGS` warning while `config.require_concept_tags: false` (day-1). RESOLUTION only — NOT entailment (W4 `block_prose_entailment` owns NLI grounding of prose vs chunk; W3 is W4's cheap structural precondition). Gate input built by `MCP/hardening/gate_input_routing.py::_build_manifest_completeness`; per-type required-matrix wiring in `Courseforge/config/block_routing.yaml`. **Day-1 SHADOW (warning)** until a live 7B run confirms the emission; `# TODO(integration)` critical flip at the gate in `config/workflows.yaml`) |
| `textbook_to_course` | `post_rewrite_validation` | `rewrite_shacl` | CourseforgeOutlineShaclValidator (warning) |
| `textbook_to_course` | `post_rewrite_validation` | `rewrite_objective_assessment_similarity` | ObjectiveAssessmentSimilarityValidator (warning) |
| `textbook_to_course` | `post_rewrite_validation` | `rewrite_concept_example_similarity` | ConceptExampleSimilarityValidator (warning) |
| `textbook_to_course` | `post_rewrite_validation` | `rewrite_objective_roundtrip_similarity` | ObjectiveRoundtripSimilarityValidator (warning) |
| `textbook_to_course` | `post_rewrite_validation` | `rewrite_bloom_classifier_disagreement` | BloomClassifierDisagreementValidator (warning) |
| `textbook_to_course` | `post_rewrite_validation` | `rewrite_block_objective_delivery` | BlockObjectiveDeliveryValidator (warning — Wave 1.7 W1.7.C tri-axis post-rewrite mirror; stamps post-rewrite `objectiveAlignment[]` per Block; mirrors `outline_block_objective_delivery` shape) |
| `textbook_to_course` | `post_rewrite_validation` | `claim_support` | ClaimSupportValidator (warning — Wave 2 W2.F per-claim NLI entailment vs cited chunks; fires UNSUPPORTED_CLAIM >20% and CONTRADICTED_CLAIM >5%; stamps `key_claims[].outcome` shape per Block per `chunk_v4.schema.json::key_claims` `oneOf` projection — Wave 1.5 W1.5.A migration contract preserves legacy List[str] back-compat. Wave W-D11 T11.1 adds substring + char_span enforcement against the optional `evidence_quote` / `evidence_char_span` fields on each structured claim entry: emits `EVIDENCE_QUOTE_NOT_SUBSTRING` when the quote is not a verbatim substring of any cited chunk, `EVIDENCE_CHAR_SPAN_MISMATCH` when `chunk_text[start:end] != evidence_quote`, and reserves `EVIDENCE_QUOTE_MISSING` for the future threshold-gated coverage warning (T11.5/T11.6). Aggregate counters surfaced on `GateResult.metadata`: `evidence_quote_coverage_rate`, `evidence_quote_substring_fail_rate`, `evidence_quote_char_span_mismatch_rate` (always present, 0.0 default). Day-1 warning-severity per the W1.7.C / W4.A / W4.C calibration-deferred contract — evidence-quote findings do NOT contribute to the regenerate signal and `passed` stays True; future critical flip via per-block-type thresholds once per-corpus baselines are calibrated) |
| `textbook_to_course` | `post_rewrite_validation` | `rewrite_assessment_retrieval_grounding` | AssessmentRetrievalGroundingValidator (warning; calibration-gated severity flip — see `lib/governance/calibration_gate.py::resolve_severity_flip`) |
| `textbook_to_course` | `post_rewrite_validation` | `rewrite_distractor_plausibility` | DistractorPlausibilityValidator (warning) |
| `textbook_to_course` | `post_rewrite_validation` | `rewrite_distractor_misconception_alignment` | DistractorMisconceptionAlignmentValidator (warning) |
| `textbook_to_course` | `post_rewrite_validation` | `rewrite_distractor_structural` | DistractorStructuralValidator (warning) |
| `textbook_to_course` | `post_rewrite_validation` | `rewrite_padded_distractor` | PaddedDistractorValidator |
| `textbook_to_course` | `post_rewrite_validation` | `rewrite_instructional_depth` | InstructionalDepthValidator (warning) |
| `textbook_to_course` | `post_rewrite_validation` | `rewrite_bloom_structural_enforcement` | BloomStructuralEnforcementValidator (warning) |
| `textbook_to_course` | `packaging` | `imscc_structure` | IMSCCValidator (warning) |
| `textbook_to_course` | `packaging` | `page_objectives` | PageObjectivesValidator |
| `textbook_to_course` | `packaging` | `page_objectives_shacl` | PageObjectivesShaclValidator (warning) |
| `textbook_to_course` | `imscc_chunking` | `chunkset_manifest` | ChunksetManifestValidator (warning) |
| `textbook_to_course` | `trainforge_assessment` | `imscc_input_valid` | IMSCCValidator (pre-assessment) |
| `textbook_to_course` | `trainforge_assessment` | `assessment_quality` | AssessmentQualityValidator (critical at corpus-wide level for back-compat; per-question-type diversity sub-checks fire warning-severity day-1 via `_PER_QUESTION_TYPE_THRESHOLDS` in `lib/validators/assessment.py`. Calibration plan: rebuild the RDF/SHACL calibration corpus with W5 fields → tune per-type thresholds → flip warning → critical in a micro-wave. Mirrors W1.7.C `_PER_BLOCK_TYPE_ENTAILMENT_FLOOR` + W4.C `_PER_PAIR_KIND_ENTAILMENT_FLOOR` patterns.) |
| `textbook_to_course` | `trainforge_assessment` | `assessment_objective_alignment` | AssessmentObjectiveAlignmentValidator |
| `textbook_to_course` | `trainforge_assessment` | `trainforge_padded_distractor` | PaddedDistractorValidator |
| `textbook_to_course` | `training_synthesis` | `synthesis_quota` | SynthesisQuotaValidator (warning) |
| `textbook_to_course` | `training_synthesis` | `min_edge_count` | MinEdgeCountValidator |
| `textbook_to_course` | `training_synthesis` | `synthesis_diversity` | SynthesisDiversityValidator |
| `textbook_to_course` | `training_synthesis` | `property_coverage` | PropertyCoverageValidator (no-ops on courses without a property manifest) |
| `textbook_to_course` | `training_synthesis` | `synthesis_leakage` | SynthesisLeakageValidator (fails closed at >5% verbatim chunk leakage) |
| `textbook_to_course` | `training_synthesis` | `curie_anchoring` | CurieAnchoringValidator (binary per-pair anchoring sentinel, default min_pair_anchoring_rate=0.95; supersedes the deprecated `curie_preservation` mean-retention metric — Wave 135c→135d migration; the `CuriePreservationValidator` deprecation shim was removed after Wave 137. Operators who pinned `curie_preservation` in custom workflows: rename to `curie_anchoring` and update threshold key from `min_mean_retention` (0.40) to `min_pair_anchoring_rate` (0.95)) |
| `textbook_to_course` | `training_synthesis` | `pair_claim_support` | PairClaimSupportValidator (warning — Wave 4 W4.A per-claim NLI entailment vs cited chunks; day-1 warning per Wave 1.5/1.6/1.7 contract, flips to critical once per-corpus baseline is calibrated; stamps `per_claim_support[]` per pair per `instruction_pair.schema.json:113-187`. Wave 9 TIGHT extends `outcome` enum with `dart_disagreement` value sourced from `dart_source_check` sub-stamp; gate emits aggregate-rate `DART_DISAGREEMENT_RATE_HIGH` warning at >5%. Wave W-D11 T11.2 mirrors the block-surface evidence-quote enforcement on the pair surface: emits `EVIDENCE_QUOTE_NOT_SUBSTRING` when the structured claim's `evidence_quote` is not a verbatim substring of any chunk in the cited universe, `EVIDENCE_CHAR_SPAN_MISMATCH` when `chunk_text[start:end] != evidence_quote`, and reserves `EVIDENCE_QUOTE_MISSING` for the future threshold-gated coverage warning. Aggregate counters surfaced on `GateResult.metadata`: `evidence_quote_coverage_rate`, `evidence_quote_substring_fail_rate`, `evidence_quote_char_span_mismatch_rate` (always present, 0.0 default). Day-1 warning-severity per the calibration-deferred contract — evidence-quote findings do NOT contribute to the regenerate signal and `passed` stays True; future critical flip via per-pair-kind thresholds once per-corpus baselines are calibrated) |
| `textbook_to_course` | `training_synthesis` | `pair_lo_refs` | PairLearningOutcomeRefsValidator (Wave 4 W4.B — per-pair learning_outcome_refs subset check; critical because phantom-LO is structural; mirrors AssessmentObjectiveAlignmentValidator severity; stamps `pair_lo_resolution.{declared_los,chunk_los,phantom_los}` per `instruction_pair.schema.json:188-209`. Wave 8 deterministic-template paths additionally stamp `pair_lo_resolution.skipped: "deterministic_template"` — currently rejected by `additionalProperties: false`; W-D1 P0.1 is the schema fix.) |
| `textbook_to_course` | `training_synthesis` | `pair_objective_delivery` | PairObjectiveDeliveryValidator (warning — Wave 4 W4.C MEDIUM per-pair-per-objective tri-axis NLI entailment + Bloom-gap + verb cooccurrence delivery audit; mirrors BlockObjectiveDeliveryValidator on the pair surface; day-1 warning per Wave 1.5/1.6/1.7 contract, flips to critical once per-corpus baseline is calibrated; stamps `pair_objective_alignment[]` per pair per `instruction_pair.schema.json:211-268`; aggregate `pair_objective_alignment_pass_rate` carried at line 269-273) |
| `textbook_to_course` | `training_synthesis` | `pair_promotion` | TrainingPairPromotionValidator |
| `textbook_to_course` | `libv2_archival` | `libv2_manifest` | LibV2ManifestValidator |
| `textbook_to_course` | `libv2_archival` | `packet_integrity_strict` | PacketIntegrityValidator |
| `textbook_to_course` | `libv2_archival` | `semantic_graph_rule_output` | SemanticGraphRuleOutputValidator (warning) |
| `textbook_to_course` | `libv2_archival` | `kg_quality_report` | KGQualityValidator (critical, thresholds 0.95/0.95/0.95/0.5 = completeness/consistency/accuracy/coverage). The `coverage` floor (0.5) now thresholds **chunk-anchored DomainConcept node-grounding** — the share of DomainConcept-or-classless concept nodes incident to ≥1 chunk-evidenced edge (the seven chunk-anchored rules: co-occurrence, intra-chunk-link, defined-by, exemplifies, is-a, misconception-of, assesses) — NOT the prior asserted/(asserted+derived) edge share, which grew a quadratic LO-order-derived denominator and anti-correlated with quality (the RDF/SHACL calibration corpus fell to 0.047 under it; reads 1.0 under node-grounding, a general-textbook corpus ~0.69). The legacy edge ratio is preserved unthresholded as `asserted_edge_share` in the coverage detail. **ACTIVATED (operator-approved):** the builder `MCP/hardening/gate_input_routing.py::_build_kg_quality_inputs` is now registered, so the gate actually runs — pre-activation it had NO builder, the router returned `__no_builder_registered__`, and the executor stamped `GATE_SKIPPED_MISSING_INPUTS` (`passed=True`), i.e. the gate never executed. The builder routes `semantic_graph_path` from `concept_extraction.concept_graph_path` (which points at `concept_graph_semantic.json`), a best-effort `concept_graph.json` sibling for the asserted graph (the reporter degrades its absence to `{}`), `course_slug`/`run_id` from phase outputs + workflow params, and `output_dir` = the LibV2 course `quality/` dir. **Threshold floors are ENFORCED:** the YAML `threshold:` block (`min_completeness`/`min_consistency`/`min_accuracy`/`min_coverage`) is forwarded into the validator's inputs by `ValidationGateManager.run_gate` (it now merges `gate.threshold` into the inputs dict alongside `gate.config`, via `setdefault` so a per-call override still wins). `_apply_thresholds` still recognizes only the result-level keys (`max_critical_issues`/`max_issues`/`min_score`/`required_score`); the per-dimension `min_*` floors are enforced inside the validator, which reads them from its inputs. A **metric breach now emits a critical `KG_QUALITY_<DIM>_BELOW_THRESHOLD` GateIssue and inverts `passed` to `False`** (the gate is `severity: critical` / `on_fail: block`, so a below-floor KG refuses to ship). When no floor is configured (each defaults to 0.0) or every dimension clears its floor the gate passes as before. A **missing/malformed graph or reporter exception also FAILS CLOSED** (`KG_QUALITY_PEDAGOGY_GRAPH_MISSING` / `KG_QUALITY_REPORTER_ERROR`, critical/block) — the point of activation: refuse to ship an empty KG to LibV2. The builder NEVER fabricates a graph path for a truly-absent graph and returns an empty router missing-list, so the gate is adjudicated by the validator's own fail-closed arm rather than re-skipped. The validator re-runs `EdgeConsensusAggregator` (idempotent over the same semantic graph) and writes its consensus copy under `output_dir`; the authoring-time canonical `concept_graph/edge_consensus_report.json` sibling is left untouched (no second divergent report)) |
| `textbook_to_course` | `libv2_archival` | `chunkset_drift` | ChunksetDriftValidator (warning — DART vs. IMSCC chunkset drift; sidecar `drift_report.json`) |
| `rag_training` | `assessment_generation` | `assessment_quality` | AssessmentQualityValidator (critical at corpus-wide level for back-compat; per-question-type diversity sub-checks fire warning-severity day-1 via `_PER_QUESTION_TYPE_THRESHOLDS` in `lib/validators/assessment.py`. Calibration plan: rebuild the RDF/SHACL calibration corpus with W5 fields → tune per-type thresholds → flip warning → critical in a micro-wave. Mirrors W1.7.C `_PER_BLOCK_TYPE_ENTAILMENT_FLOOR` + W4.C `_PER_PAIR_KIND_ENTAILMENT_FLOOR` patterns.) |
| `rag_training` | `assessment_generation` | `bloom_alignment` | BloomAlignmentValidator (warning) |
| `rag_training` | `assessment_generation` | `leak_check` | LeakCheckValidator |
| `rag_training` | `assessment_generation` | `outcome_ref_integrity` | LeakCheckValidator (warning) |
| `rag_training` | `assessment_generation` | `content_fact_check` | ContentFactValidator (warning) |
| `rag_training` | `assessment_generation` | `question_quality` | QuestionQualityValidator |
| `rag_training` | `validation` | `final_quality` | FinalQualityValidator |
| `trainforge_train` | `post_training_validation` | `eval_gating` | EvalGatingValidator (fails closed on regression / yes-bias / no-bias / source-match drop; also surfaces `EVAL_CONTENT_TYPE_ROLE_ALIGNMENT_LOW` warning-severity GateIssue when `content_type_role_alignment_summary.alignment_rate` < 0.70 — internal warning code, not a separately-registered gate. Wave 7 W7.D iterates each W3.F metric's `per_question_type` bucket and emits a per-type warning code per relevant bucket below its floor: `EVAL_ANSWERABLE_PER_TYPE_BELOW_THRESHOLD`, `EVAL_SINGLE_CORRECT_PER_TYPE_BELOW_THRESHOLD`, `EVAL_DISTRACTOR_ENTROPY_PER_TYPE_BELOW_THRESHOLD`, `EVAL_BLOOM_ALIGNMENT_PER_TYPE_BELOW_THRESHOLD`, `EVAL_PLACEHOLDER_PER_TYPE_ABOVE_THRESHOLD`, `EVAL_SOURCE_SUPPORT_PER_TYPE_BELOW_THRESHOLD` — all warning-severity day-1, calibration-deferred per plan §5; non-relevant + deps-missing buckets are skipped) |
| `trainforge_train` | `post_training_validation` | `family_completeness` | FamilyCompletenessValidator (fails closed when any CURIE family is partially complete; family clusters declared in `schemas/training/family_map.<family>.yaml`) |

## escalation_marker honoring (2026-06-10 TEMP-flip revert)

Seven 2026-05 `TEMP` critical→warning flips (plus one deferred revert) in `config/workflows.yaml::textbook_to_course` were introduced because the Block-input inter-tier / post-rewrite validators audited **every** Block in the input list — including blocks the router had already terminally escalated (`Block.escalation_marker` non-null: outline budget exhausted, validator consensus failure, structural-unfixable, dispatch error, per-claim attribution unfixable). On a corpus that legitimately escalated some fraction of its blocks, these gates fired `action=block` against the degenerate content the escalation already recorded — a guaranteed false positive for the blocks that never ship. The flips' recorded revert condition was "once the validators honor `block.escalation_marker`".

The hook is now in place, but it honors the marker the way the **packager** does — NOT a blanket marker skip. `Courseforge/router/inter_tier_gates.py::_is_unshipped_escalation_tombstone(block)` and `lib/validators/rewrite_html_shape.py::_is_unshipped_escalation_tombstone(block)` skip a Block **only when** `escalation_marker is not None AND not (content or "").strip()` — i.e. a marker + empty *tombstone* that the packager filters out of the IMSCC (the exact ship-exclusion predicate at `MCP/tools/pipeline_tools.py`'s per-page emit filter, `pipeline_tools.py:5226`). A marker + non-empty *salvaged* block (escalated-rewrite salvage path, `Courseforge/generators/_rewrite_provider.py::_apply_rewrite_touch` via `dataclasses.replace`, marker preserved) DOES ship — per the design comment at `pipeline_tools.py:5135` — and stays on the audit path. Every `Block*Validator` (`BlockCurieAnchoringValidator`, `BlockContentTypeValidator`, `BlockPageObjectivesValidator`, `BlockSourceRefValidator`) plus `RewriteHtmlShapeValidator` honors it; tombstones are skipped (no audit, not counted toward `audited`, no issue emitted), salvaged-with-content blocks are audited.

The IMSCC-side backstop is `lib/validators/imscc.py::IMSCCValidator._check_escalated_blocks_absent` (code `ESCALATED_BLOCK_IN_IMSCC`), which confirms no tombstone leaked into shipped HTML. It is wired at **warning** severity in `textbook_to_course` and is **input-starved** in fully orchestrated runs (it no-ops unless `blocks_final_path` + the shipped HTML are threaded into its inputs), so it is NOT a critical leak backstop — the lockstep tombstone-skip predicate above is what keeps salvaged-with-content blocks on the audit path. With the false-positive class (tombstones only) removed from the gates, the flips were reverted to `critical` (`on_fail: block`) on 2026-06-10:

| Gate | Phase | Disposition |
|------|-------|-------------|
| `outline_curie_anchoring` | `inter_tier_validation` | reverted → critical |
| `outline_content_type` | `inter_tier_validation` | reverted → critical |
| `outline_page_objectives` | `inter_tier_validation` | reverted → critical |
| `rewrite_curie_anchoring` | `post_rewrite_validation` | reverted → critical |
| `rewrite_content_type` | `post_rewrite_validation` | reverted → critical |
| `rewrite_page_objectives` | `post_rewrite_validation` | reverted → critical |
| `rewrite_html_shape` | `post_rewrite_validation` | reverted → critical |
| `content_grounding` (rewrite tier) | `content_generation_rewrite` | **revert deferred** — consumes rendered HTML `page_paths`, not the Block list, so the escalation_marker hook does not reach it; original corpus-quality revert condition (substantive prose) stands |

The `course_generation` workflow's copies of the outline/rewrite Block gates were never TEMP-flipped (already `critical`), so only `textbook_to_course` counts moved (27→38 critical, 49→38 warning, including the four day-1 promotions in the rows above).
