# Courseforge behavior flags

Canonical per-flag reference for the `COURSEFORGE_*` / `COURSEPLANNER_*` / `TEXTBOOK_SYNTHESIS_*` env-var prefixes.
Split out of `Courseforge/CLAUDE.md` so the table is not loaded into context
on every session that touches `Courseforge/`. Grep it by flag name:

```bash
grep -n '<FLAG_NAME>' docs/operations/behavior-flags-courseforge.md
```

Ownership and the per-prefix index live in the root `CLAUDE.md`
§ Opt-In Behavior Flags.

---

Courseforge-owned env-var toggles. Every `COURSEFORGE_*_*` flag is no-op when `COURSEFORGE_TWO_PASS` is unset, **except** `COURSEFORGE_ALLOW_TEMPLATE_EMITTER` (the anti-silent-template guard escape hatch, which applies to every content_generation run), `COURSEFORGE_RUN_ID` (a provenance label, read on every emit), and `COURSEFORGE_PAGE_MATHJAX` (a page-render include, applied on every `_wrap_page` emit — single-pass and two-pass alike). This table is the authority for the `COURSEFORGE_*` / `COURSEPLANNER_*` / `TEXTBOOK_SYNTHESIS_*` prefixes — `schemas/ONTOLOGY.md` § 12 and `docs/operations/behavior-flags.md` both point back here rather than restating the rows. The three `ED4ALL_REWRITE_*` rows below are rewrite-tier flags owned by this file by the same convention.

| Flag | When on |
|------|---------|
| `COURSEFORGE_ALLOW_TEMPLATE_EMITTER` | Escape hatch for the anti-silent-template guard (`lib/validators/content_authorship.py::ContentAuthorshipValidator`). When unset (default), the `content_authorship` gate **blocks** any `content_generation` run where the deterministic `generate_week` template emitter produced the pages AND LLM authoring was intended (`COURSEFORGE_PROVIDER` / `COURSEFORGE_TWO_PASS` / `ED4ALL_AGENT_DISPATCH` set, or a constructed provider/router that degraded to the template floor on every page at runtime). Set to `1`/`true` to permit the template emitter anyway — for offline / no-LLM smoke runs where templated pages are acceptable. `_generate_course_content` records the decision (plus `generator_mode`, `template_fallback_fired`, `llm_authored_pages`/`template_fallback_pages`) in `<export>/content_generation_provenance.json`; the gate reads it. NOT gated by `COURSEFORGE_TWO_PASS`. |
| `COURSEFORGE_RUN_ID` | Provenance label stamped into the JSON-LD `blocks[]` `provenance.runId` field by `Courseforge/scripts/generate_course.py::_build_page_metadata` (read via `os.environ.get("COURSEFORGE_RUN_ID", "")`). Default empty string — no run identifier in the emitted metadata. Purely a provenance tag (lets a `blocks[]`-emitting run be traced back to its orchestrator run); selects no provider/model and gates no behavior. NOT gated by `COURSEFORGE_TWO_PASS` — read on every `blocks[]` emit (which is itself gated by `COURSEFORGE_EMIT_BLOCKS`). |
| `COURSEFORGE_BLOCK_ROUTING_PATH` | Absolute or repo-relative path to the `block_routing.yaml` policy file consumed by `Courseforge/router/policy.py::load_block_routing_policy` and `Courseforge/router/router.py::CourseforgeRouter._resolve_spec`. Default `Courseforge/config/block_routing.yaml`; setting this var overrides the default location for ops topologies that ship the policy out-of-tree (e.g. mounted ConfigMap in containerised runs). The file itself is optional — when missing, the loader returns an empty policy and the router falls through to tier-default env vars (`COURSEFORGE_OUTLINE_*` / `COURSEFORGE_REWRITE_*`) plus the hardcoded defaults table at `policy.py::DEFAULT_BLOCK_ROUTING`. Schema (Draft 2020-12, `additionalProperties: false`): `schemas/courseforge/block_routing.schema.json`. Resolution priority (high → low): per-call kwargs > YAML > tier env vars > hardcoded defaults. **Per-block-type validator matrix** (active): the YAML now carries a per-block-type `validators` matrix (`{required[], optional[], fail_action}`) for every entry in `Courseforge/scripts/blocks.py::BLOCK_TYPES`. `CourseforgeRouter._dispatch_validation_chain` filters the global `validation_gates` list down to the per-block_type allowed set; required-gate failures stamp `fail_action` (`regenerate`/`escalate`/`block`) onto the resulting `GateResult`, optional-gate failures emit warning-only (`GateResult.action` coerced to `pass`, issues preserved), and gates not in either array are skipped silently. Resolution priority for the validator matrix mirrors the provider/model chain: per-block-id override (YAML `overrides[]`) > per-block-type `validators` block (YAML `blocks.<type>.validators`) > tier env defaults > hardcoded `DEFAULT_BLOCK_ROUTING` table. Backward-compat: a block_type with no validators block falls through to the legacy "all gates run" behavior. No-op when `COURSEFORGE_TWO_PASS` is unset (default off). |
| `COURSEFORGE_PAGE_MATHJAX` | Page-render MathJax v3 include: **DEFAULT ON** — a documented deviation from the Courseforge default-off convention (mirrors how `ED4ALL_GPU_LIFECYCLE` documents its default-ON deviation). Rendered course pages carry inline `\( … \)` / display `\[ … \]` LaTeX math (e.g. 415/659 blocks on a representative math corpus) but ship no math renderer, so a standalone / preview page shows raw LaTeX source and LMSes vary in whether they inject their own MathJax. When on, `Courseforge/scripts/generate_course.py::_wrap_page` injects — before `</head>` — an inline config (`inlineMath: [['\(','\)']]`, `displayMath: [['\[','\]']]`, `options.enableAssistiveMml: true` for screen-reader-accessible math) plus the combined `tex-chtml` loader `<script id="MathJax-script" async src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-chtml.js">`. **Presentation-only** — no LLM / output-content change beyond the two script tags — which is why default-ON is safe (the include cannot change authored content, only how it renders). Applies to BOTH the legacy single-pass emit and the two-pass rewrite path (the rewrite phase routes its per-page shell through `_wrap_page` in `MCP/tools/pipeline_tools.py`), and the include survives IMSCC packaging byte-for-byte (`package_multifile_imscc.py` `zf.write`s page bytes as-is; no re-render/strip) and adds no WCAG issue (`lib/validators/wcag.py` does not flag script tags). Read each call so tests can `setenv`-toggle. Parse-with-fallback (mirrors the default-ON rewrite-tier resolvers): explicit falsey `0`/`false`/`no`/`off` (case-insensitive) omits the include **byte-identically**; unset / garbage / truthy → on. **CDN / offline caveat:** the loader is a jsDelivr CDN URL, so an offline / CSP-locked-down / air-gapped LMS install will NOT fetch it and math stays as raw source there — acceptable, since an LMS that hosts its own MathJax still renders the LaTeX; disable via `=off` for a fully self-contained offline deliverable. Selects no provider/model → no `docs/LICENSING.md` row. NOT gated by `COURSEFORGE_TWO_PASS`. |
| `COURSEFORGE_EMIT_BLOCKS` | Intermediate Block format: `true` / `false`, default `false`. When truthy, `Courseforge/scripts/generate_course.py::_build_page_metadata` emits the new top-level JSON-LD `blocks[]` (canonical projection of the `Block` dataclass via `Block.to_jsonld_entry()`) plus `provenance` (`{runId, pipelineVersion: "phase2", tiers[]}`) and `contentHash` (SHA-256 of the canonicalised meta payload before the hash field is added) — and stamps `data-cf-block-id="{page_id}#{block_type}_{slug}_{idx}"` on every block-bearing wrapper (`<section>` / heading / component wrapper). Default off keeps emit byte-stable for the legacy snapshot regression suite under `Courseforge/scripts/tests/`, and Trainforge's `process_course._extract_section_metadata` falls back to the existing `data-cf-*` attribute chain so legacy corpora (and corpora rebuilt with the flag off) extract identically. Cross-link: canonical schema shape at `schemas/knowledge/courseforge_jsonld_v1.schema.json` (`$defs.Block`, `$defs.Touch`, top-level optional `blocks[]` / `provenance` / `contentHash`); dataclass + emitters at `Courseforge/scripts/blocks.py` (`Block` dataclass + the 30-value `BLOCK_TYPES` enum, mapped onto the framework's canonical 15 B-codes via the per-entry `framework_block` field in `Courseforge/config/block_catalog.yaml` — see the IB2 reconciliation map under § "Block format"). Rolls forward to `true` after byte-stable confirmation across a clean run; the flag drops once the two-pass pipeline depends on the new fields unconditionally. |
| `COURSEFORGE_OUTLINE_GRAMMAR_MODE` | Selects the constrained-decoding payload `Courseforge/generators/_outline_provider.py::OutlineProvider._build_grammar_payload` injects into the wire body via `OpenAICompatibleClient.chat_completion(extra_payload=...)`. Values: `gbnf` (llama.cpp `grammar: <gbnf-string>`), `json_schema` (Ollama 0.5+ full-schema `format: <json-schema-dict>` and Together AI `response_format: {type: "json_schema", json_schema: {...}}`), `json_object` (default — Ollama legacy `format: "json"` + OpenAI `response_format: {"type": "json_object"}`), `none` (no grammar payload; falls back to JSON-mode-only + lenient parse). Unset → autodetect from the resolved outline provider (local → `json_object`; vLLM → `gbnf`; Together → `json_schema`; Anthropic → `none`). The Anthropic outline tier (rare; not the default) does not support sample-time grammar and always falls back to JSON-mode + lenient parse + remediation retry. No-op when `COURSEFORGE_TWO_PASS` is unset. On Qwen-2.5-7B-Q4 + Ollama 0.5+, attempt-2 convergence is expected; `COURSEFORGE_OUTLINE_REGEN_BUDGET` default of 10 leaves ample headroom. |
| `COURSEFORGE_OUTLINE_MODEL` | Model identifier the outline-tier provider expects (e.g. `qwen2.5:7b-instruct-q4_K_M` for Ollama, `Qwen/Qwen2.5-7B-Instruct` for vLLM, `meta-llama/Llama-3.1-8B-Instruct-Turbo` for Together). Default `qwen2.5:7b-instruct-q4_K_M` — matches the smaller `LOCAL_SYNTHESIS_MODEL` default so an out-of-box Ollama install on an 8 GB GPU runs the outline tier with no further tuning. Captured per call in the `block_outline_call` decision event so the audit trail records which 7B-class model produced each block draft. Reuses the same `LOCAL_SYNTHESIS_BASE_URL` / `LOCAL_SYNTHESIS_API_KEY` connection plumbing when the outline provider resolves to `local`. (License: model-specific — Qwen2.5-7B/14B/32B are Apache 2.0 and training-permitted; see `docs/LICENSING.md`.) No-op when `COURSEFORGE_TWO_PASS` is unset. |
| `COURSEFORGE_OUTLINE_N_CANDIDATES` | Self-consistency budget per block consumed by `Courseforge/router/router.py::CourseforgeRouter.route_with_self_consistency` via `_resolve_n_candidates`. Default `3`. Higher values trade outline-tier latency / cost for distractor-quality stability; sequential dispatch (not parallel asyncio) for the first cut so per-candidate decision capture stays straightforward — `route()` is called N times, each emit is one `block_outline_call` decision event tagged with its candidate index, and the router selects the highest-scoring passing candidate after running the validator chain against each. When every candidate fails validation across the regen budget, the surviving best-effort candidate is stamped with `escalation_marker="validator_consensus_fail"`. `route_all` (the multi-block dispatch entry point) consults the same `_resolve_n_candidates` chain, so the layer-5 default of `3` fires for every block by default; per-block-type opt-out via `block_routing.yaml::blocks.{type}.n_candidates: 1` (resolves at layer 2) short-circuits a single block-type to direct single-candidate dispatch. No-op when `COURSEFORGE_TWO_PASS` is unset. |
| `COURSEFORGE_OUTLINE_PROVIDER` | Selects the LLM backend for the outline-tier authoring surface (`Courseforge/generators/_outline_provider.py::OutlineProvider`, instantiated by `Courseforge/router/router.py::CourseforgeRouter._get_outline_provider`). Values: `local` (default — ToS-clean local OSS via the shared `OpenAICompatibleClient`; recommended for the outline-tier first draft since the output is later ingested as Trainforge training chunks), `together` (ToS-clean cloud OSS), `anthropic` (ToS-restricted; rarely the right choice for outline tier — the rewrite tier is the natural Anthropic seat), **plus any provider registered in the unified endpoint registry (`config/endpoints.yaml`)** — `groq` / `fireworks` / `deepseek` / any future `kind: openai_compatible` row; adding a provider is a registry-entry change, NOT a subclass (the tier's `SUPPORTED_PROVIDERS` is derived from the base's `_default_supported_providers()`), and the legacy `openai_compatible` alias collapses to `local` at constructor entry. Reuses the same `LOCAL_SYNTHESIS_*` / `TOGETHER_*` / `ANTHROPIC_SYNTHESIS_*` env vars as the synthesis pipelines so one local server serves all three task surfaces. Captured per call in the `block_outline_call` decision event. Per-block-type pinning via `block_routing.yaml` overrides this tier-default (e.g. a block-type-level `outline.provider: local` mapping in YAML wins over this env var). No-op when `COURSEFORGE_TWO_PASS` is unset. (License: see `docs/LICENSING.md`.) |
| `COURSEFORGE_OUTLINE_REGEN_BUDGET` | Per-block outline-tier regeneration cap consumed by `Courseforge/router/router.py::CourseforgeRouter` via `_resolve_regen_budget`. Default `10`. Each time the inter-tier validator chain returns `action="regenerate"` against a block, `Block.validation_attempts` increments and the outline tier re-rolls; once the counter hits the budget and the chain still rejects, the block is stamped with `escalation_marker="outline_budget_exhausted"` and skips the rewrite tier. The `block_routing.yaml` per-type `escalate_immediately: true` short-circuit is a separate path that ALSO sets `escalation_marker="outline_budget_exhausted"` — provenance is carried via `Touch.purpose="escalate_immediately"` to disambiguate from the budget-exhausted path. Legacy validators (returning `action=None, passed=False`) trigger regenerate-loop semantics; only EXPLICIT `action="block"` / `action="escalate"` triggers short-circuits. No-op when `COURSEFORGE_TWO_PASS` is unset. |
| `COURSEFORGE_OUTLINE_MAX_CHUNKS` | Head-K cap on the number of source chunks rendered into a SINGLE outline-tier user prompt (`Courseforge/generators/_outline_provider.py::_resolve_outline_max_chunks`; applied in `OutlineProvider._render_user_prompt`), default **8**. outline-overflow-fix-2026-07: the outline prompt previously rendered EVERY resolved chunk per block (median ~20/block, chunk sections up to ~46k chars) with only a per-chunk 1200-char cap and NO count cap, so on a small-ctx served model the prompt overflowed the window and the local server silently head-truncated it — the model saw only the objectives / closing TAIL and authored wrong-topic outlines. This caps the rendered chunks to a deterministic head-K over the already relevance/citation-ordered list (the per-chunk 1200-char cap is unchanged). Parse-with-fallback: garbage / non-positive / unset → the `8` default (a misconfigured cap never disables the guard). Read once at `OutlineProvider.__init__`. Selects no provider/model → no `docs/LICENSING.md` row. No-op when `COURSEFORGE_TWO_PASS` is unset. |
| `COURSEFORGE_OUTLINE_TRUNCATION_TRIPWIRE` | Outline-tier input-truncation tripwire gate (`Courseforge/generators/_outline_provider.py::_resolve_outline_truncation_tripwire`; fires in `OutlineProvider.generate_outline` via the shared `lib/llm/truncation_guard.py::check_prompt_not_truncated`), default **ON** (output-neutral; escape hatch `=off`/`0`/`false`/`no`). Symmetric with the rewrite tier's `ED4ALL_REWRITE_TRUNCATION_TRIPWIRE`. After each outline dispatch it compares the server-reported `usage.prompt_tokens` against the local 2.5-char/token estimate of system+user prompt; a large shortfall (reported < ½ the estimate, above a 256-tok floor) means the served window silently dropped the prompt HEAD (the block header + source chunks), so the model authored ungrounded. **HARD, NON-RETRYABLE** — re-dispatching the same over-window prompt re-truncates, so `generate_outline` raises `OutlineProviderError(code="outline_input_truncated")` (surfaces as a failed call → the outline loop stamps the block escalated, never silent success). **Fail-OPEN:** no-op when usage is absent / zero (Ollama may omit it) or empty (Anthropic returns no per-request token tally), so a missing signal never blocks a valid call. The served-window number the message + ratio read is `ED4ALL_ANSWER_NUM_CTX` (default 4096, the generic content-gen serving window — the outline tier ships no dedicated window env). Wired into the existing `block_outline_call` decision capture (`success=False` + `last_error` carries the estimate / reported / num_ctx). Garbage / unset / truthy → on (parse-with-fallback, mirroring the default-ON rewrite tripwire). Selects no provider/model → no `docs/LICENSING.md` row. No-op when `COURSEFORGE_TWO_PASS` is unset. |
| `COURSEFORGE_OUTLINE_DISPATCH_BREAKER` | Outline per-block dispatch circuit-breaker threshold (`MCP/tools/pipeline_tools.py::_resolve_outline_dispatch_breaker`, consumed by `_run_content_generation_outline`), default **5** (`0` disables). dispatch-resilience-2026-07: the outline per-block loop wraps `route_with_self_consistency` in a classify-and-retry path — a TRANSIENT dispatch error (endpoint flap under VRAM contention / transport reset / 429 / 5xx / timeout, classified via the shared `MCP/hardening/error_classifier.py` taxonomy) is retried in-loop up to 3 times with a short exponential backoff (~2s/8s/30s); a PERMANENT error (or exhausted transient retries) gives up on the block and stamps the canonical `escalation_marker="outline_dispatch_error"` (a `Courseforge/scripts/blocks.py::_ESCALATION_MARKERS` member — NOT `outline_budget_exhausted`, which is reserved for SEMANTIC regen-budget exhaustion, so postmortems separate endpoint outages from budget exhaustion). This flag governs the CONSECUTIVE-dispatch-failure count (a successful dispatch — even one returning a semantic marker — resets it) at which the loop STOPS and returns a loud phase failure (`error_code="OUTLINE_DISPATCH_CIRCUIT_BREAKER"`) instead of chewing through the rest of the course; already-authored/checkpointed blocks survive on the sidecar so a plain `--resume` re-attempts the marker + never-reached blocks. `0` restores the legacy full-course chew-through (but with the CORRECTED marker). Parse-with-fallback: garbage / negative → the `5` default. Read once at phase start. The per-block give-up + breaker-trip both emit a `content_selection` decision event with a dynamic rationale (block_id / exception class / consecutive count). Selects no provider/model → no `docs/LICENSING.md` row. Active only on the two-pass outline phase (`COURSEFORGE_TWO_PASS=true`). |
| `COURSEFORGE_PROVIDER` | Selects the LLM backend for the Courseforge content-generator surface (`Courseforge/generators/_provider.py::ContentGeneratorProvider`, instantiated by `MCP/tools/pipeline_tools.py::_generate_course_content`). Values: `anthropic` (class default — ToS-restricted for training-data; mirrors `CURRICULUM_ALIGNMENT_PROVIDER`), `together` (ToS-clean cloud OSS via the shared `OpenAICompatibleClient`), `local` (ToS-clean local OSS via the shared `OpenAICompatibleClient`). **Recommended setting for ToS-clean course content is `local`** since the authored HTML is later ingested as Trainforge training chunks. Reuses the same `LOCAL_SYNTHESIS_*` / `TOGETHER_*` / `ANTHROPIC_SYNTHESIS_*` env vars as the synthesis pipeline so one local server serves both task surfaces. Default unset → the legacy deterministic-template path runs (`build_week_data` falls back to SemantiK-source-paragraph synthesis with no LLM call), preserving backward compatibility on every existing run. **Short-circuit semantics:** setting `COURSEFORGE_PROVIDER` to any non-empty value overrides `ED4ALL_AGENT_DISPATCH=true` for the `content-generator` agent only — the executor falls through to the in-process provider call instead of dispatching the Claude Code subagent, while every other agent (course-outliner, oscqr-course-evaluator, etc.) keeps dispatching unchanged. Captured per call in the `content_generator_call` decision event. (License: see `docs/LICENSING.md` for the per-provider ToS + per-model layer.) |
| `COURSEPLANNER_PROVIDER` | Selects the LLM backend for the Courseforge course-outliner surface (`Courseforge/generators/_outliner_provider.py::OutlinerProvider`, instantiated by `MCP/tools/pipeline_tools.py::_plan_course_structure`). Values: `anthropic` (class default — ToS-restricted for training-data), `together`, `local`, **plus any provider registered in the W-D12 `_OPENAI_COMPATIBLE_PROVIDERS` registry at `MCP/orchestrator/llm_backend.py`** (`groq`, `fireworks`, `deepseek`, …). Adding a new OpenAI-compatible provider is a registry-entry change — **NOT** a new subclass — per the W-D12 dynamic-references contract. The class consults the registry at construction time, so a new entry flows through without a code edit here. **Recommended setting for ToS-clean course planning is `local`** (Apache 2.0 Qwen) since the synthesised objective text lands in `synthesized_objectives.json` and propagates to every downstream chunk's `learning_outcome_refs[]` — i.e. it touches training data. Reuses the same `LOCAL_SYNTHESIS_*` / `TOGETHER_*` / `ANTHROPIC_SYNTHESIS_*` env vars as `COURSEFORGE_PROVIDER` so one local server serves both task surfaces. Override the model via `COURSEPLANNER_MODEL`. Default unset → the legacy Claude Code subagent dispatch fires (when `ED4ALL_AGENT_DISPATCH=true`) or the deterministic `synthesize_objectives_from_topics` path runs, preserving backward compatibility on every existing run. **Short-circuit semantics:** setting `COURSEPLANNER_PROVIDER` to any non-empty value overrides `ED4ALL_AGENT_DISPATCH=true` for the `course-outliner` agent only — the executor falls through to `_plan_course_structure`, which constructs an `OutlinerProvider` and uses it in lieu of the deterministic synthesizer. Other Wave-74 agents (content-generator, oscqr-course-evaluator, etc.) keep dispatching unchanged. Captured per call in the `course_outline_call` decision event whose rationale interpolates the runtime provider name (NOT a static label) per the W-D12 dynamic-rationale contract. (License: see `docs/LICENSING.md` for the per-provider ToS + per-model layer.) |
| `COURSEPLANNER_MODEL` | Model-ID override for the `OutlinerProvider` backend selected by `COURSEPLANNER_PROVIDER` (`Courseforge/generators/_outliner_provider.py::OutlinerProvider`, env symbol `ENV_MODEL`). Resolution chain (high → low): per-call kwarg > `COURSEPLANNER_MODEL` > the per-backend baseline (resolved by the base from the `LOCAL_SYNTHESIS_*` / `TOGETHER_*` / `ANTHROPIC_SYNTHESIS_*` synthesis env vars). Captured per call in the `course_outline_call` decision event alongside the resolved provider. No-op when `COURSEPLANNER_PROVIDER` is unset. (License: model-specific — Qwen2.5-7B/14B/32B are Apache 2.0 and training-permitted; see `docs/LICENSING.md`.) |
| `TEXTBOOK_SYNTHESIS_PROVIDER` | Selects the LLM backend for the three-stage large-LLM textbook synthesis surface (`Courseforge/generators/_textbook_synthesis_provider.py::TextbookSynthesisProvider`, instantiated by the three post-conversion phase handlers in `MCP/tools/pipeline_tools.py` — `_extract_textbook_structure` for Stage 1, `_run_concept_extraction` for Stage 3, `_plan_course_structure` for Stage 2 + reconciliation). Values: `anthropic` (class default — ToS-restricted for training-data), `together`, `local`, **plus any provider registered in the W-D12 `_OPENAI_COMPATIBLE_PROVIDERS` registry at `MCP/orchestrator/llm_backend.py`** (`groq`, `fireworks`, `deepseek`, …). Adding a new OpenAI-compatible provider is a registry-entry change — **NOT** a new subclass — per the W-D12 dynamic-references contract. **Recommended setting for a ToS-clean corpus is `local`** (Apache 2.0 Qwen): the Stage-3 domain-concept vocabulary propagates into chunk `concept_tags[]` and the Stage-1/2 synthesized objectives propagate into every downstream chunk's `learning_outcome_refs[]` — i.e. this surface touches training data. Reuses the same `LOCAL_SYNTHESIS_*` / `TOGETHER_*` / `ANTHROPIC_SYNTHESIS_*` env vars as `COURSEPLANNER_PROVIDER` so one local server serves every task surface. Override the model via `TEXTBOOK_SYNTHESIS_MODEL`. **Default unset → no in-process textbook-synthesis provider runs:** Stage 1 emits `textbook_structure.json` byte-identical (no new keys), Stage 3 runs with empty `domain_concept_seeds` (status quo), Stage 2 runs the deterministic `synthesize_objectives_from_topics` path (or the `COURSEPLANNER_PROVIDER`/`OutlinerProvider` path if THAT env var is set) with no reconciliation step — every existing run is unaffected. **Marketable-v1 A5: auto-on for `textbook_to_course` / `course_generation` runs.** `MCP/core/workflow_runner.py::_apply_corpus_generalization_defaults` sets this env (only when unset) to the run's resolved provider — `LLM_PROVIDER` (the global routing provider, set by the CLI `--provider` / GUI request) > `local` (license-clean default) — so a fresh CLI/GUI textbook run gets three-stage synthesis (and therefore dynamic CURIE minting, which rides on the Stage-3 `domain_concept_vocabulary.json` → outline/rewrite `minted_curie_map`) by default. setdefault semantics: an explicitly-pinned `TEXTBOOK_SYNTHESIS_PROVIDER` is honored verbatim, and a bare library call that never reaches `run_workflow` keeps the default-unset deterministic-fallback contract. This aligns with the A4-promoted critical gates (`textbook_outline_enrichment`, `domain_concept_vocabulary`, `chapter_objective_coverage`) whose pass-evidence came from synthesis-on runs and which skip-with-pass when this env is unset. **Short-circuit semantics:** each of the three phase handlers checks `os.environ.get("TEXTBOOK_SYNTHESIS_PROVIDER","").strip()` for a non-empty value before constructing the provider; an empty/unset value short-circuits to the deterministic fallback. Captured per call in the `textbook_outline_call` (Stage 1), `textbook_concept_call` (Stage 3), `chapter_objective_call` (Stage 2), and `terminal_objective_reconciliation` (reconcile) decision events whose rationales interpolate the runtime-resolved provider name (NOT a static label) per the W-D12 dynamic-rationale contract. (License: see `docs/LICENSING.md` for the per-provider ToS + per-model layer.) See § "Three-stage textbook synthesis" below for the architecture. |
| `TEXTBOOK_SYNTHESIS_MODEL` | Model-ID override for the `TextbookSynthesisProvider` backend selected by `TEXTBOOK_SYNTHESIS_PROVIDER`. Resolution chain (high → low): per-call kwarg > `TEXTBOOK_SYNTHESIS_MODEL` > the per-backend baseline (`claude-sonnet-4-6` for Anthropic, `qwen2.5:7b-instruct-q4_K_M` for local, the Together OSS default for `together`). Captured per call in the four `textbook_*` decision events alongside the resolved provider. No-op when `TEXTBOOK_SYNTHESIS_PROVIDER` is unset. (License: model-specific — Qwen2.5-7B/14B/32B are Apache 2.0 and training-permitted; see `docs/LICENSING.md`.) |
| `TEXTBOOK_SYNTHESIS_NUM_CTX` | W2 §1.5 serving-window token budget for the Stage-2 **window-map** synthesis surface (`Courseforge/generators/_textbook_synthesis_provider.py` `_SYNTHESIS_NUM_CTX_ENV`, consumed by `MCP/tools/pipeline_tools.py::_run_stage2_window_synthesis`). When unset, the budget resolves via `lib/retrieval/_prompts.py::resolve_num_ctx()` (which reads `ED4ALL_ANSWER_NUM_CTX`, default 4096), so one knob can drive both the grounded-answer path and the synthesis window budget — but synthesis gets its OWN override here. Drives `lib/objectives/chunk_window.py::group_chunks_into_windows`, which sizes each chunk-window's prompt (reusing the `_prompts.py` 2.5 chars/token math + drop-trailing-whole discipline) so a 7B serving window never silently head-truncates the chapter prose into fabrication. Garbage / non-positive values fall back to the resolved default. Selects no provider/model, so N/A for `docs/LICENSING.md`. No-op when `TEXTBOOK_SYNTHESIS_PROVIDER` is unset. |
| `TEXTBOOK_SYNTHESIS_GRAMMAR_MODE` | W2 §3 constrained-decoding mode for the Stage-2 window-objectives surface (`TextbookSynthesisProvider._build_synthesis_grammar_payload`). Mirrors `COURSEFORGE_OUTLINE_GRAMMAR_MODE`. Values: `gbnf` (reuses the outline tier's `_GENERIC_JSON_GBNF`), `json_schema` (Ollama 0.5+ `format: <window-objectives-schema>` / Together `response_format`), `json_object`, `none`. Unset → autodetect from the resolved provider + base_url (`local`/Ollama → `format` full-schema; vLLM → `guided_json`; llama.cpp/LM Studio → GBNF; Together → `response_format json_schema`; Anthropic → none). The single `_WINDOW_OBJECTIVES_SCHEMA` carries `source_chunk_ids` `minItems: 1`, so the grammar forbids the empty citation list at sample time; post-parse ⊆-enforcement (against the supplied window ids) is the backstop. Selects no provider/model, so N/A for `docs/LICENSING.md`. No-op when `TEXTBOOK_SYNTHESIS_PROVIDER` is unset. |
| `TEXTBOOK_SYNTHESIS_TIMEOUT_SECONDS` | Per-request HTTP timeout (float seconds) for the three-stage `TextbookSynthesisProvider` LLM calls (`Courseforge/generators/_textbook_synthesis_provider.py` `ENV_TIMEOUT`). A live needle-probe measured 7B calls already exceeding the `OpenAICompatibleClient` 60s default and 14B + 4096-token runs ~120-300s, so the provider sources a generous **300s** default from this env. Resolution (high → low): per-call `timeout` kwarg > `TEXTBOOK_SYNTHESIS_TIMEOUT_SECONDS` > the `300.0` provider default. Garbage / non-positive → the default (parse-with-fallback). No-op when `TEXTBOOK_SYNTHESIS_PROVIDER` is unset. Selects no provider/model → no `docs/LICENSING.md` row. |
| `TEXTBOOK_SYNTHESIS_MAX_TOKENS` | Per-call generation cap (`max_tokens`) for the three-stage `TextbookSynthesisProvider` LLM calls (`Courseforge/generators/_textbook_synthesis_provider.py` `ENV_MAX_TOKENS`, resolver `_resolve_synthesis_max_tokens`). Whole-chapter objective / structure synthesis emits multiple TO/CO statements + sub-objectives; a REASONING model (nemotron_v3) that leaks residual thinking tokens overruns a tight budget and trips the `OpenAICompatibleClient` `finish_reason='length'` anti-truncation guard, failing the `objective_extraction` (and all downstream authoring) stages. Default **4096** (generous — an 800-style floor truncates multi-objective output). Resolution (high → low): per-call `max_tokens` kwarg > `TEXTBOOK_SYNTHESIS_MAX_TOKENS` > the `4096` provider default. Garbage / non-positive → the default (parse-with-fallback). NB: for a reasoning model ALSO set `ED4ALL_REASONING_THINKING_OFF=1` (root cross-cutting table) — that is now wired through the `_base.py::_dispatch_call_with_usage` local/together/nvidia dispatch path (previously it bypassed the `chat_completion` injection), so `chat_template_kwargs.enable_thinking=false` reaches the textbook/objective/concept synthesis calls. No-op when `TEXTBOOK_SYNTHESIS_PROVIDER` is unset. Selects no provider/model → no `docs/LICENSING.md` row. |
| `COURSEFORGE_ENFORCE_JSONLD_SCHEMA` | unset (off) — emit-time JSON-LD schema enforcement (`Courseforge/scripts/generate_course.py::_jsonld_enforcement_enabled`, consumed in `_validate_page_jsonld`; read each call so tests can `setenv`-toggle). Default OFF → a page whose JSON-LD fails `schemas/knowledge/courseforge_jsonld_v1.schema.json` is LOGGED (non-fatal, byte-stable). When truthy (`1`/`true`/`yes`/`on`), the validation RAISES at emit time (fail-closed). Selects no provider/model → no `docs/LICENSING.md` row. Falsey / garbage → off (parse-with-fallback). |
| `COURSEFORGE_ENFORCE_SHACL` | unset (off) — symmetric emit-time SHACL enforcement (`Courseforge/scripts/generate_course.py::_shacl_enforcement_enabled`). Default OFF → a SHACL shape violation on the emitted page graph is LOGGED (non-fatal). When truthy (`1`/`true`/`yes`/`on`), the violation RAISES at emit time. Selects no provider/model → no `docs/LICENSING.md` row. Falsey / garbage → off (parse-with-fallback). |
| `COURSEFORGE_REWRITE_MODEL` | Model identifier the rewrite-tier provider expects (e.g. `claude-sonnet-4-6` for Anthropic, `qwen2.5:7b-instruct-q4_K_M` for a 7B local model, `meta-llama/Llama-3.3-70B-Instruct-Turbo` for Together). Default `claude-sonnet-4-6` — pedagogy-rich rewrite of a structurally-shaped outline draft is the model class where the cloud provider's instruction-following margin pays for itself, especially on `assessment_item` / `prereq_set` / `misconception` blocks where misconception-targeted distractor authoring drives the entire downstream eval signal. Captured per call in the `block_rewrite_call` decision event. Per-block-type pinning via `block_routing.yaml` overrides this default (e.g. `flip_card_grid` is structurally simple enough that the local 7B model handles rewrite at parity, and `assessment_item` MUST stay on Anthropic to avoid silent regression — the shipped policy file pins both). (License: see `docs/LICENSING.md`.) No-op when `COURSEFORGE_TWO_PASS` is unset. |
| `COURSEFORGE_REWRITE_PROVIDER` | Selects the LLM backend for the rewrite-tier authoring surface (`Courseforge/generators/_rewrite_provider.py::RewriteProvider`, instantiated by `Courseforge/router/router.py::CourseforgeRouter._get_rewrite_provider`). Values: `anthropic` (default — ToS-restricted for training-data; the rewrite tier is the natural Anthropic seat because the rewrite-tier output is the canonical published HTML, which Trainforge re-ingests as chunks but with a clear training-data licensing posture documented in `docs/LICENSING.md`), `together` (ToS-clean cloud OSS), `local` (ToS-clean — pair with a 14B+ model via `COURSEFORGE_REWRITE_MODEL` for acceptable rewrite-tier quality on multi-step reasoning blocks), `claude_session` (Wave6 — dispatches the rewrite tier to the `content-generator` Claude Code subagent via `MCP/orchestrator/local_dispatcher.py::LocalDispatcher`; subagent inherits the Wave4b `model: sonnet` frontmatter pin + Wave4-W27 MANDATORY directives from `Courseforge/agents/content-generator.md`, so a Claude Max session can drive the rewrite tier with no `ANTHROPIC_API_KEY`), **plus any provider registered in the unified endpoint registry (`config/endpoints.yaml`)** — `groq` / `fireworks` / `deepseek` / `nvidia-deepseek` / any future `kind: openai_compatible` row; adding a provider is a registry-entry change, NOT a new subclass (the tier's `SUPPORTED_PROVIDERS` is derived from the base's `_default_supported_providers()`). The legacy `openai_compatible` alias collapses to `local` at constructor entry. The `claude_session` backend requires a `LocalDispatcher` injected by the workflow runner / MCP tool layer; standalone CLI runs raise `RuntimeError` at provider construction. Reuses the same `LOCAL_SYNTHESIS_*` / `TOGETHER_*` / `ANTHROPIC_SYNTHESIS_*` env vars as the synthesis pipelines. Captured per call in the `block_rewrite_call` decision event. Per-block-type pinning via `block_routing.yaml` overrides this tier-default. No-op when `COURSEFORGE_TWO_PASS` is unset. (License: see `docs/LICENSING.md`.) |
| `COURSEFORGE_REWRITE_REGEN_BUDGET` | Per-block rewrite-tier regeneration cap consumed by `Courseforge/router/router.py::CourseforgeRouter._resolve_rewrite_regen_budget` and the `route_rewrite_with_remediation` loop. Default `10` — matches `COURSEFORGE_OUTLINE_REGEN_BUDGET` because the post-rewrite validator chain runs the same four shape-discriminating `Block*Validator` adapters as the inter-tier seam (CURIE / content_type / page_objectives / source_refs), so the same budget makes sense across both seams. Each time the post-rewrite validator chain returns `action="regenerate"` against a block, the rewrite tier re-rolls with the remediation suffix appended to the prompt (`Courseforge/router/remediation.py`); once the budget is exhausted and the chain still rejects, the surviving best-effort candidate is stamped with `escalation_marker="validator_consensus_fail"` (semantic mirror of the outline-tier consensus-failure marker — the consumer-side handling is identical so the marker is reused rather than minting a new value). Per-block-type override via `block_routing.yaml`'s `regen_budget_rewrite` field wins over this env var. No-op when `COURSEFORGE_TWO_PASS` is unset. |
| `COURSEFORGE_REWRITE_CONCURRENCY` | Opt-in rewrite-tier per-block dispatch concurrency, default `1` (sequential — the EXACT legacy path; no thread pool constructed, byte-identical output). Consumed by `MCP/tools/pipeline_tools.py::_run_content_generation_rewrite`. An int `> 1` fans the per-block `CourseforgeRouter.route_rewrite_with_remediation` dispatch out across a bounded `ThreadPoolExecutor` (pool sized to `min(knob, n_blocks)`) so the I/O-bound per-block LLM rewrite HTTP call (cloud, ~2.7s, parallelizes well) overlaps across blocks — a 100-block course drops from ~10-13 min sequential to minutes. **Determinism:** results are collected into an index-keyed dict and re-assembled in the ORIGINAL block order, so `blocks_final.jsonl` is identical regardless of completion order. **Thread safety:** the shared GPU-resident singletons — the NLI/DeBERTa classifier + sentence-transformer embedder reached via the best-of-N `score_candidate` seam — are serialized by the module-level `Courseforge/router/router.py::_NLI_SCORE_LOCK` (only one block in the GPU forward pass at a time; the slow LLM call still overlaps), and per-block `DecisionCapture` appends/JSONL writes are serialized by an in-process `threading.RLock` on the shared capture (`lib/decision_capture.py::DecisionCapture._log_lock`) so concurrent writes never interleave or drop an event (the POSIX `flock` there is inter-process only and a no-op on WSL2). The per-block cache (resume reuse) and FAIL-SOFT-per-block isolation (one block's dispatch error yields a `validator_consensus_fail`-marked block, never aborting siblings) are preserved under concurrency. The flag is read ONCE at phase start, so a change does not affect an already-running rewrite phase. Garbage / `< 1` → `1` (parse-with-fallback, mirroring `ED4ALL_ANSWER_NUM_CTX`). Selects no provider/model → no `docs/LICENSING.md` row. Active only on the two-pass rewrite phase (`COURSEFORGE_TWO_PASS=true`). |
| `COURSEFORGE_OUTLINE_CONCURRENCY` | Opt-in outline-tier per-block dispatch concurrency, default `1` (sequential — the EXACT legacy path; no thread pool constructed, byte-identical output). Consumed by `MCP/tools/pipeline_tools.py::_run_content_generation_outline`; the symmetric twin of `COURSEFORGE_REWRITE_CONCURRENCY` (same parse, pool, reorder, and locking contracts). An int `> 1` fans the per-block `CourseforgeRouter.route_with_self_consistency` dispatch out across a bounded `ThreadPoolExecutor` (pool sized to `min(knob, n_blocks)`) so the I/O-bound per-block outline LLM call overlaps across blocks; the ~3 self-consistency candidates per block stay SEQUENTIAL within their block — only blocks fan out. **Determinism:** results are collected into an index-keyed dict and re-assembled in the ORIGINAL block order, so the emitted outline blocks are identical regardless of completion order. **Thread safety:** the shared GPU-resident singletons — the NLI/DeBERTa classifier + sentence-transformer embedder reached via the inter-tier validator chain (statistical-tier similarity gates, Bloom classifier) and the best-of-N `score_candidate` seam — are serialized by the module-level `Courseforge/router/router.py::_NLI_SCORE_LOCK` (the per-candidate `_run_validator_chain` call in `route_with_self_consistency` acquires it; only one block in the GPU forward pass at a time while the slow LLM call still overlaps), and per-block `DecisionCapture` appends/JSONL writes (incl. the per-call `block_outline_call` events) are serialized by the in-process `threading.RLock` on the shared capture (`lib/decision_capture.py::DecisionCapture._log_lock`). Preserved under concurrency: the fingerprinted resume sidecar (`.blocks_outline_checkpoint.jsonl` — each worker appends its own whole-line JSONL entry the moment its block completes via `_append_block_checkpoint`, which opens+closes per call; resume dedupes by `block_id`), graceful-stop semantics (pre-armed sentinel refuses the fan-out with zero dispatches; a mid-fanout stop makes workers RETURN `STOP_MARKER`, the drain keeps every resolved block, then `GracefulStopRequested` raises), FAIL-SOFT-per-block isolation (one block's dispatch error yields an `outline_dispatch_error`-marked block, never aborting siblings), and the `COURSEFORGE_OUTLINE_DISPATCH_BREAKER` circuit breaker (counted in completion order under the fan-out; the trip is evaluated after the drain so authored blocks stay checkpointed, halt envelope identical to sequential). The flag is read ONCE at phase start, so a change does not affect an already-running outline phase. Garbage / `< 1` → `1` (parse-with-fallback, mirroring `COURSEFORGE_REWRITE_CONCURRENCY`). Selects no provider/model → no `docs/LICENSING.md` row. Active only on the two-pass outline phase (`COURSEFORGE_TWO_PASS=true`). |
| `COURSEFORGE_REWRITE_BATCH` | **Multi-block BATCHED rewrite-tier gate** — defeats the hosted endpoint's ~req/MINUTE rate cap (`Courseforge/generators/_rewrite_batch.py::resolve_rewrite_batch_mode`; consumed by `MCP/tools/pipeline_tools.py::_run_content_generation_rewrite` + `Courseforge/router/router.py::route_rewrite_batch`). Mirrors SemantiK's `SEMANTIK_SPECIALIST_BATCH`. **Default ON for the cloud lane:** when on AND the resolved rewrite spec dispatches to a hosted CLOUD endpoint (`CourseforgeRouter._is_cloud_rewrite_lane` — a non-loopback `base_url` host OR an `anthropic`/`together`/`nvidia` provider seat), blocks are packed into multi-block POSTs (one POST per batch, the limit is requests not tokens) via `RewriteProvider.generate_rewrite_batch` instead of one POST per block (the hundreds of per-block POSTs that HTTP-429 on ~every call). Each block travels in a delimited `<<<CF_BLOCK id="…">>> … <<<CF_BLOCK_END id="…">>>` envelope carrying its full single-block authoring prompt; the response is split back per-block (a missing/malformed slot → the same `None` fail-soft sentinel as the per-block path, re-batched by the round loop). The router runs each block's EXISTING per-block-type validator chain post-parse; a FAILED block is re-batched ALONE-with-other-failures (passing siblings are never re-rolled), bounded by `COURSEFORGE_REWRITE_REGEN_BUDGET`; survivors after the budget are stamped `validator_consensus_fail`. Batching is scoped to a single `(provider, model, max_tokens)` group so per-block-type routing is honored. Default-ON parse semantics: explicit falsey (`0`/`false`/`no`/`off`) → the EXACT legacy per-block path (byte-stable; `generate_rewrite_batch` never called); unset / truthy / garbage → on. Only consulted on the cloud lane — a loopback (local Ollama/vLLM) lane always uses the per-block path. The cache / key-terms short-circuit / per-block checkpoint / `_apply_str_backstops` / index-keyed reorder are all preserved. Selects no provider/model → no `docs/LICENSING.md` row. Active only on the two-pass rewrite phase (`COURSEFORGE_TWO_PASS=true`). |
| `COURSEFORGE_REWRITE_BATCH_SIZE` | Maximum number of blocks packed into ONE batched rewrite POST (`Courseforge/generators/_rewrite_batch.py::resolve_rewrite_batch_size`, consumed by `pack_rewrite_batches`), default **3**. A new batch is also started early when the running batch's summed per-block output budget (each block's resolved rewrite `max_tokens`, default 4096) would exceed the output-token cap (16384 − 512 margin), so a run of big-output blocks automatically gets smaller batches and an oversized single block gets its own one-block batch. Larger values cut total POSTs (the rate-limit win) at the cost of a bigger per-call prompt. Parse-with-fallback: non-int / non-positive / garbage → `3`. No-op when `COURSEFORGE_REWRITE_BATCH` is off / the lane is local. No provider/model selection — no licensing row. |
| `COURSEFORGE_REWRITE_BATCH_K` | Candidate-K cap on the BATCHED cloud lane (`Courseforge/generators/_rewrite_batch.py::resolve_rewrite_batch_k`; applied as the top-1 clamp in `route_rewrite_with_remediation`), default → **clamp to 1** (top-1 on cloud). Best-of-N multiplies POSTs by N and re-creates the rate-limit pressure batching defeats, so on the cloud lane the caller's N is clamped down to 1 by default; a positive int pins the cap explicitly. Never raises K above what the caller requested. Parse-with-fallback: garbage / non-positive → the default-1 cap. No-op when `COURSEFORGE_REWRITE_BATCH` is off / the lane is local (full N runs on the per-block / local path). No provider/model selection — no licensing row. **Cloud-auto concurrency default:** on the batched cloud lane the batch fan-out uses a LOW default of **2** (`resolve_rewrite_batched_concurrency`) because each POST already packs several blocks; an explicitly-set `COURSEFORGE_REWRITE_CONCURRENCY` is honoured as the override (explicit value always wins). |
| `ED4ALL_REWRITE_FIT_WINDOW` | Rewrite-overflow fit-window gate (`Courseforge/generators/_rewrite_fit_window.py::resolve_fit_window`; consumed in `Courseforge/generators/_rewrite_provider.py::RewriteProvider.__init__`), default **OFF**. Fixes a proven correctness bug: on an 8k-window local 7B the untrimmed rewrite-tier system prompt (≈7,800 tok) + the user scaffold + output budget overflows the served window BEFORE a single grounding chunk, so the local server silently head-truncates and drops the system-prompt authoring CONTRACT — the 7B then authors with source but no rules (the 7B→Sonnet parity gap). When OFF (default) → byte-identical to today: the constructor uses the untrimmed `_REWRITE_SYSTEM_PROMPT`, `_block_type_output_contract` returns the original per-type contract, and `_format_source_chunks` renders every chunk (no chunk-window budget). When truthy (`1`/`true`/`yes`/`on`), this gate JOINTLY enables (c) the **system-prompt trim** — the ≈44% block-type-specific segments (styled-components, the assessment_item MCQ example, the self_check reveal rules, the concept/vocab tables, the numeric-distractor rules) are RELOCATED out of the global system prompt into the per-block-type output contract (only the relevant block sees its rules; derivation is structural — the authoritative `_REWRITE_SYSTEM_PROMPT` bytes are split on `\n\n` and re-composed, so the kept text is byte-identical) — and (d) the **whole-prompt budget** (`select_chunks_under_budget`): the grounding is trimmed to `num_ctx − sys − scaffold − reserve − max_tokens`, cited-chunks-first (from `key_claims[].source_chunk_ids`) then cosine-ranked via the statistical-tier embedder (embedding-absent → citation-order fallback), always-keep-≥1, drop-trailing-whole, per-chunk cap 1,200 chars. **Whole-prompt budgeting (rewrite-overflow-fix-2026-07):** the `scaffold` term is now MEASURED by rendering the block's user prompt with an EMPTY chunk list (the real non-chunk cost — block context + outline dict + per-claim + objectives + contract + closing instructions) instead of a fixed estimate, so a huge outline payload can no longer smuggle an over-window prompt past a chunk-only budget. Applied on BOTH render paths (standard + escalated). When the non-chunk scaffold ALONE (`sys + scaffold + reserve`) exceeds `num_ctx`, the block is stamped `rewrite_scaffold_overflow` and NEVER dispatched (a prompt that cannot fit would silently head-truncate the authoring CONTRACT). A final pre-dispatch deterministic check (`check_prompt_fits_window`, fit-window ON only) also short-circuits to `input_prompt_truncated` if the assembled prompt still estimates above the window. **Anti-fabrication:** chunk selection only DROPS from the block's real chunk universe (never invents); every dropped CITED chunk is recorded on `dropped_cited_chunk_ids` + the `block_rewrite_call` decision capture. Selects no provider/model → no `docs/LICENSING.md` row. Falsey / garbage → off (parse-with-fallback, mirroring `ED4ALL_ANSWER_NUM_CTX`). Active only on the two-pass rewrite surface (`COURSEFORGE_TWO_PASS=true`). |
| `ED4ALL_REWRITE_NUM_CTX` | Rewrite-tier served-window token budget (`Courseforge/generators/_rewrite_fit_window.py::resolve_rewrite_num_ctx`), default **8192** (matches the qwen2.5-7b-8k Modelfile the local lane ships). **Single source of truth (rewrite-overflow-fix-2026-07):** this ONE value drives (a) the chunk-window budget (when `ED4ALL_REWRITE_FIT_WINDOW` is on), (b) BOTH the pre- and post-dispatch input-truncation tripwires, AND (c) the SERVED window itself — the rewrite dispatch now emits `options: {"num_ctx": <this>}` on the OpenAI-compatible REQUEST for the **local loopback** lane (`RewriteProvider._num_ctx_options_payload`; `generate_rewrite` + `generate_rewrite_batch`). Previously the request omitted num_ctx, so a local Ollama/vLLM/llama.cpp server served ITS OWN default window (often 8192) even when the Modelfile / `OLLAMA_CONTEXT_LENGTH` said 16384 — a split-brain that silently head-truncated while the tripwire message reported the assumed larger value. Now the request, the budget, and the tripwires all resolve this same number. (Cloud/anthropic/claude_session seats never receive `options.num_ctx` — they carry a large native context and may reject an unknown field; a server that ignores the option is no worse than before, since the pre/post-dispatch tripwires remain the deterministic backstop.) Set it to the model server's ACTUAL window (e.g. `16384` for a long-context Modelfile, matching `OLLAMA_CONTEXT_LENGTH`) so the budget shrinks the grounding to fit. The design is window-parametric: targeting a larger window is a one-line env flip (+ the Modelfile). Garbage / non-positive → `8192` (parse-with-fallback, mirroring `resolve_num_ctx`). Selects no provider/model → no `docs/LICENSING.md` row. |
| `ED4ALL_REWRITE_TRUNCATION_TRIPWIRE` | Rewrite-tier input-truncation tripwire gate (`Courseforge/generators/_rewrite_fit_window.py::resolve_truncation_tripwire`; fires in `RewriteProvider.generate_rewrite` + `generate_rewrite_batch` via the shared `lib/llm/truncation_guard.py::check_prompt_not_truncated` + `check_prompt_fits_window`), default **ON** (output-neutral; escape hatch `=off`/`0`/`false`/`no`). **Two arms (rewrite-overflow-fix-2026-07):** (1) a PRE-dispatch deterministic check (`check_prompt_fits_window`, fit-window ON only) short-circuits BEFORE the round-trip when the local estimate exceeds `num_ctx`; (2) a POST-dispatch check compares the server-reported `usage.prompt_tokens` against the local 2.5-char/token estimate — it trips on EITHER the legacy SEVERE shortfall (reported < ½ the estimate) OR a MID-SIZE truncation (reported materially below the estimate — < 0.75× — AND sitting within ±5% of the served-window cap `num_ctx`). The mid-size arm closes the gap where an 8192-served window on a ~9k–16k estimate reports ~8194 (> estimate/2, so the severe arm passed) yet the HEAD was silently dropped. Both arms above a 256-tok floor. A trip means the served window dropped the prompt HEAD (the authoring CONTRACT), so the model authored ungrounded. **HARD, NON-RETRYABLE** — re-dispatching the same over-window prompt re-truncates, so the block is stamped `escalation_marker='input_prompt_truncated'` (a `Courseforge/scripts/blocks.py::_ESCALATION_MARKERS` member → surfaces as `escalated` in the validation report, routes through the W5 packager-side escalation filter) and the parse-retry loop is short-circuited. **Fail-OPEN:** no-op when usage is absent / zero (Ollama may omit it) or empty (Anthropic / claude_session return no per-request token tally), so a missing signal never blocks a valid call. Wired into the existing `block_rewrite_call` decision capture (`estimated_prompt_tokens` / `reported_prompt_tokens` / `num_ctx` / `outcome='input_truncated'`). NEVER changes an answer verdict on the answer path (that has its own `ED4ALL_ANSWER_NUM_CTX` tripwire). Garbage / unset / truthy → on (parse-with-fallback, mirroring the default-ON `COURSEFORGE_REWRITE_BATCH`). Selects no provider/model → no `docs/LICENSING.md` row. |
| `ED4ALL_REWRITE_MAX_ESCALATION_SHARE` | Wholesale-dispatch-failure guard on the rewrite tier (`MCP/tools/pipeline_tools.py::_run_content_generation_rewrite`, just before the `blocks_final.jsonl` emit), default **0.5**. The per-block escalation path is a safety net for the block that loses a consensus vote or blows its parse-retry budget: it keeps the outline block, stamps `escalation_marker`, and lets the deterministic template renderer author from `key_claims` rather than dropping grounded content off the page. That is right per block and wrong for a whole tier — when essentially EVERY block escalates, the rewrite tier did not run, and the cause is a transport-level fault that fails identically for every request (a strict-OpenAI seat 400-ing the Ollama-only `options.num_ctx` extra unless `ED4ALL_LLM_OMIT_OLLAMA_FORMAT` is set, a wrong served model name, an unreachable seat). Without the guard the phase reports `_completed: True`, passes its gates on template markup, and hands packaging / archival / training synthesis a course of un-authored blocks; the failure only surfaces two phases later at `post_rewrite_validation` with no trace of the cause. Measured 2026-07-29 on a TRT-LLM seat: 4,984/5,091 blocks (97.9%) escalated and the build carried on. When the escalated SHARE exceeds this ceiling the phase raises with the diagnosis. **Small-N floor:** inert below 20 blocks, where a high share carries no information (one block failing consensus is 100% of a one-block page — exactly the case the fallback serves). Set `=1.0` to disable (an operator who genuinely wants a template-rendered course); a value ABOVE 1.0 is CLAMPED to 1.0 rather than reverted to the default, because an escalation share cannot exceed 1.0 so `1.5`/`2` can only mean "off" — silently handing back a STRICTER guard on the one knob reached for mid-incident is a foot-gun. Negative or unparseable → `0.5` (parse-with-fallback); every non-default resolution logs a warning. The raise message reports the escalation-marker HISTOGRAM and offers a transport fault as the most likely cause rather than asserting it — the counter is deliberately broad (any truthy marker, including outline-carried ones), so a weak model failing validator consensus can cross the ceiling with no transport fault anywhere. Selects no provider/model → no `docs/LICENSING.md` row. |
| `COURSEFORGE_OUTLINE_CHECKPOINT` | Crash-resume checkpoint for the outline tier (`MCP/tools/pipeline_tools.py::_run_content_generation_outline`), default **ON** (parse-with-fallback: an explicit falsey `0`/`false`/`no`/`off` disables; unset / garbage / any other value keeps it on). When on, every SUCCESSFULLY-authored outline block is appended (flush + fsync per block) to the dot-prefixed sidecar `<export>/01_outline/.blocks_outline_checkpoint.jsonl` the moment its `route_with_self_consistency` dispatch completes — so a ~2h local-7B run killed mid-authoring is resumable. On restart the sidecar is rehydrated into a `{block_id: entry}` map (last-line-wins) and the expensive LLM dispatch is SKIPPED for every already-authored block; the remaining blocks are authored fresh, then the SAME deterministic post-passes (`_repair_outline_objective_refs` / `_mint_outline_curies` / `_align_outline_blocks_to_objectives`) run over the FULL rehydrated+new set and the final `blocks_outline.jsonl` is written unchanged in shape. **Anti-fabrication / no-dup:** dedupe is by stable `block_id`, so a resumed run's block set equals a clean run's (resumed blocks are byte-stable from the checkpoint; only the actually-re-authored blocks vary by LLM nondeterminism). Degraded (escalation-marker) blocks are NOT checkpointed → they re-attempt on resume (identical contract to the rewrite-tier per-block cache). The sidecar is deleted on the successful final write (resume is only for an INTERRUPTED run). OFF (or no sidecar present) → byte-identical to the legacy single-final-write emit. Mirrors the training-synthesis `.synthesis_pairs_checkpoint.jsonl` per-item resume contract. Emits one `content_selection` decision event per resume (dynamic rationale: blocks resumed / authored fresh / sidecar name). Selects no provider/model → no `docs/LICENSING.md` row. Active only on the two-pass outline phase (`COURSEFORGE_TWO_PASS=true`). |
| `COURSEFORGE_REWRITE_CHECKPOINT` | Symmetric crash-resume checkpoint for the rewrite tier (`MCP/tools/pipeline_tools.py::_run_content_generation_rewrite`), default **ON** (same parse-with-fallback as `COURSEFORGE_OUTLINE_CHECKPOINT`). The rewrite tier already resumed from its FINAL `blocks_final.jsonl` (written once at the very end) — so a mid-loop kill left nothing on disk to resume from. This flag adds the dot-prefixed append-as-you-go sidecar `<export>/04_rewrite/.blocks_final_checkpoint.jsonl`, flushed per successfully-rewritten block (both the sequential and the `COURSEFORGE_REWRITE_CONCURRENCY` thread-pool paths — concurrent appends interleave whole JSONL lines and resume dedupes by `block_id`). On restart its successful entries are MERGED into the existing `_rewrite_cache` (which the loop already consults to skip re-dispatch), so an interrupted rewrite loop is resumable beyond just the final-JSONL cache. Marker-bearing / non-string-content blocks are NOT checkpointed (re-attempt on resume). The sidecar is deleted on the successful final write. OFF (or no sidecar) → byte-identical to the prior `blocks_final.jsonl`-only resume. Emits one `content_selection` decision event when it resumed any block (dynamic rationale: blocks resumed from the sidecar / sidecar name). Selects no provider/model → no `docs/LICENSING.md` row. Active only on the two-pass rewrite phase (`COURSEFORGE_TWO_PASS=true`). |
| `COURSEFORGE_REWRITE_HTML_REPAIR` | **Deterministic HTML tag-balance repair at rewrite emit**, default **OFF** (unset / falsey / garbage → byte-identical `blocks_final.jsonl`; truthy `1`/`true`/`yes`/`on` parse-with-fallback via `lib/utils/html_balance.py::resolve_rewrite_html_repair`, read per call). When on, `MCP/tools/pipeline_tools.py::_maybe_repair_block_html_balance` (called in `_run_content_generation_rewrite` AFTER the final CURIE-anchoring sweep, immediately before the `blocks_final.jsonl` write) runs every str-content block through `lib/utils/html_balance.py::repair_html_balance` — the deterministic, LLM-free, idempotent repair of the dominant `rewrite_html_shape` `REWRITE_HTML_PARSE_FAIL` class (model-emitted tag-balance slop): stray close tags matching nothing open are DROPPED, missing intermediate closes are INSERTED before a mid-stack close, and missing closes are APPENDED at fragment end in LIFO order — placed BEFORE the trailing hidden `data-cf-curie` span tail so the postmint spans stay top-level siblings. Text nodes, attributes, void elements (gate-lockstep set) and self-closing forms are NEVER edited; a fragment that cannot converge (pathological malformed start tags) is left byte-identical (loud honest gate failure, never a partial repair). A repaired block appends a `Touch(model="html-balance-repair", provider="deterministic", tier="rewrite_val", purpose="html_balance_repair")` to its `touched_by` chain with the compact `op=count` summary in `decision_capture_id`; one `content_selection` decision event summarizes the pass. Standalone (offline, flag-independent) runner over an existing export: `python -m lib.utils.html_balance <blocks_final.jsonl> --out <dir>` (writes a repaired COPY + `repair_report.json` incl. before/after gate scores; never modifies the input). Measured on a real 424-block export: gate score 0.3519 → 0.9612, all 262 unbalanced blocks repaired. Selects no provider/model → no `docs/LICENSING.md` row. Active only on the two-pass rewrite phase (`COURSEFORGE_TWO_PASS=true`). |
| `COURSEFORGE_BEST_OF_N` | W5 best-of-N umbrella count, default `1` (OFF). When set, `Courseforge/router/router.py` fills both per-tier candidate counts (`COURSEFORGE_OUTLINE_N_CANDIDATES` / `COURSEFORGE_REWRITE_N_CANDIDATES`) from it unless a per-tier knob is already set (per-tier wins). Projected by the `ED4ALL_GENERATION_TECHNIQUE=C5` mode via `lib/generation/technique_modes.py::apply_mode_to_env` (C5 sets `5`). The umbrella → per-tier resolution lives in `technique_modes.resolve_best_of_n`. No-op when `COURSEFORGE_TWO_PASS` is unset. (Generated product content, not training data — no `docs/LICENSING.md` row; see the operator-local `w5-7b-quality-harness` plan §9.) |
| `COURSEFORGE_REWRITE_N_CANDIDATES` | Rewrite-tier best-of-N candidate count (W5). Read by `lib/generation/technique_modes.py::resolve_best_of_n(tier="rewrite")`; per-tier knob WINS over the `COURSEFORGE_BEST_OF_N` umbrella. `apply_mode_to_env` fills it via `setdefault` (an operator-pinned value is preserved). No-op when `COURSEFORGE_TWO_PASS` is unset. |
| `COURSEFORGE_SELF_VERIFY` / `COURSEFORGE_REFINE_ROUNDS` / `COURSEFORGE_CHUNK_SCOPED` | W5 technique-mode **projection-only** flags written by `lib/generation/technique_modes.py::apply_mode_to_env` so the `ED4ALL_GENERATION_TECHNIQUE` mode (C1/C3/C4 levers) is carried on the env for the W2 synthesis passes to read (the C3 self-verify micro-pass, the C4 refine rounds, the C1 chunk-scoping). W5's router/harness do not branch on them directly — they are the env surface the one-codebase-six-states design (§2 of the operator-local `w5-7b-quality-harness` plan) projects per arm. No-op when `COURSEFORGE_TWO_PASS` is unset. |
| `COURSEFORGE_BEST_OF_N_SELECT_BY` | W5 best-of-N selector, default `gate_pass` (legacy first-passing-candidate). When set to `entailment_argmax` (projected only by the C5 technique mode), the self-consistency loops in `route_with_self_consistency` + `route_rewrite_with_remediation` flip from "return the FIRST validator-PASS candidate" to **`argmax(entailment_rate)` among the validator-PASSING candidates** that cover their objective and carry zero contradictions — turning the W4 `block_prose_entailment` NLI gate into a generation-time selector (`score_candidate` reuses `score_groundedness`; the NLI model is already loaded for the gates, so best-of-N adds only N× forward passes, not a second model). The validator-pass requirement is preserved (a failing candidate is never selectable). When no candidate is clean the selector keeps the highest-entailment passing candidate and stamps `escalation_marker="best_of_n_no_clean_candidate"` (never fabricates a winner). Each selection emits a `block_best_of_n_selection` decision event whose rationale interpolates the per-candidate entailment spread + chosen index + temp/model. The flip is strictly additive — an unset/other value runs the legacy selector unchanged. No-op when `COURSEFORGE_TWO_PASS` is unset. |
| `COURSEFORGE_REWRITE_EARLY_EXIT` | **Lazy best-of-N / first-passing-candidate early exit** on the REWRITE tier, default **OFF** (unset / falsey / garbage → byte-identical to the all-N argmax path; truthy `1`/`true`/`yes`/`on` parse-with-fallback via `Courseforge/router/router.py::CourseforgeRouter._resolve_rewrite_early_exit`, read per call). Only meaningful under the `entailment_argmax` selector (`COURSEFORGE_BEST_OF_N_SELECT_BY=entailment_argmax`); the legacy `gate_pass` path already first-exits. When on, `route_rewrite_with_remediation` STOPS at the first validator-PASSING candidate whose SAME per-candidate NLI verdict (`score_candidate`, already computed for entailment_argmax) also clears the "passes" floors — never sampling candidates 2..N. **Floors** (`_verdict_passes_early_exit_floors`): `covers_objective` required, contradiction disqualifies, and `entailment_rate >= 0.60`. The entailment_argmax selector itself carries no entailment_rate floor (`_select_best_of_n_winner` only ranks among clean candidates), so per the documented mapping the floor adopts the `block_prose_entailment` PRODUCTION critical-gate value (`config/workflows.yaml`: `min_block_entailment_rate: 0.60`, contradiction disqualifies) — an early-exit winner is therefore a candidate the `post_rewrite_validation` + `block_prose_entailment` backstop would also accept. On a floor MISS the loop `continue`s and samples the next candidate; if NO candidate ever clears the floors the loop exhausts and the post-loop `_select_best_of_n_winner` argmax runs **byte-for-byte** as legacy (the same passing candidates were still collected into `best_of_n_scored`). Each early exit emits the SAME `block_best_of_n_selection` decision event (with the collected verdict numbers) so the choice stays auditable. **Interaction with per-block-type `n_candidates` (`block_routing.yaml::blocks.{type}.n_candidates` / `COURSEFORGE_REWRITE_N_CANDIDATES` / the `COURSEFORGE_BEST_OF_N` umbrella):** early-exit makes N a **cap, not a spend** — the trim-N YAML is largely moot when the first candidate typically clears the floors (expected samples/block ≈ `1 + (1-p) + (1-p)² + …`, p = first-candidate floor-pass rate); a high N stays cheap because sampling stops at the first passer, and the trim only bounds the pathological no-passer tail. Scoped to the rewrite tier only (the outline loop `route_with_self_consistency` is untouched). Selects no provider/model → no `docs/LICENSING.md` row. No-op when `COURSEFORGE_TWO_PASS` is unset. |
| `COURSEFORGE_CURIE_DETERMINISTIC` | **Fully-deterministic rewrite-tier CURIE handling** — default **OFF** (unset / falsey / garbage → byte-identical to the legacy CURIE-preservation retry ladder; truthy `1`/`true`/`yes`/`on` parse-with-fallback via `Courseforge/generators/_rewrite_fit_window.py::resolve_curie_deterministic`, read once at `RewriteProvider.__init__`). The pre-flag rewrite tier makes CURIE survival a **model-facing contract**: the system prompt tells the model to preserve every outline CURIE, and `RewriteProvider.generate_rewrite`'s CURIE-preservation retry ladder RE-ROLLS the whole block (fresh ~8k-prompt LLM call) whenever the emitted HTML drops a required minted CURIE — the dominant cost of the ~2.7-calls/block rewrite pace. When ON, CURIEs are handled **entirely deterministically — the model is never asked to recite them**: (1) the model-facing CURIE directive is stripped from BOTH the system prompt (`_strip_curie_directive_from_system_prompt`) and the serialised outline payload the model sees (`_strip_curies_from_content`, applied on the standard + escalated render paths — no CURIE instruction, no CURIE token in the prompt); (2) the CURIE-preservation retry ladder is **REMOVED** — a CURIE miss never triggers a re-roll (the structural dispatch-failure / input-truncation retries are untouched), so the rewrite tier makes **ZERO CURIE-driven LLM calls** and converges at ~1 call/block; (3) every vocabulary-resolvable outline CURIE is stamped onto the emit deterministically post-generation via the same idempotent `_force_inject_curies` hidden-`data-cf-curie`-span the exhaustion path uses (mirrors the outline tier's post-loop `_mint_outline_curies`), so the `rewrite_curie_anchoring` gate (`Courseforge/router/inter_tier_gates.py::BlockCurieAnchoringValidator`) anchors each stamped token via its minted-literal arm — the gate is satisfied honestly, never weakened. **Anti-fabrication:** only CURIEs whose token is a key in the per-course `domain_concept_vocabulary` minted-CURIE map are stamped — the SAME universe the checker treats as minted. The map is threaded provider-side via `CourseforgeRouter.set_rewrite_minted_map` (called by `MCP/tools/pipeline_tools.py::_run_content_generation_rewrite` once it loads `domain_concept_vocabulary.json`) → `RewriteProvider(minted_curie_map=…)`. A token absent from the map is silently skipped (never fabricated); an RDF / legacy corpus (no vocabulary) stamps nothing → keep this flag OFF there (it is a prose-corpus flag). Each stamp emits a `curie_minting` decision carrying the minted token list + dynamic per-call rationale (block id / provider / model / resolved-vs-skipped counts), and the per-call `block_rewrite_call` decision records `outcome="curie_deterministic_stamp"`; the Touch chain carries `purpose="curie_deterministic_stamp"` (distinct from the exhaustion path's `curie_force_injected`) so a postmortem separates "flag-on deterministic stamp, no CURIE retry ever ran" from "model dropped after the retry budget". Selects no provider/model → no `docs/LICENSING.md` row. Active only on the two-pass rewrite phase (`COURSEFORGE_TWO_PASS=true`). |
| `COURSEFORGE_CURIE_PRESERVE_SKIP_WHEN_POSTMINT` | **Postmint-aware CURIE-preservation skip** — default **OFF** (unset / falsey / garbage → byte-identical to the legacy CURIE-preservation retry ladder; truthy `1`/`true`/`yes`/`on` parse-with-fallback via `Courseforge/generators/_rewrite_provider.py::resolve_curie_preserve_skip_when_postmint`, read once at `RewriteProvider.__init__`). A **lighter-weight middle ground** between the legacy retry ladder and `COURSEFORGE_CURIE_DETERMINISTIC`: it keeps the model-facing CURIE directive intact (unlike `COURSEFORGE_CURIE_DETERMINISTIC`, which strips the directive + the CURIE payload the model sees) but removes the CURIE-driven **churn**. The per-block `RewriteProvider.generate_rewrite` path runs a CURIE-preservation retry ladder that RE-ROLLS the whole block (a fresh ~8k-prompt LLM call) up to `MAX_PARSE_RETRIES` (2) times whenever the emitted HTML drops an enforceable CURIE token, then force-injects (postmints) the still-missing set anyway at exhaustion via the idempotent `_force_inject_curies` hidden-`data-cf-curie`-span. That trailing postmint anchors EXACTLY the enforceable set — EXACTLY what the preservation gate and the downstream str-path `BlockCurieAnchoringValidator` (`Courseforge/router/inter_tier_gates.py`) enforce — so the re-rolls buy nothing the postmint doesn't already deliver, and they fight the system prompt (which explicitly tells the model NOT to echo synthetic `prefix:localname` CURIE tokens into visible prose). When ON, a missing-CURIE candidate does **NOT** consume a preservation retry: the enforceable set is postminted immediately and the candidate is accepted (a LOUD `logger.debug` per skip + a `block_rewrite_call` decision whose rationale names the skip), subject to every OTHER (router-side) gate — the router's own remediation loop still re-rolls on any non-CURIE gate failure. This brings the per-block path to parity with the already-shipping BATCHED cloud path (`generate_rewrite_batch`), which already force-injects the enforceable set with ZERO CURIE re-rolls — strong evidence the skip is lossless on the anchoring axis. **Scope caveat (design):** the skip covers exactly what postmint provably restores — CURIE-*token* anchoring, which the hidden-span force-inject fully covers (the batch path relies on this identically). It does NOT try to make the model write the token in visible prose (the prompt already forbids that). No effect when `COURSEFORGE_CURIE_DETERMINISTIC` is ON (that path returns before the ladder is reached) → the two flags never conflict, and a block declaring no CURIEs never enters the branch. Selects no provider/model → no `docs/LICENSING.md` row. Active only on the two-pass rewrite phase (`COURSEFORGE_TWO_PASS=true`). |
| `COURSEFORGE_TWO_PASS` | Two-pass router master gate: `true` / `false`, default `false`. When unset (or any value other than `true` / `1` / `yes` / `on` — case-insensitive truthy match per `Courseforge/scripts/blocks.py::_EMIT_BLOCKS_TRUTHY`), the legacy single-pass `content_generation` workflow phase runs unchanged via the `ContentGeneratorProvider` — every existing run is unaffected. When truthy, `MCP/core/workflow_runner.py::_should_skip_phase` honours the `enabled_when_env: "COURSEFORGE_TWO_PASS=true"` predicate on the two-pass phases (`content_generation_outline` → `inter_tier_validation` → `content_generation_rewrite`) and skips the legacy phase via its `enabled_when_env: "COURSEFORGE_TWO_PASS!=true"` predicate. The two-pass pipeline instantiates `Courseforge/router/router.py::CourseforgeRouter` in-process inside `MCP/tools/pipeline_tools.py::_generate_course_content` and dispatches each `Block` through outline → inter-tier validators → rewrite. Setting this var implicitly opts into the in-process content-generator path (same short-circuit semantics as `COURSEFORGE_PROVIDER`). Captured at workflow scope by the new `phase` enum values `courseforge-content-generator-outline` and `courseforge-content-generator-rewrite`. Cross-link: canonical workflow definition at `config/workflows.yaml::textbook_to_course` (and `course_generation`); architecture overview at `Courseforge/CLAUDE.md` § "Two-pass router". |
