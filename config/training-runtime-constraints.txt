# Generic qualified Ed4All Nano LoRA runtime.
#
# Keep the ABI-sensitive SSM wheels synchronized with Torch, Python, CUDA, and
# the host architecture.  bootstrap-training-env.sh prefers the operator's
# wheel cache so those two packages never need an implicit source build.
torch==2.9.0
transformers==4.57.6
trl==0.26.2
peft==0.19.1
accelerate==1.12.0
datasets==4.4.1
mamba-ssm==2.3.2.post1
causal-conv1d==1.6.2.post1
setuptools==80.9.0
