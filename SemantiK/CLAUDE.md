# SemantiK

> **Universal Protocols**: See root `/CLAUDE.md` for orchestrator protocol, execution rules, decision capture requirements, and error handling. This file contains SemantiK-specific guidance only.

This file provides guidance to coding agents working in this repository.

## Overview

SemantiK is Ed4All's **PDF → WCAG 2.2 AA accessible-HTML conversion
engine** — a local-only pipeline that is **license-clean by construction**:
the extraction stack is built entirely on permissively-licensed tooling
(pypdfium2 + pdfplumber + pikepdf + Tesseract), so the code ships Apache-2.0
(see `LICENSE`). The runtime needs no
cloud LLM — the local GGUF specialists run fully offline; a hosted large-model
endpoint is an opt-in quality seat, not a dependency.

SemantiK emits a stable **source-provenance wire contract** — the
`data-dart-*` block attributes and the `dart:{slug}#{block_id}` sourceId —
that every downstream Ed4All consumer (Courseforge staging, source-mapping,
the chunker, the Ask path) reads to thread block-level provenance through the
pipeline. The full contract is in [`architecture.md`](architecture.md) §12.

Core principle: **learned models are narrow candidate generators;
deterministic code orchestrates, gates, and assembles.** BERTs classify,
Qwens generate, deterministic code owns composition / hierarchy / ARIA /
validation. That is what lets SemantiK make a WCAG conformance claim — the
rules that produce conformance are auditable code, not weights.

Code layout: the cascade + models live under `SemantiK/dart_semantic/`; the
Ed4All-facing adapter seam (output-contract normalizer + deterministic
front-matter filter) lives under `lib/semantik/`; the MCP bridge wiring lives
in `MCP/tools/pipeline_tools.py`.

Trainer layout: the seven council/theta head-trainers live under
`SemantiK/training/` (`train_classifier.py`, `train_structure.py`,
`train_semantic.py`, `train_merge_or_split.py`, `train_table_specialist.py`,
`train_math_specialist.py`, `train_reasoner.py`). Three trainers remain under
`SemantiK/scripts/` by design, not as an unfinished migration:
`train_qwen_lora.py` is a thin CLI wrapper, while `train_figure_router.py` and
`train_semantic_preservation.py` are substantial standalone trainers (the
theta cross-encoder + the figure-router head) that are run as one-off scripts
rather than part of the council DAG.

## The 13-stage v2 cascade at a glance

Entry: `dart_semantic/cascade.py::run_full_cascade` (reachable as
`pipeline_v2.run(pdf, mode="v2")`). Full per-stage depth (input → behavior →
output) is in [`architecture.md`](architecture.md); the one-liner map:

| # | Stage | What it does |
|---|-------|--------------|
| 1 | extract | pikepdf + pypdfium2 + pdfplumber + Tesseract → per-page text/bbox |
| 2 | features | font/geometry/column/reading-order features per block |
| 3 | council | BERT specialists: Structure · Semantic · MergeOrSplit · TableSpecialist · Math (shared backbone, one-resident LoRA adapter swap) |
| 4 | cross-BERT reranker | arbitrate conflicting council signals → routing decisions |
| 5 | structure_graph | deterministic 6-pass grouping → typed `Region` objects |
| 5b | GLM-OCR enrich | optional table-cell OCR enrichment (cache/live) |
| 5c | figure bbox → PNG | render figure Region bboxes to PNG bytes |
| 5d | structure reviewer | **off by default** — conservative large-model heading/structure corrector under a strict text-conservation invariant |
| 5e | block join/split | **regroup arm default-ON** (`SEMANTIK_UNIT_REGROUP`, ITEM1); same-kind join/split + fused-title + the optional large-model op-proposal layer stay off by default — a deterministic-first FB-boundary re-partitioner (merge shattered/continued same-kind regions, fuse cross-kind pedagogical label+body units, split over-merged/pedagogical-label regions) under the R-PART partition + text-conservation invariant + bounded audited MOVE (shadow by default) |
| 6 | Qwen specialists | prose/table/math HTML generation; local GGUF or hosted large-model endpoint; batched two-phase |
| 6b | figure captioner | SmolVLM2 alt-text + extended description per figure |
| 7 | per-region hard gate | axe-wcag22aa · html5 · text_preserve · mathml · table_structure · heading_tree (eliminating) |
| 8 | per-region soft reranker | pick top surviving candidate per region |
| 9 | assembler | role→HTML via ontology, heading-tree normalize, gap-fill splice (`pass_9a/9b/9c`). **Whole-document heading-contiguity pass** (this session, `3a71e57`): `assembler/heading_contiguity.py::normalize_document_heading_levels`, wired into `assemble_document` after 9a/9c, re-levels EVERY `<hN>` (structural + specialist-EMBEDDED + gap-filled) so the Stage-10 `heading_tree` gate sees a contiguous hierarchy — fixes a prose-embedded `<h6>` that 9a's `kind=="heading"`-only normalization misses (text/ids/attrs byte-preserved, idempotent). |
| 10 | document hard gate | document-scope axe · lang · title · landmark · heading contiguity |
| 11 | document soft reranker | document quality / lane-selection signal |
| 12 | theta | DeBERTa-v3-small + LoRA semantic-preservation cross-encoder (Stage-12 score) |
| 13 | exit decider | one capped offline retry, then stamp `ship_with_confidence` / `ship_with_flag` / `offline_qwen_lane` / `non_certified_stamp`. **Theta-stub bypass** (this session, `3a71e57`): when theta runs in stub mode (`DART_ALLOW_THETA_STUB=1`, the v8 mode-collapse fallback's flat 0.7 placeholder), the exit-decider does NOT let the placeholder trip the theta-`<TAU>` offline retry / non-certified path — `theta_is_stubbed` (keyed off `semantic_preservation.method == "stub_v1"`, in `theta/evaluator.py`) makes `theta/offline_retry.py::_needs_retry` skip the theta trigger (still retries on a real `wcag=failed`) and `theta/exits.py::decide_exit` ships `ship_with_flag` + appends the `THETA_UNVERIFIED_STUB` flag (byte-stable when theta is real). |

## Runtime modes

- **Local GGUF specialists (default).** `SEMANTIK_SPECIALIST_PROVIDER=local`
  (or unset). The Stage-6 specialists run in-process on `llama-cpp-python`
  (built against CUDA), fully offline, no key. This is the byte-stable
  default.
- **Hosted large-model endpoint seat (opt-in, never silent).** `SEMANTIK_SPECIALIST_PROVIDER=nvidia`
  (any non-local value) CONFIGURES an OpenAI-compatible hosted endpoint —
  defaults to the NVIDIA large-model seat (`NVIDIA_API_KEY` / `NVIDIA_BASE_URL`).
  The Stage-5d reviewer / Stage-5e resegment seats route there directly when
  their own flags are on. For **Stage-6 authoring**, selecting the provider
  alone leaves the LOCAL specialists as the authoring tier; the large-model seat displaces
  them only on an explicit opt-in — `SEMANTIK_SPECIALIST_REFINE=1` (hybrid) or
  `SEMANTIK_SPECIALIST_ENDPOINT_DISPLACE=1` (pure-endpoint). This is the
  **only** part of SemantiK that selects an LLM provider, so it is the only
  flag carrying a `docs/LICENSING.md` row.
- **Batched two-phase Stage-6.** Phase 1 generates local drafts **batched by
  adapter** (one PROSE/TABLE/MATH adapter resident at a time — never
  interleaved on 8 GB VRAM) and is the DEFAULT authoring tier even when an
  endpoint provider is configured. Phase 2 is the optional hosted-endpoint
  pass, reached only on an explicit opt-in: `SEMANTIK_SPECIALIST_ENDPOINT_DISPLACE=1`
  (pure-endpoint) skips Phase 1 and fans all regions concurrently; the hybrid
  `SEMANTIK_SPECIALIST_REFINE=1` mode runs Phase 1 then sends the drafts to
  the large-model seat for a polish pass. Region-index keying is identical across modes, so
  Stages 7+ never see the difference. On the endpoint lane the Phase-2 POSTs
  are **multi-region BATCHED by default** (`SEMANTIK_SPECIALIST_BATCH`, on) —
  many regions per POST via `generate_multi` so the hosted seat's
  ~40-requests/minute cap is not exhausted by the ~197×K per-region POSTs
  that otherwise 429 on ~every call (set `SEMANTIK_SPECIALIST_BATCH=0` for the
  byte-stable per-region path). The batched lane caps candidate-K to ≤2 and
  uses low concurrency (2) so a 16-page slice fires ~34 POSTs.
- **Stage-5d structure reviewer (off by default).** `SEMANTIK_STRUCTURE_REVIEW=1`
  enables a conservative, cluster-aware large-model reviewer that corrects
  chapter/section headings before structure is finalized. It never alters
  text or re-partitions FeatureBlocks, runs a document-level
  token-conservation check, and **fails closed** (reverts to the unreviewed
  region list) on any mismatch. The load-bearing front-matter fix is the
  always-on deterministic detector (see § Front-matter), not this reviewer.
- **Stage-5d full-block reviewer — content-type re-typing (off by default).**
  `SEMANTIK_BLOCK_REVIEW=1` (on top of `SEMANTIK_STRUCTURE_REVIEW=1`) widens the
  reviewer from heading-only into a *structural editor over block IDs*: it
  re-types the council's known-weak content kinds (`code_block`, `table` —
  dispatched unconditionally) and drops furniture, emitting ops keyed by index,
  **never text** (verbatim source rides deterministic assembly; token-conservation
  fails closed). The 7B sees an edge-windowed view (head/tail tokens per block)
  and returns `corrected_kind`. Live-validated on a 3-chapter algebra-textbook slice
  (2026-06-27, commit `7e3f842`): **197 re-types, 0 text reverts** — all 35
  council `code_block`s (which were "TRY IT" exercises) → `paragraph`/`math`;
  over-detected `table`s (prose callouts, single definitions) → `paragraph`,
  section titles → `heading`, while **real data tables stay `table`**.

  **Canonical run config (default).** The reviewer rides the endpoint seat
  (`make_runtime("endpoint", …)`), so point it at a LOCAL Ollama model:
  ```bash
  SEMANTIK_STRUCTURE_REVIEW=on SEMANTIK_BLOCK_REVIEW=on \
  SEMANTIK_STRUCTURE_REVIEW_TEMPERATURE=0 \
  SEMANTIK_SPECIALIST_BASE_URL=http://localhost:11434/v1 \
  SEMANTIK_SPECIALIST_API_KEY=ollama \
  SEMANTIK_STRUCTURE_REVIEW_MODEL=qwen2.5-7b-16k:latest \
  SEMANTIK_SPECIALIST_CONCURRENCY=1     # serialize: one Ollama model can't run windows in parallel
  ```
  **On an 8GB dev box use the 16k model, NOT 32k, for the *reviewer*** — the
  reviewer's windows are ~2k tokens, but `qwen2.5-7b-32k` allocates the full 32k
  KV (≈8.7GB > 8GB → ~27% spills to CPU → 120s timeouts); `qwen2.5-7b-16k` is
  fully GPU-resident (~6.3GB). 32k stays the *authoring* seat. `CONCURRENCY=1` is
  load-bearing: the windowed dispatch fans all windows at once, but one Ollama
  model serializes them, so concurrent windows trip the client timeout while
  queued. **GPU lifecycle:** the cascade keeps the council BERTs resident until
  Stage-5e (`release_council_gpu()` only fires before Stage-6), so a full-cascade
  run on 8GB has council + reviewer coexisting at Stage-5d. For an isolated
  reviewer run/validation that keeps one model on the card at a time
  (council → `release_council_gpu()` → reviewer), see the council/structure/clean
  → release → `run_structure_review` sequence. Run the council on GPU (sequential
  cascade phases time-share the card — do NOT force the council to CPU).
  On top of that, the deterministic **GPU-lifecycle stage-lease** seams
  (`ED4ALL_GPU_LIFECYCLE`, root-owned, default ON — the cross-venv twin
  `dart_semantic/gpu_lifecycle.py` since the bridge cannot import Ed4All's `lib/`)
  hand the card between STAGES rather than relying on contention: `cascade.py::
  _gpu_lifecycle_release` fires an ollama `keep_alive:0` sweep post-Stage-5e /
  pre-Stage-6 (the 5d reviewer + 5e resegment are ONE lease — never released
  between 5d and 5e), a torch `empty_cache` post-Stage-6b captioner /
  post-Stage-6 GGUF (belt-and-suspenders over the AdapterSwap `runtime.free`) /
  post-Stage-12 theta, and a second ollama sweep post-second-pass+ocr_repair
  (one lease over both same-seat consumers). It WRAPS, never replaces,
  `release_council_gpu()` (still the council's canonical end-of-stage release)
  and the AdapterSwap/theta scope-drops. Every seam is idempotent +
  lazy-reload-safe so `maybe_offline_retry`'s `_run_inner` re-entry is safe; the
  seams gate on `ED4ALL_GPU_LIFECYCLE` (flag-off → zero release calls,
  byte-identical) and are fail-soft. No new `SEMANTIK_*` flag — the gate is the
  root-owned `ED4ALL_*` flag.

## The cross-venv bridge

SemantiK's runtime pulls in heavy ML deps (torch, transformers, peft, a
CUDA-built `llama-cpp-python`, sentence-transformers for theta) that must NOT
live in Ed4All's MCP/orchestrator venv. So the cascade runs **out of
process**, in its own venv, behind a JSON bridge:

- `SemantiK/scripts/run_cascade_json.py` is the subprocess entry point: PDF
  in → `run_full_cascade` → HTML + `region_provenance` + conformance audit as
  JSON on stdout.
- `MCP/tools/pipeline_tools.py` invokes it (`_run_semantik_bridge_subprocess`,
  the conversion seam for the `dart_conversion` phase). `SEMANTIK_PYTHON` =
  absolute path to the SemantiK venv python; `SEMANTIK_RUNTIME_DIR` = SemantiK
  repo root used as the subprocess `cwd` (so model/cache dirs resolve). When
  the in-process deps are absent and `SEMANTIK_PYTHON` is unset, the bridge
  **fails closed with operator guidance** (no silent stub).

Everything crossing the bridge is the § Output contract wire contract, so the
rest of Ed4All consumes conversion output through one stable interface.

## In-process install (Option A)

As an alternative to the cross-venv bridge, SemantiK's deps can be installed
*directly* into Ed4All's venv so the cascade runs **in-process**
(`MCP/tools/pipeline_tools.py::_run_semantik_v2_conversion` takes its arm (a)
when `from SemantiK.dart_semantic.cascade import run_pipeline_v2` succeeds).
The bare `dart_semantic.*` imports resolve because that function inserts
`<repo>/SemantiK` onto `sys.path` before the import (mirroring
`scripts/run_cascade_json.py`).

Provision into a venv that already carries the heavy ML stack
(torch≥2.2 / transformers≥4.45 / peft≥0.10 / scikit-learn / scipy /
bitsandbytes — all satisfied by Ed4All's `[training]`+`[embedding]` extras):

```bash
# 1. The 10 missing pure-pip deps (the [semantik] extra).
pip install -e '.[semantik]'

# 2. The headless Chromium used by the axe-core a11y audit.
playwright install chromium

# 3. llama-cpp-python for the LOCAL Stage-6 GGUF specialists — a CUDA
#    *source* build, NOT the CPU wheel:
CMAKE_ARGS="-DGGML_CUDA=on -DCMAKE_CUDA_ARCHITECTURES=86" \
  pip install --no-binary llama-cpp-python llama-cpp-python
```

Step 3 is **optional for structure-only conversion**: the Stage-6 specialists
can run on the hosted NVIDIA large-model endpoint instead
(`SEMANTIK_SPECIALIST_PROVIDER=nvidia`, key `NVIDIA_API_KEY`), so a box without
a CUDA-built `llama-cpp-python` can still run the full cascade with Stage-6 via
the endpoint seat.

## Output contract

SemantiK emits a stable, downstream-facing shape (full detail:
[`architecture.md`](architecture.md) §12):

- **Source-provenance block attributes + `dart:{slug}#{block_id}` sourceId.**
  The adapter seam (`lib/semantik/adapter.py`, `cascade_ir.py`) wraps each
  block in `<section class="dart-section" data-dart-block-id=…
  data-dart-source=… data-dart-pages=… data-dart-page-kind="physical"
  data-dart-confidence=… data-dart-wcag=…>` — `data-dart-block-id` carries
  the block's stable id, `data-dart-source` the `synthesized`/`vendor`
  provenance, `data-dart-pages` the physical PDF page span, and
  `data-dart-wcag` the per-region gate verdict. The sourceId `block_id` is
  **deterministic** (first raw FeatureBlock index, or a content hash under
  `TRAINFORGE_CONTENT_HASH_IDS=1`) — same PDF in → same sourceIds out, so
  chunk refs / `source_module_map.json` / citation deep-links survive re-runs.
- **`region_provenance`.** Per-region list in emission order
  (`cascade.py::_build_region_provenance`): `region_index`, `region_kind`,
  `role`, `confidence`, `wcag_status`, `first_raw_block_index`, `pages`,
  `heading_text`/`level`, `figure_alt`, `raw_text`, and an OPTIONAL `review`
  block (Stage-5d corrections only).
- **`*.conformance_audit.json`** (`conformance_audit.py`, schema
  `conformance-audit/1.0`): exit action + WCAG status, per-region + document
  gate logs **with skip counts** (skips are first-class — "no measurement",
  not "verified safe"), theta report, thresholds, heading tree, and rule-id →
  WCAG SC coverage map.

## Front-matter handling (deterministic)

Phantom-TOC / front-matter contamination (a book's front-matter Table of
Contents getting classified as real chapter headings) is fixed at the adapter
seam by the **deterministic, CPU-only** detector
`lib/semantik/toc_frontmatter_detector.py`, applied inside `build_chapters_ir`
before chapters assemble. Three signals: a front-matter zone anchor (first
real chapter heading not in a TOC run), a monotonic-page-number TOC run
(≥`_MIN_TOC_RUN` contiguous "Title … pageN" entries with increasing page
numbers), and a page-density cluster (many chapter-level headings packed into
a tiny early-page span). It reuses tested predicates from
`lib/semantic_structure_extractor`. This deterministic detector is the
load-bearing front-matter fix; the off-by-default Stage-5d large-model reviewer is the
secondary, conservative defensive layer.

## Testing

| Area | Tests |
|------|-------|
| Cascade IR / adapter seam | `lib/semantik/tests/test_cascade_ir.py`, `test_cascade_ir_real_ea2e.py`, `test_adapter.py`, `test_vendor_ingest.py` |
| Stage-5d reviewer | `dart_semantic/qwen_specialists/tests/test_reviewer.py`, `test_reviewer_prompt.py`, `test_cascade_stage5d.py`; bridge: `lib/semantik/tests/test_structure_review_bridge.py` |
| Theta device | `lib/semantik/tests/test_theta_device.py` |
| Front-matter detector | `lib/semantik/tests/test_toc_frontmatter_detector.py` |
| Bridge / MCP seam | `MCP/tests/test_semantik_v2_seam.py`, `test_semantik_bridge_e2e.py`, `test_semantik_bridge_subprocess.py`, `test_semantik_dispatch_flip.py`, `lib/semantik/tests/test_run_cascade_json_env.py`, `test_run_cascade_json_alloc_conf.py` |
| Chunker enrichment | `Trainforge/chunker/tests/test_semantik_chunk_enrichment.py` |
| Specialist runtime | `dart_semantic/qwen_specialists/tests/` |
| Tables (structural confirm) + Figures (detect/route/sidecar) | `dart_semantic/council/tests/test_table_figure_detection.py`, `dart_semantic/tests/test_figure_detection.py`, `test_figure_sidecar.py`, `test_image_extract_yflip.py`, `lib/semantik/tests/test_figure_img_src.py` |

## MCP Tools

SemantiK has no dedicated `@mcp.tool()` surface of its own — it is wired in as
the **conversion backend** in `MCP/tools/pipeline_tools.py`. The
`dart_conversion` phase routes through the cross-venv bridge
(`_run_semantik_bridge_subprocess`) when the SemantiK runtime is configured,
producing the § Output contract shape that the downstream `stage_dart_outputs`
/ `validate_dart_markers` pipeline tools consume. Bridge env (`SEMANTIK_PYTHON`
/ `SEMANTIK_RUNTIME_DIR` / timeout / alloc-conf) is in the flag table below.

## Decision Capture

The MCP bridge emits one **`structure_review`** DecisionCapture event per
converted document (the doc-level Stage-5d verdict list — block_id, verdict,
kind/level before→after, reason code, reverted flag — surfaced off
`conformance_audit["structure_review"]`; resolves to `None`/not-run when the
reviewer is off). Rationale is dynamic and replayable (per-doc verdict
counts). Regression: `lib/semantik/tests/test_structure_review_bridge.py`
asserts the JSONL row fires with a dynamic rationale. See root `/CLAUDE.md`
§ Decision Capture for the contract.

The Stage-6b figure captioner (the SmolVLM2 VLM call site,
`dart_semantic/figure_captioner.py::caption_figure_regions`) emits one
**`alt_text_generation`** DecisionCapture event per captioned figure (W7.6),
constructed best-effort under the canonical `dart` tool / `dart-conversion`
phase (`_build_caption_capture`). Rationale is dynamic + replayable — image
content hash (sha256), figure geometry (`px_size`) + stable min-FB id, the
SmolVLM2 model id + per-prompt `max_new_tokens`, and the produced alt/extended
caption lengths — so a replay can attribute the alt text to its exact input.
Best-effort: a capture-construction / log failure is non-fatal (never breaks
captioning), mirroring the bridge's `structure_review` posture. Regression:
`dart_semantic/tests/test_figure_captioner_capture.py` asserts one
`alt_text_generation` decision fires per figure with a dynamic rationale (VLM
boundary mocked).

The MCP conversion seam also emits one **`block_resegment`** DecisionCapture
event per converted doc when the Stage-5e re-partition pass fired
(`MCP/tools/pipeline_tools.py` section 2c → `_emit_block_resegment_capture`,
off the cascade's `block_resegment` audit rows). It resolves the audit off
BOTH cascade arms — the in-process result-dict top-level `block_resegment` key
AND the cross-venv bridge (`run_cascade_json._build_bridge_dict` now forwards
it via `_resolve_block_resegment`; the same-touch `second_pass_verify` arm is
forwarded alongside). Rationale is dynamic + replayable: merge / split /
regroup op tallies, the fused-title-split count, the folded-region tally, the
merged-unit `semantic_class` set, a bounded `source_ids` sample, and
`conservation_verified`. It is a deterministic-pass capture (the resegment ops
are deterministic-first, so it fires even with no LLM op-proposal layer) riding
the canonical `block_resegment` `decision_type` enum (no schema change), and is
best-effort (a capture failure logs a warning, never breaks conversion) —
mirroring the `structure_review` posture. Regression:
`lib/semantik/tests/test_structure_review_bridge.py` § 3c
(`test_block_resegment_capture_row_emitted_dynamic_rationale`).

## Opt-In Behavior Flags

SemantiK (the license-clean semantic-cascade PDF→structured-content converter
under `SemantiK/dart_semantic/` + `lib/semantik/`) owns the `SEMANTIK_*`
env-var prefix. All toggles default to
byte-stable / off-or-local behaviour to preserve backward compatibility; only
the model/provider selectors below (`SEMANTIK_SPECIALIST_PROVIDER` + the
`*_MODEL` seats) carry a `docs/LICENSING.md` row. The per-phase selectors
(`SEMANTIK_SPECIALIST_PHASE{1,2}_PROVIDER` / `_MODEL`) reuse the SAME endpoint
seat, so their exposure is covered by the `SEMANTIK_SPECIALIST_PROVIDER` /
`_MODEL` rows — no separate licensing rows. Resolution everywhere is
parse-with-fallback (unset / garbage / out-of-range → the documented default;
a run never crashes on a malformed flag), mirroring the root `ED4ALL_*`
posture.

| Flag | Default | Purpose |
|------|---------|---------|
| `SEMANTIK_SPECIALIST_PROVIDER` | `local` | **The single-seat / Stage-6 Phase-2 (refine) + Stage-5d structure-reviewer generation backend selector** (`SemantiK/dart_semantic/qwen_specialists/runtime.py::resolve_specialist_provider`, ~L97). Resolution: explicit arg > `SEMANTIK_SPECIALIST_PROVIDER` (lowercased/stripped) > `local`. The `_LOCAL_PROVIDER_VALUES` set (`{"", "local", "gguf", "llama_cpp", "llamacpp"}`) all resolve to the byte-stable in-process local-GGUF council adapters (no network, no key); ANY other non-empty value (e.g. `nvidia` / `local-openai` / `endpoint`) CONFIGURES the OpenAI-compatible endpoint seat (`endpoint_runtime.py`) as reachable. **Per-phase seats (model-agnostic Stage 6):** Stage 6's two phases each resolve their OWN seat — this single value is now the **Phase-2 (refine/generate) DEFAULT** (overridable via `SEMANTIK_SPECIALIST_PHASE2_PROVIDER`); **Phase 1 (draft) defaults to `local` REGARDLESS of this value** (it does NOT inherit the single seat — the dereliction default keeps the on-device GGUF specialists as the draft tier unless `SEMANTIK_SPECIALIST_PHASE1_PROVIDER` is set). **Note (dereliction fix):** selecting an endpoint provider here NO LONGER, on its own, displaces the local Stage-6 specialists — Phase 1 stays local and Phase 2 fires only on `SEMANTIK_SPECIALIST_REFINE` (hybrid) or `SEMANTIK_SPECIALIST_ENDPOINT_DISPLACE` (pure-Phase-2). The value still selects WHICH endpoint/model the Stage-5d reviewer / Stage-5e resegment seats use. **Selects an LLM provider → has a `docs/LICENSING.md` row.** The endpoint seat defaults to the NVIDIA large-model seat (key `NVIDIA_API_KEY`, base `NVIDIA_BASE_URL`). Garbage / unknown → treated as a provider name (fail-loud at POST time if no base_url/key resolves). |
| `SEMANTIK_SPECIALIST_PHASE1_PROVIDER` | unset → `local` | **Per-phase Stage-6 DRAFT-seat provider** (`runtime.py::resolve_phase_provider("phase1")`). Resolution: `SEMANTIK_SPECIALIST_PHASE1_PROVIDER` (normalized) > **`local`** — Phase 1 does NOT inherit the single `SEMANTIK_SPECIALIST_PROVIDER` (the dereliction default keeps the on-device GGUF specialists as the draft tier). Set it to a non-local value (e.g. `nvidia` for hosted-draft, or `local-openai` for a local server) to author Phase-1 drafts on that seat instead. `local`/aliases → the in-process GGUF specialists (no network/key). Reuses the SAME endpoint seat as `SEMANTIK_SPECIALIST_PROVIDER`, so its provider/model exposure is covered by that row's `docs/LICENSING.md` entry — no separate row. Parse-with-fallback. |
| `SEMANTIK_SPECIALIST_PHASE1_MODEL` | per-seat | **Per-phase Stage-6 DRAFT-seat model id** (`runtime.py::resolve_phase_model("phase1")`; only consulted when Phase 1's provider is non-local — the local GGUF arm picks its model from the adapter config). Resolution: `SEMANTIK_SPECIALIST_PHASE1_MODEL` > `SEMANTIK_SPECIALIST_MODEL` > `NVIDIA_LARGE_MODEL` > `meta/llama-3.3-70b-instruct`. Rides the existing licensed endpoint seat → covered by the `SEMANTIK_SPECIALIST_PROVIDER` / `_MODEL` licensing row, no separate row. Parse-with-fallback. |
| `SEMANTIK_SPECIALIST_PHASE2_PROVIDER` | unset → `SEMANTIK_SPECIALIST_PROVIDER` → `local` | **Per-phase Stage-6 REFINE/GENERATE-seat provider** (`runtime.py::resolve_phase_provider("phase2")`). Resolution: `SEMANTIK_SPECIALIST_PHASE2_PROVIDER` (normalized) > `SEMANTIK_SPECIALIST_PROVIDER` (the single seat) > `local`. This is the seat Phase 2 uses when enabled (`SEMANTIK_SPECIALIST_REFINE` / `_ENDPOINT_DISPLACE`); it may independently be hosted (NVIDIA cloud), a local OpenAI-compatible server (`local-openai` + a localhost `SEMANTIK_SPECIALIST_BASE_URL` — local-draft→local-large-refine with NO hosted call), or the local GGUF arm. The hosted large model is just one possible value, never a hardcoded tier. Reuses the SAME endpoint seat → covered by the `SEMANTIK_SPECIALIST_PROVIDER` licensing row, no separate row. Parse-with-fallback. |
| `SEMANTIK_SPECIALIST_PHASE2_MODEL` | per-seat | **Per-phase Stage-6 REFINE/GENERATE-seat model id** (`runtime.py::resolve_phase_model("phase2")`; consulted when Phase 2's provider is non-local). Resolution: `SEMANTIK_SPECIALIST_PHASE2_MODEL` > `SEMANTIK_SPECIALIST_MODEL` > `NVIDIA_LARGE_MODEL` > `meta/llama-3.3-70b-instruct`. This is how you pin the refiner model (e.g. a local `qwen2.5:32b` for on-device refine) without touching the single `SEMANTIK_SPECIALIST_MODEL`. Rides the existing licensed endpoint seat → covered by the `SEMANTIK_SPECIALIST_PROVIDER` / `_MODEL` licensing row, no separate row. Parse-with-fallback. |
| `SEMANTIK_SPECIALIST_BASE_URL` | unset → `NVIDIA_BASE_URL` | Base URL of the hosted OpenAI-compatible endpoint for Stage-6 specialist + Stage-5d reviewer generation (`endpoint_runtime.py::_resolve_base_url`, ~L113). Resolution: `SEMANTIK_SPECIALIST_BASE_URL` > `NVIDIA_BASE_URL` > none. No machine default is hardcoded — when the provider is non-local and neither resolves, the endpoint runtime fails loud at generation time (no silent fallback). URL only — no `docs/LICENSING.md` row. |
| `SEMANTIK_SPECIALIST_API_KEY` | unset → `NVIDIA_API_KEY` | Bearer token for the hosted endpoint seat (`endpoint_runtime.py::_resolve_api_key`, ~L120). Resolution: `SEMANTIK_SPECIALIST_API_KEY` > `NVIDIA_API_KEY` > none; a missing key on a non-local provider raises `EndpointRuntimeError` before any dispatch (fail-closed). Credential, never hardcoded — no `docs/LICENSING.md` row (the seat it unlocks is covered by the NVIDIA large-model-seat licensing row). |
| `SEMANTIK_SPECIALIST_MODEL` | `meta/llama-3.3-70b-instruct` | **Shared model ID for Stage-6 specialist generation** (math / table / prose / gap-fill) on the endpoint seat (`endpoint_runtime.py::_resolve_model`, ~L127). Resolution: `SEMANTIK_SPECIALIST_MODEL` > `NVIDIA_LARGE_MODEL` > the literal `meta/llama-3.3-70b-instruct` default. This is the SHARED fallback for both per-phase model seats (`SEMANTIK_SPECIALIST_PHASE{1,2}_MODEL` win over it). **Selects an LLM model → has a `docs/LICENSING.md` row** (the NVIDIA large-model seat). No-op when the consuming phase's provider resolves local. |
| `SEMANTIK_STRUCTURE_REVIEW` | unset (off) | Stage-5d large-model **structure-reviewer** gate (`SemantiK/dart_semantic/qwen_specialists/reviewer.py::resolve_structure_review_mode`, ~L322). Default OFF → the cascade's heading/structure output is byte-identical (no reviewer dispatch). Truthy (`1`/`true`/`yes`/`on`, case-insensitive) → a conservative, cluster-aware large-model reviewer corrects chapter/section headings before the structure is finalized. Falsey / unset / garbage → off. Gate only (the model it dispatches is the `SEMANTIK_STRUCTURE_REVIEW_MODEL` / specialist seat) — no separate `docs/LICENSING.md` row beyond the model seat's. |
| `SEMANTIK_STRUCTURE_REVIEW_MODEL` | `meta/llama-3.3-70b-instruct` | **Model ID for the Stage-5d structure-reviewer seat** (`runtime.py::resolve_structure_review_model`, ~L126; mirrored in `MCP/tools/pipeline_tools.py` ~L6380). Resolution: `SEMANTIK_STRUCTURE_REVIEW_MODEL` > `SEMANTIK_SPECIALIST_MODEL` > `NVIDIA_LARGE_MODEL` > the literal `meta/llama-3.3-70b-instruct`. **Selects an LLM model → has a `docs/LICENSING.md` row** (same NVIDIA large-model seat as the specialist). No-op when `SEMANTIK_STRUCTURE_REVIEW` is off. |
| `SEMANTIK_STRUCTURE_REVIEW_TEMPERATURE` | `0.0` (greedy / deterministic) | Sampling temperature threaded into the Stage-5d structure-reviewer's `generate_batch` dispatch (`reviewer.py::resolve_structure_review_temperature`, consumed at both `run_structure_review` dispatch sites). Default `0.0` = **deterministic decoding** — a structure-correction pass should NOT introduce run-to-run noise in chapter/section heading decisions (the reviewer previously fell to the endpoint runtime's `temperature=0.6` default and SAMPLED, measured at heading-set Jaccard 0.91-0.94 across re-runs of the same PDF). A positive float opts back into sampling. **Honesty note:** greedy decoding makes the dispatch deterministic at the sampling layer; hosted-endpoint / float non-determinism can still cause rare token ties — "deterministic decoding (temperature 0)", not an absolute guarantee. Parse-with-fallback: non-float / negative / NaN / garbage → `0.0`. No-op when `SEMANTIK_STRUCTURE_REVIEW` is off (no reviewer dispatch). A request parameter only — selects no provider/model → no `docs/LICENSING.md` row. |
| `SEMANTIK_BLOCK_RESEGMENT` | unset (off) | Stage-5e **block JOIN/SPLIT** pass gate (`SemantiK/dart_semantic/qwen_specialists/block_resegment.py::resolve_block_resegment_mode`). Default OFF → the cascade's region partition is byte-identical (no resegment pass; the result dict carries NO `block_resegment` key). Truthy (`1`/`true`/`yes`/`on`, case-insensitive) → a DETERMINISTIC-first pass re-partitions FORMED Region objects: it MERGES adjacent SAME-KIND regions wrongly cut apart (a list/paragraph shattered across a page break — driven by the council MergeOrSplit head + a cross-page continuation cue) and SPLITS one region that fused two units (a confident MergeOrSplit `not_same` boundary, or a pedagogical-label — "EXAMPLE 2"/"Solution"/"Step 3" — starting mid-region). The FeatureBlock is the atom: MERGE concatenates whole FBs, SPLIT cuts only at FB boundaries, so TEXT is never rewritten — only boundaries move. Holds the R-PART invariant (FB multiset + document order preserved) + the reviewer's token-conservation check and **fails closed** (reverts to the input region list) on any violation; skips table/math passthrough regions (non-null `source_region_id`). **ITEM3:** Stage-5e now ALSO hosts the bounded audited **MOVE arm** (its own flag `SEMANTIK_MOVE_OP`, below), and `assert_partition_conservation` is **move-aware** — R-PART clause 1 (FB multiset) stays unconditional, but clause 2 (document order) now permits an order change ONLY when it is exactly explained by an explicit audited `MoveOp` (an unexplained permutation still fails closed); the default `move_ops=()` keeps every existing merge/split/regroup/fused call byte-identical. Falsey / unset / garbage → off. Gate only — no separate `docs/LICENSING.md` row (the optional LLM layer rides the already-licensed specialist seat). |
| `SEMANTIK_MOVE_OP` | `shadow` (default-ON-shadow — deviation) | Stage-5e **bounded audited MOVE** arm gate (ITEM3; `SemantiK/dart_semantic/qwen_specialists/block_resegment.py::resolve_move_op_mode`). The pipeline's FIRST order-changing repair op. **Three-valued parse-with-fallback:** the explicit falsey tokens `0`/`false`/`no`/`off` → `off` (the byte-identical-INCLUDING-audit revert lever); `live` → `live` (moves are APPLIED — emission order changes); unset / blank / garbage / ANY other truthy → **`shadow`** (the DEFAULT). **Default `shadow` (not `live`)** because a wrong move is a reader-visible coherence defect no conservation gate can catch (token + FB-multiset both pass on a wrong permutation) and there is zero calibration data on the deterministic detector's false-positive rate; shadow runs the detector, validates each candidate through BOTH R-PART (move-aware) + token-conservation gates, and writes the full audit row (`applied=false, mode="shadow"`) + the `block_resegment` DecisionCapture tally — producing calibration evidence on the operator's own corpora at **ZERO output-byte change** (HTML + `region_provenance` byte-identical to the `=0` run; only the `block_resegment` audit gains shadow rows). `off` is the byte-identical-including-audit revert. The planned later `shadow`→`live` default-flip is keyed on ≥95% shadow-audited precision over ≥2 calibration corpora (mirrors the `SEMANTIK_READING_ORDER_FIX` house default-flip posture; **OWNER-GATED — O1**). **Detector (deterministic, CPU-only):** relocates a single unit-start **LABEL** region to immediately before its detected same-page **body** when the label's list-successor cannot be its body (unit BROKEN) and the body is the UNIQUE candidate within a bounded region-distance (`_MOVE_MAX_REGION_DISTANCE=12`) + FB-gap (`_MOVE_MAX_FB_GAP=40`), with a **heading barrier** (no heading strictly between label and body) + same-page constraint; two-or-more candidates → NOTHING (fail-closed). The label-moves-to-body DIRECTION (D5, **OWNER-GATED — O2**) keeps a minimal, provably-misplaced blast radius. **Live additionally REQUIRES the ITEM2 region bbox** on both label and body (body top within `_MOVE_BBOX_Y_EPS=24pt` of the label bottom + ≥`_MOVE_BBOX_X_OVERLAP=0.5` horizontal overlap on the shared page); a candidate without bbox corroboration stays **shadow-only** even in live mode (so the shadow audit also measures ITEM2 coverage). **Zero sourceId blast radius:** a MOVE only permutes the region LIST — every region's `feature_block_indices` / `source_region_id` / min-FB `data-dart-block-id` are untouched (strictly safer than merge/split, which shrink/grow the id set). **Fail-closed ladder:** per-candidate validation (scratch apply + both gates) drops a bad move alone; the whole-set gate uses the pre-move input + `move_ops=applied_moves`; ANY failure reverts ALL THE WAY to the pre-move region list. Rides the existing `block_resegment` DecisionCapture enum (NO schema change) — the per-doc capture rationale gains conditional move/shadow-move tallies + reason set + a bounded `src->dest` source_id sample (a doc with no move op is byte-identical). Falsey / unset / garbage → shadow (parse-with-fallback). Gate only — deterministic region relocation, selects no provider/model → no separate `docs/LICENSING.md` row. |
| `SEMANTIK_BLOCK_RESEGMENT_LLM` | unset (off) | OPTIONAL Stage-5e **LLM op-proposal** layer gate (`block_resegment.py::resolve_block_resegment_llm_mode`). Default OFF → the deterministic detectors are the only source of join/split ops (load-bearing, no endpoint touched). Truthy → the hosted large-model endpoint seat (same seat as the Stage-6 specialist / Stage-5d reviewer) is asked to propose EXTRA boundary ops; each proposed op is re-validated by the SAME R-PART + token-conservation gates (a hallucinated / unsafe op is dropped individually) and an endpoint-down LLM keeps the deterministic result. **ITEM6:** the op-proposal digest (`block_resegment_prompt._region_obj`) now carries an additive `role_top_k` evidence field read off `region.provenance` (stamped before Stage-5e runs); absent for unstamped/mock regions → byte-stable digest. No-op when `SEMANTIK_BLOCK_RESEGMENT` is off. Falsey / unset / garbage → off. Gate only — routes to the already-licensed specialist seat, no separate `docs/LICENSING.md` row. |
| `SEMANTIK_SPLIT_FUSED_SECTION_TITLES` | unset (off) | Stage-5e **fused-section-title SPLIT** arm gate (`SemantiK/dart_semantic/qwen_specialists/block_resegment.py::resolve_split_fused_section_titles_mode`). Default OFF → byte-identical to today (no fused-title op emitted; the `_detect_fused_title_splits` detector never runs, no `resegment` breadcrumb / audit row gains a `subtype='fused_title'` key). Truthy (`1`/`true`/`yes`/`on`, case-insensitive) → a DETERMINISTIC third orthogonal Stage-5e detector SPLITS a non-heading, non-passthrough region whose leading FeatureBlock(s) are WHOLLY a title shape — a chapter-consistent `N.M Title-Case` (N == the document's dominant chapter ordinal, M in 1..40, ≤10 words) OR a gutter-glyph-prefixed apparatus opener (`Introduction`/`Key Terms`/`Key Concepts`/`Chapter Review`/`Review Exercises`/`Practice Test`/`Summary`, ≤5 words) — followed by ≥1 sentence of prose (≥8 words + terminal `.`/`!`/`?`), cutting at the FB boundary between title and prose (subtype `fused_title`; title may wrap over up to 3 leading OCR-line FBs). The title-shape SoT (`_STANDALONE_SECTION_RE` + the `_APPARATUS_OPENER_PATTERNS` tuple + `_matches_apparatus_opener`) is co-homed in `deterministic_structure.py` and REUSED by the promotion hook (never re-derived). The FB is the atom: MERGE never happens here and SPLIT cuts only at FB boundaries, so TEXT is never rewritten. Rides the EXISTING `apply_resegment` + R-PART partition + token-conservation harness with TWO fail-closed belts — an INNER per-op re-validation (structural interior-cut check + the same conservation gates `apply_proposed_regroups` rides; a wrong split is dropped INDIVIDUALLY, never poisoning the pass) and the driver's OUTER whole-set gate (set→det-only→input). **Precedence:** regroup > fused-title > same-kind (appended in that order so `apply_resegment`'s first-registered overlap guard drops a colliding same-kind split/merge whole rather than the title recovery, while a regroup consuming the region still wins). **Promotion interaction (`SEMANTIK_PROMOTE_SECTION_HEADINGS`):** the clean_structure sub-pass E promotion runs at Stage-5d-det (BEFORE Stage-5e) so it never sees the split-off child; a narrow post-5e hook `deterministic_structure.promote_fused_title_children` (gated on BOTH this flag AND `SEMANTIK_PROMOTE_SECTION_HEADINGS`, re-verifying the title shape via the shared SoT, reusing `_promote_to_heading` level_hint=2, fail-closed whole-revert on token-conservation) promotes the title child (`child_index==0`) to a section heading. split-on/promote-off → the title child stays a `<p>` (still an improvement; promotable later by rerender); promote-on/split-off → byte-identical to today (sub-pass E untouched — the apparatus-opener promotion is reachable ONLY through the new hook). **Intra-FB detect-only limitation:** a title fused with prose INSIDE ONE FeatureBlock is structurally unsplittable (R-PART forbids intra-FB cuts) — the detector counts a `fused_title_intra_fb` diagnostic (logged) and emits NO op; Phase-1 300-DPI re-raster may unfuse some such lines upstream, shrinking this detector's target set. **sourceId blast radius (honest):** the SPLIT is the mirror of the regroup — R-PART conserves the FB multiset but child 0 keeps the parent id (min-FB preserved) while children ≥1 MINT new `data-dart-block-id` / `sourceId` from their slice min (`_build_region_provenance.first_raw`), so the id SET GROWS. Deterministic per run (same PDF + same flags → same ids), but a flag flip changes which ids exist — matches the documented `SEMANTIK_UNIT_REGROUP` sourceId-blast-radius posture (resegment is allowed to change which `block_id`s exist; the regroup itself is now default-ON as of ITEM1, this fused-title arm stays off by default); downstream chunk refs / `source_module_map.json` / citation deep-links from a PRIOR run must be re-derived after a flip. Falsey / unset / garbage → off (parse-with-fallback). Gate only — deterministic region split, selects no provider/model → no separate `docs/LICENSING.md` row. |
| `SEMANTIK_UNIT_REGROUP` | `1` (default ON — deviation) | Stage-5e **cross-kind pedagogical-unit REGROUP** pass gate (`SemantiK/dart_semantic/pedagogical_units.py::resolve_unit_regroup_mode`, re-exported from `qwen_specialists/block_resegment.py`). **Default ON (ITEM1, calibrated 2026-07 via `scripts/regroup_calibration_ab.py`: 0 dual-oracle over-merges, full label-only-box elimination on an anchored corpus, byte-identical no-op on an anchor-free control).** The direct fix for split example-label/body units is now the default; the falsey-set parse (unset / blank / truthy / garbage → on) mirrors the house default-ON pattern of `SEMANTIK_READING_ORDER_FIX` / `SEMANTIK_STRUCTURE_CLEAN`. **`SEMANTIK_UNIT_REGROUP=0` is the byte-identical revert lever** (the ONLY explicit falsey reverts to the legacy split-label/body partition — proved byte-identical on both an anchor-free fixture and an anchored textbook). Explicit falsey → byte-identical to the legacy pass-through (no regroup op emitted; the `_detect_unit_merges` detector is uncalled, the cascade region partition is unchanged). Default-ON / truthy → a DETERMINISTIC cross-kind detector keys off the always-on `payload['css_class']` pedagogy hints (and `payload['semantic_class']` when the reviewer ran) to fuse a unit's LABEL region with its following BODY regions (problem / Solution / steps) into ONE region, walking forward until the first boundary (next unit-start label / heading / next body-bearing component / empty region / passthrough table-math / `ABSORB_MAX_RUN`=8 count cap / a Stage-6-serving-window token-budget cap), so the unit's `semantic_class` carries onto the merged region and the per-region gold box wraps the WHOLE example. Rides the EXISTING `apply_resegment` + R-PART partition + token-conservation fail-closed harness (no new merge engine). **Additionally REQUIRES the reading-order fix on** (`SEMANTIK_READING_ORDER_FIX`, default on — the Phase-6 driver guard; only then is a unit's regions contiguous for a clean forward-adjacency merge); the merge FIRES off `css_class` alone, but its USER-VISIBLE gold box additionally requires `SEMANTIK_GOLD_SHELL` on. **v1 passthrough gap:** a Stage-4 table/math passthrough region (non-null `source_region_id`) is a HARD unit BOUNDARY — the forward walk stops before it so the box ends just before an embedded table and the table renders as its own adjacent region (the Stage-4 link is never orphaned, since `_merged_region` cannot preserve a child's `source_region_id`). **Resolved by `SEMANTIK_UNIT_REGROUP_TABLE` (table-v2):** rather than a Stage-5e split-then-regroup, a render-time narrow passthrough-only absorb (`assembler/gold_shell_markup.py::compute_absorption_runs(passthrough_only=True)`) span-wraps the trailing FB-adjacent passthrough table INSIDE the box while it stays a distinct `source_region_id` Region — the absorb relocates already-rendered bytes POST-Stage-6, never the Region, so the Stage-4 link survives untouched (see that flag's row). **sourceId blast radius (Phase 8 — honest):** the merge REDUCES region count (N→1), so the FB partition *multiset* stays conserved (R-PART) but the `data-dart-block-id` / `sourceId` *SET shrinks* — every BODY region's id folds into the LABEL region's `min(feature_block_indices)` (the unit's stable id = the label's min-FB id; `cascade.py::_build_region_provenance`'s `first_raw`). This is NOT a stable multiset like the reading-order reorder fix; it matches the same-kind `_detect_merges` precedent. **On-by-default consequence (ITEM1):** a re-conversion of a previously-converted corpus under the new default mints a SMALLER id set than a pre-flip conversion (body regions fold into their label's id), so downstream chunk refs / `source_module_map.json` / citation deep-links from a PRIOR (pre-flip) run must be re-derived ONCE after upgrading; deterministic per run (same PDF + same flags → same ids), and `SEMANTIK_UNIT_REGROUP=0` restores the pre-flip id set exactly. **Mutual exclusion with `SEMANTIK_GOLD_ABSORB` (Phase 7):** when this regroup is FIRING (regroup-on **AND** the reading-order fix on), it SUPERSEDES the assembler-side absorb — `assembler/shell.py::resolve_gold_absorb_mode` short-circuits the absorb OFF so the unit is never double-boxed; when this regroup is INERT (reading-order off), the absorb remains the fallback. **Decision-capture (Phase 9):** a fired regroup enriches the `result['block_resegment']` audit row (`op='regroup'` + `semantic_class` + `regions_folded`, via `block_resegment.build_resegment_audit_rows`) riding the already-present `block_resegment` `decision_type` enum (NO schema change); a fired regroup now emits through the per-doc `block_resegment` DecisionCapture at the MCP seam (regroup count + merged-unit `semantic_class` + `regions_folded` interpolated into the dynamic rationale), and the emit is wired for BOTH the in-process and cross-venv bridge arms. **Verifier seam (Phase 10 + ITEM3 Phase 3):** `block_resegment.apply_proposed_unit_fix` lets an external proposer (the second-pass verify-refine plan's `section_no_body` / `example_misordered_from_body` fix) pass explicit `region_indices` runs into the SAME `apply_resegment` + R-PART/token gates — a CONTIGUOUS run merges directly (delegating to `apply_proposed_regroups`), and a hallucinated/over-broad run is dropped per-op. **ITEM3:** a NON-contiguous run (previously SILENTLY DROPPED at the R-PART order gate) is now DECOMPOSED into MoveOps + a contiguous merge when `SEMANTIK_MOVE_OP=live` (each move bounded + individually gated, whole run dropped fail-closed if any member is unboundable); non-live (`off`/`shadow`) delegates wholesale to `apply_proposed_regroups` → byte-identical (the non-contiguous run is dropped exactly as before). **ITEM4 interplay:** `SEMANTIK_UNIT_REGROUP` is NOT retired — it remains the *region-level* unit fuser at Stage-5e; the `SEMANTIK_CONTAINMENT` tree's unit arm covers the *residue* (regroup-inert runs, trailing FB-adjacent passthrough tables, post-regroup stragglers) at emit time, and with the regroup default-ON most units arrive already fused into ONE region so the tree's unit arm is usually a no-op. Falsey / unset / garbage → off (parse-with-fallback). Gate only — deterministic region merge, selects no provider/model → no separate `docs/LICENSING.md` row. |
| `SEMANTIK_UNIT_REGROUP_TABLE` | unset (off) | **RETIRED / SUBSUMED — the `SEMANTIK_CONTAINMENT` (ITEM4) unit arm claims the trailing FB-adjacent passthrough table UNCONDITIONALLY (no sub-flag), so `resolve_narrow_table_absorb_mode` short-circuits `False` whenever containment resolves ON (the default); consulted ONLY when `SEMANTIK_CONTAINMENT=0`.** table-v2 sub-flag — narrow render-time **passthrough-only absorb** that span-wraps a worked example's trailing Solution TABLE *inside* its gold box (resolver `SemantiK/dart_semantic/pedagogical_units.py::resolve_unit_regroup_table_mode`, re-exported from `qwen_specialists/block_resegment.py`; gate composite `assembler/shell.py::resolve_narrow_table_absorb_mode`; mechanism `assembler/gold_shell_markup.py::compute_absorption_runs(passthrough_only=True)` wired at the `assembler/pass_9a.py` Sub-task-1b `elif`). Default OFF → **byte-identical to the committed v1 regroup** (the `compute_absorption_runs` `passthrough_only` kwarg defaults False, the `pass_9a` `elif` is dead, the Stage-5e partition is unchanged, the table renders ADJACENT to the box). Truthy (`1`/`true`/`yes`/`on`, case-insensitive) → a render-time FB-adjacency-bounded passthrough-only span-wrap pulls the trailing FB-contiguous Stage-4 table/math/figure passthrough (`is_passthrough_region` — non-null `source_region_id`) into the body-bearing-anchor box; **the table stays a distinct `Region(kind='table', source_region_id=N)`** so its Stage-4 link + GLM-OCR enrichment + table-specialist routing survive untouched (the absorb runs POST-Stage-6 on rendered bytes, never the Region); the box appears ONCE, the table renders ONCE via `absorbed_indices`. The forward walk stops at the first non-passthrough OR non-FB-adjacent follower (`cur_first == prev_last + 1`) + the `ABSORB_MAX_RUN`=8 cap, so a standalone section table / the next unit's opening table is never over-captured; a post-table conclusion paragraph stays OUTSIDE the box (documented owner-tunable scope limit). **GATED UNDER `SEMANTIK_UNIT_REGROUP` (now default ON, ITEM1)** so in a default env the composite reduces to this sub-flag AND `SEMANTIK_READING_ORDER_FIX` (also default-ON) — no-op unless the regroup is firing, the SAME composite `resolve_gold_absorb_mode` short-circuits the full absorb under; the visible box additionally requires `SEMANTIK_GOLD_SHELL` on. NOTE: this sub-flag's resolver stays a truthy-set OPT-IN (its body no longer mirrors `resolve_unit_regroup_mode`'s default-ON falsey-set — it is render-inert while `SEMANTIK_GOLD_SHELL` is off, so it was deliberately NOT flipped with the v1 regroup). The merged anchor's body-bearing `semantic_class` is supplied by the css_class FLOOR in `_merged_region`, so the anchor exists INDEPENDENT of `SEMANTIK_SEMANTIC_CLASS`/`SEMANTIK_BLOCK_REVIEW` (and cleanly no-ops when the FLOOR yields no valid component). Mutually exclusive with the full `SEMANTIK_GOLD_ABSORB` at the `pass_9a` call site (`if resolve_gold_absorb_mode(): … elif resolve_narrow_table_absorb_mode(): …`). Falsey / unset / garbage → off (parse-with-fallback). Gate only — deterministic render-time span-wrap, selects no provider/model → no separate `docs/LICENSING.md` row. |
| `SEMANTIK_BLOCK_REVIEW` | unset (off) | Stage-5d full-block **structural-editor reviewer** master gate (`SemantiK/dart_semantic/qwen_specialists/reviewer.py::resolve_block_review_mode`, ~L387). Default OFF → byte-identical to today (the reviewer stays heading-only; no full-block re-type / drop dispatch). Truthy (`1`/`true`/`yes`/`on`, case-insensitive) → widens the Stage-5d reviewer into a structural editor over block IDs (re-type / drop, partition-immutable; merge / split ride Stage-5e under `SEMANTIK_BLOCK_RESEGMENT_LLM`) that emits ops keyed by index, never text, so verbatim source rides deterministic assembly. **ITEM6:** the windowed edge records now carry an additive `council_top_k` field (the persisted role distribution, k=3 @2dp — from `council_state` or a fallback to `region.provenance['role_top_k']` on the post-run re-drive path) and the windowed prompt appends a council-top-k re-typing directive when any record carries it (windowed path ONLY — the single-block heading path stays byte-identical); the per-window DecisionCapture metadata gains `council_margin_min`/`_mean`. One-time consequence: the `SEMANTIK_BLOCK_REVIEW_CACHE` content-hash window-op cache MISSES once after upgrade (the edge records changed shape) — harmless pure memoization. Falsey / unset / garbage → off (parse-with-fallback). Gate only — rides the already-licensed specialist seat, no separate `docs/LICENSING.md` row. |
| `SEMANTIK_BLOCK_REVIEW_WINDOW` | `24` | Max blocks packed into ONE windowed block-review POST (`reviewer.py::resolve_block_review_window`, ~L399). Parse-with-fallback: blank / non-int / non-positive / garbage → `24` (mirrors `resolve_specialist_batch_regions`). No-op until the Phase-4 windowed dispatch consumes it; the flag-off path is byte-identical. Gate only — rides the already-licensed specialist seat, no separate `docs/LICENSING.md` row. |
| `SEMANTIK_BLOCK_REVIEW_EDGE_TOKENS` | `12` | Head / tail tokens kept per block in the edge-input record fed to the block reviewer (`reviewer.py::resolve_block_review_edge_tokens`, ~L418) — furniture-deduped edges keep full-document review tractable on a 7B. Parse-with-fallback: blank / non-int / non-positive / garbage → `12`. No-op until the Phase-1 edge-input builder consumes it; the flag-off path is byte-identical. Gate only — rides the already-licensed specialist seat, no separate `docs/LICENSING.md` row. |
| `SEMANTIK_BLOCK_REVIEW_CACHE` | `1` (on) | Content-hash **window-op cache** gate for the block reviewer (`reviewer.py::resolve_block_review_cache_mode`, ~L438; consumed in Phase 4b). Default ON → pure memoization, output-identical (mirrors the extract / council disk-cache). Default-on parse semantics: explicit falsey (`0`/`false`/`no`/`off`) → off; unset / blank / truthy / garbage → on (mirrors `SEMANTIK_STRUCTURE_CLEAN` / `SEMANTIK_SPECIALIST_BATCH`). Gate only — rides the already-licensed specialist seat, no separate `docs/LICENSING.md` row. |
| `SEMANTIK_BLOCK_REVIEW_CONF` | `0.4` | Council `structural_role` top-1 **confidence floor** — the SECONDARY arm of the content-block dispatch gate (`reviewer.py::_content_block_dispatch_gate` / `resolve_block_review_conf_floor`). Per the Phase-6 live-validation recalibration (commit `7e3f842`), the council's **known-weak kinds `code_block` + `table` are dispatched UNCONDITIONALLY** (their high "confidence" is uninformative for systematic content-type errors — the council confidently mislabels "TRY IT" exercises as code_block), so this floor does NOT gate them. It applies only to the OTHER content kinds (`paragraph`/`list`/`blockquote`): such a block is dispatched when it carries a TRY/EXAMPLE/EXERCISE pedagogical-label prefix OR its council confidence is below this floor — a plain high-confidence paragraph gets NO prompt (so the reviewer doesn't re-judge all prose). Float parse-with-fallback: blank / non-float / NaN / Inf / out-of-`[0.0, 1.0]` → `0.4`. No-op when `SEMANTIK_BLOCK_REVIEW` is off. Gate threshold only — rides the already-licensed specialist seat, no separate `docs/LICENSING.md` row. |
| `SEMANTIK_SEMANTIC_CLASS` | unset (off) | Stage-5d reviewer **gold accessible semantic-class** assignment gate (`SemantiK/dart_semantic/qwen_specialists/reviewer.py::resolve_semantic_class_mode`, ~L514). Default OFF → byte-identical to today (the reviewer assigns no `semantic_class`; only `REGION_KINDS` re-typing applies). Truthy (`1`/`true`/`yes`/`on`, case-insensitive) → the reviewer ALSO assigns a gold accessible-component `semantic_class` (orthogonal to `REGION_KINDS`) keyed by block index, stamped onto `region.payload['semantic_class']`. **Additionally requires `SEMANTIK_BLOCK_REVIEW` on** — it rides the P1 content-review dispatch. Falsey / unset / garbage → off (parse-with-fallback). No-op until Phase 4+ consumes it; the flag-off path is byte-identical. Gate only — rides the already-licensed specialist seat, no separate `docs/LICENSING.md` row. |
| `SEMANTIK_GOLD_SHELL` | unset (off) | Assembler **gold ARIA markup + document shell** emit gate (`SemantiK/dart_semantic/assembler/shell.py::resolve_gold_shell_mode`, ~L28). Default OFF → byte-identical to today (bare `<p>` / flat concat, minimal shell; the assembler never reads `payload['semantic_class']`). Truthy (`1`/`true`/`yes`/`on`, case-insensitive) → the assembler emits the gold per-region ARIA wrap keyed by `payload['semantic_class']` + the gold accessible shell (`<style>` bundle + `<article>`/`<header>`/`<footer role=contentinfo>` landmarks + schema.org accessibility JSON-LD). **ITEM4:** when `SEMANTIK_CONTAINMENT` is on (the default) the gold branch's `<section>` grouping + unit boxes + caption nesting are emitted by the containment **tree walk** (`containment.render_tree`) instead of the legacy `_group_regions_into_sections` + gold-absorb (the walk also fixes the caption double-render + label-only box). Falsey / unset / garbage → off (parse-with-fallback). No-op until Phase 7+ consumes it; the flag-off path is byte-identical. Gate only — rides the already-licensed specialist seat, no separate `docs/LICENSING.md` row. |
| `SEMANTIK_GOLD_ABSORB` | unset (off) | **DEPRECATED — superseded by `SEMANTIK_CONTAINMENT` (ITEM4); consulted ONLY when `SEMANTIK_CONTAINMENT=0`.** The materialized containment tree's unit arm subsumes the absorb, so `resolve_gold_absorb_mode` short-circuits to `False` whenever `SEMANTIK_CONTAINMENT` resolves ON (the default) — BEFORE any other check. Code retained solely as the OFF-lever fallback pending the post-soak deletion (OPEN Q1). (Prior ITEM1 deprecation: the default-on Stage-5e regroup `SEMANTIK_UNIT_REGROUP` already made this engage only under an explicit legacy revert `SEMANTIK_UNIT_REGROUP=0` / `SEMANTIK_READING_ORDER_FIX=0`, with a one-shot `logger.warning` when it did — still true when containment is explicitly off.) Assembler **body-region absorption** stopgap gate (`SemantiK/dart_semantic/assembler/shell.py::resolve_gold_absorb_mode`; absorption pre-step in `assembler/pass_9a.py` Sub-task 1b, run helper `assembler/gold_shell_markup.py::compute_absorption_runs`). **Additionally requires `SEMANTIK_GOLD_SHELL` on** (absorption only matters once components are wrapped — absorb-off-with-shell-on is byte-identical to the v3 gold render). Fixes a live render defect: the council emits a `worked_example`'s LABEL ("EXAMPLE 1.3") and its BODY (problem statement / "Solution" / steps) as SEPARATE regions, so the Phase-7 per-region wrap boxes only the label. Default OFF → byte-identical to the pre-absorption gold wrap (one region per box). Truthy (`1`/`true`/`yes`/`on`, case-insensitive) → a body-bearing component region (`semantic_class` ∈ {`worked_example`, `definition_region`, `exercise`}) ABSORBS the immediately-following sibling regions into its container box, stopping at the FIRST boundary — a heading region, a unit-opening pedagogical label (`payload['css_class']` ∈ {`pedagogy-example`/`pedagogy-try-it`/`pedagogy-how-to`/`pedagogy-be-prepared`/`pedagogy-practice`}), the next body-bearing component, or an empty fragment — and capped at a conservative `_ABSORB_MAX_RUN` (8) regions so a mis-detected boundary can't swallow a section. `pedagogy-solution` / `pedagogy-step` regions + plain paragraphs/tables ARE absorbed (they are the example's body). The box encloses the CONCATENATED raw `region_html[i..j]` fragment bytes (never mutated → every `pass_9c` flat string-replace splice key survives as a contiguous substring inside the box); absorbed indices are skipped by the Sub-task-6 `<section>` grouping so they render exactly once. A conservative, gated STOPGAP — the proper Stage-5e `SEMANTIK_UNIT_REGROUP` region-level regroup supersedes it. **SUPERSEDED when the regroup is FIRING (Phase 7):** when `SEMANTIK_UNIT_REGROUP` AND the reading-order fix (`SEMANTIK_READING_ORDER_FIX`, default on) are both on — the SAME composite the regroup fires under — `resolve_gold_absorb_mode` short-circuits to `False` regardless of this flag, so the merged-at-Stage-5e unit is boxed EXACTLY ONCE by the per-region wrap (no double-box). The short-circuit requires BOTH (not `SEMANTIK_UNIT_REGROUP` alone): with the reading-order fix OFF the regroup is inert, so killing the absorb too would box NEITHER (a regression) — the absorb stays the fallback whenever the regroup is inert. (Defense-in-depth: post-merge the anchor has no absorbable follower, so `compute_absorption_runs` is a natural no-op anyway.) Byte-identical when `SEMANTIK_UNIT_REGROUP` is off. Falsey / unset / garbage → off (parse-with-fallback). Gate only — rides the already-licensed specialist seat, no separate `docs/LICENSING.md` row. |
| `SEMANTIK_SECOND_PASS` | unset (off) | Stage-9 **Pass-2 verify-refine loop** master gate (`SemantiK/dart_semantic/qwen_specialists/reviewer.py::resolve_second_pass_mode`, ~L544). Default OFF → byte-identical to today (no verifier prompt built, no dispatch, no extra assemble round; the cascade result dict carries NO `second_pass_verify` key). Truthy (`1`/`true`/`yes`/`on`, case-insensitive) → after assembly, a Pass-2 VERIFIER judges the assembled-document digest (heading outline + per-region `{kind, semantic_class, aria_wrapper, head/tail}`) and bounces ONLY the flagged region indices back through the EXISTING Pass-1 reviewer (re-type / re-stamp keyed by index, never text), bounded (`SEMANTIK_SECOND_PASS_ROUNDS`, default 2) + FAIL-SAFE (the Pass-1 snapshot is the default return on non-convergence / any verifier error / a not-better round — never ships worse; the adopt gate's PRIMARY check is unflagged-regions-byte-identical, guarding against whole-doc Stage-6 re-author drift). The converged verdicts thread back into `region_provenance` / the `structure_review` audit (`review_regions=None` on adopt — the post-regroup min-FB-join drift fix); a per-round `DecisionCapture` (`structure_review` decision_type + `second_pass_verify`/`round_index` metadata discriminator, `MCP/tools/pipeline_tools.py::_emit_second_pass_verify_captures`) records flagged/fixed/deferred per round. **Additionally requires `SEMANTIK_STRUCTURE_REVIEW` + `SEMANTIK_BLOCK_REVIEW` on** (the verifier judges + bounces a re-typed post-assembly result). **Detect-but-cannot-fix limitation:** the index-keyed reviewer emits per-block re-type / re-stamp ONLY, so `section_no_body` / `example_misordered_from_body` are now FIXABLE **by default** (`SEMANTIK_UNIT_REGROUP` is default-ON as of ITEM1) — the Phase-6b move+merge channel graduates them via `block_resegment.apply_proposed_unit_fix`; they fall back to DETECT-ONLY only when the regroup is explicitly reverted (`SEMANTIK_UNIT_REGROUP=0`) (the verifier proposes a region-index run, re-validated through the R-PART/token gates, count may shrink; the merge-arm of `no_context_answer_fragment_heading` stays detect-only as a ground-truth backlog recorded in the capture). **ITEM3:** a NON-contiguous `example_misordered_from_body` run — the label PHYSICALLY separated from its body in the region list — is no longer dropped: under `SEMANTIK_MOVE_OP=live` it decomposes to MoveOps + a contiguous merge (`apply_proposed_unit_fix`), so a genuinely misordered example graduates from detect-only to end-to-end fixable. Falsey / unset / garbage → off (parse-with-fallback). Gate only — rides the already-licensed specialist seat (the verifier reuses the Pass-1 `make_runtime("endpoint", …)` seat, no new model/seat knob), no separate `docs/LICENSING.md` row. |
| `SEMANTIK_SECOND_PASS_ROUNDS` | `2` | Hard upper **bound on verify-refine rounds** for the Pass-2 loop (`reviewer.py::resolve_second_pass_rounds`, ~L559; the Courseforge C4 `refine_rounds=2` precedent). Parse-with-fallback: blank / non-int / non-positive / garbage → `2` (mirrors `resolve_block_review_window`). No-op when `SEMANTIK_SECOND_PASS` is off; consumed by the Phase-6 loop, a pure read until then. Gate only — rides the already-licensed specialist seat, no separate `docs/LICENSING.md` row. |
| `SEMANTIK_SECOND_PASS_WINDOW_TOKENS` | `8000` | Per-window token budget for the Pass-2 verifier's **section-aligned digest windowing** (`reviewer.py::resolve_second_pass_window_tokens`; packer `assembler/verify_digest.py::window_verifier_digest`). The committed `run_second_pass_verify` built ONE digest over the WHOLE assembled doc (a real run distilled 2190 regions → 1830 entries → ~112k tokens) and dispatched a SINGLE verifier call, so a 16k Ollama serving window head-truncated ~86% of the prompt and the verifier judged a fragment (a seeded `EXAMPLE`-as-heading defect went uncaught). The digest's per-region entries are now packed into SECTION-ALIGNED windows (a heading + its content; whole sections never split, an over-budget section entry-range sub-splits) bounded by this budget, dispatched one call per window, and aggregated (passed=all, flagged=union-deduped-by-region_index, fail-safe per window) so a real-sized doc's digest fits the serving window. The default 8000 yields ~13-15 windows on the live doc. Parse-with-fallback: blank / non-int / non-positive / garbage → `8000` (mirrors `resolve_second_pass_rounds`); a very large value collapses to a SINGLE whole-doc call (today's behaviour — the byte-stable revert lever). No-op when `SEMANTIK_SECOND_PASS` is off. Gate only — rides the already-licensed specialist seat, no separate `docs/LICENSING.md` row. |
| `SEMANTIK_OCR_CONFUSABLE_REPAIR` | unset (off) | **Bounded OCR-confusable micro-repair pass** (plan `plans/scan-conversion-improvements-2026-07.md` Phase 5 channels 2+3; resolver `SemantiK/dart_semantic/qwen_specialists/ocr_repair.py::resolve_ocr_confusable_repair_mode`). Default OFF → byte-identical to today (zero detector work, zero LLM call; the cascade result carries NO `ocr_repair` key + no `repaired_text` / `ocr_repair` provenance keys). Truthy (`1`/`true`/`yes`/`on`, case-insensitive) → after the Stage-9 verify loop and BEFORE Stage 13, a deterministic garble detector flags regions with OCR confusions (`√x`→`Vx`, `l`↔`1`, `O`↔`0`, stray `™`/curly-quotes; digit-dense answer lines excluded at detect time), windows the flagged texts (reusing `SEMANTIK_SECOND_PASS_WINDOW_TOKENS`) and asks the 7B to PROPOSE `{region_index, before, after}` micro-edits on the **already-licensed specialist seat** (`make_runtime('endpoint', …)` — no new provider/model/seat). A DETERMINISTIC accept gate owns the decision: an edit lands only if reachable via the frozen `CONFUSABLE_MAP` (the sole substitution vocabulary → char-class-safe by construction), Levenshtein ≤ 2, within-token, NOT in a numeric context, non-identity, and the region was flagged+in-window; per-block cap `_MAX_EDITS_PER_BLOCK=8` + a per-doc runaway tripwire (>2% edited tokens → revert whole pass). The pass NEVER mutates region text: `raw_text` stays VERBATIM (the content-hash sourceId basis, so `_mint_sid`/`TRAINFORGE_CONTENT_HASH_IDS` stability is preserved), and the ADDITIVE `repaired_text` + `ocr_repair` edit map ride `region_provenance` → the adapter substitutes the repaired string at render (stamped `data-dart-repair="ocr-confusable"` + `data-dart-repair-count` on the section OPENING tag) after a strict replay-conservation check (`assert_repair_conservation`; any drift → revert to verbatim, fail closed). The `_synthesized.json` sidecar `text` becomes the repaired string (retrieval fidelity) + a mirrored `repair.original_text` (reconstructible). Channel 3: a repair-stats proxy score amends the STUBBED theta semantic-preservation dimension (`theta/evaluator.py::apply_repair_stats`, method `ocr_repair_stats_v1`, ceilinged 0.95) so a stub box re-enters the real tau exit path. Decision capture rides the existing `structure_review` decision_type with an `ocr_confusable_repair` metadata discriminator (NO decision_event schema change). Falsey / unset / garbage → off (parse-with-fallback). Gate only — rides the already-licensed specialist seat, no separate `docs/LICENSING.md` row. |
| `SEMANTIK_EMIT_TOC` | `on` (default ON — deviation) | Table-of-contents nav emit at the adapter render seam (`lib/semantik/adapter.py::_resolve_emit_toc` / `_build_toc_html`). Default ON → a `<nav class="toc" aria-label="Contents">` with an anchor `<ol>` built from the h2 (chapter) / h3 (section) heading tree is inserted after the document `<h1>` on BOTH the live-conversion render and the `scripts/semantik_rerender.py --ir` path. Pure addition (exemplar-parity wave §4): the anchors reference EXISTING chapter (`#chap-N`) / section (`#{sid}`) ids via the same `_mint_unique_sids` map the render uses, so it mints no new id, adds no heading element (openers at `<h4>` are excluded — the TOC mirrors the h2/h3 tree only), and leaves the landmark + heading-hierarchy contract intact. Opt out with `=0`/`false`/`no`/`off` (case-insensitive) for a fragment with no TOC. Gate only — deterministic render-time markup, selects no provider/model → no `docs/LICENSING.md` row. |
| `SEMANTIK_LEXICON_PROFILE` | `generic-academic+open-textbook` | **Pedagogical-lexicon profile selector** (Wave #22 tiers 1-2 semantic enrichment; resolver `lib/ontology/taxonomy.py::resolve_lexicon_profile`, loader `load_semantik_lexicon` over `schemas/taxonomies/semantik_lexicon.json`). Selects the profile spec (`+`-joined profile keys, merged left-to-right, later profile OVERLAYING earlier) whose opener / apparatus-heading / OCR-confusable vocabularies drive the adapter-seam classifiers (`lib/semantik/opener_classifier.py`, `heading_classifier.py`, and the `adapter.py` confusable-marker pass) AND the Tier-2 composite-unit association roles. A new corpus is onboarded by adding a PROFILE / label row to the JSON lexicon, never a code edit (owner directive 3). Default `generic-academic+open-textbook` merges the corpus-agnostic academic base (Example / Solution / How To openers, Key-Terms-class apparatus, short-heading whitelist) with the open-textbook overlay (TRY IT / BE PREPARED numbered openers, the `trvit`→`TRY IT` confusable) and reproduces the historical hardcoded classifier vocab byte-for-byte (behavior-preserving refactor). Legacy publisher-named profile-key spellings are aliased silently at the resolver seam (`lib/ontology/taxonomy.py::LEGACY_PROFILE_ALIASES` — pre-rename compat), so a pre-rename operator env value keeps resolving the same vocabulary. Opt-in overlays (Build #24, additive — change nothing for `generic-academic`/`open-textbook`): `scholarly` (e.g. `generic-academic+scholarly`) maps Theorem/Lemma/Proposition/Corollary/Proof/Definition/Remark/Notation openers so a theorem statement + its proof + trailing remarks/corollaries coagulate into one `data-dart-unit="theorem_block"` composite unit (scholarly Definition + Example reuses the `definition_group` shape); `admonition` maps note/warning/tip/caution/important/danger/attention/hint callout labels, each label+body forming a singleton `data-dart-unit="admonition"` unit with the label as accessible name (`admonition` is the sole member of `composite_units.SINGLETON_UNIT_TYPES`, exempt from the ≥2-member-items floor). Unknown / empty spec → falls back to the default profiles (never a silently-empty vocabulary). Resolved once at module import (cached); set in the environment before the adapter loads. Gate only — deterministic vocabulary selection, selects no LLM provider/model → no `docs/LICENSING.md` row. |
| `SEMANTIK_SEMANTIC_SUBCLASS` | unset (off) | **Build #23 Tier-3 model-assisted composite-unit SUBCLASS pass** (`lib/semantik/subclassifier.py::resolve_semantic_subclass_enabled`; hooked into the render seam at `lib/semantik/adapter.py::normalize_cascade_to_ed4all` right after `_render_html`). Default OFF → **byte-identical** (no `data-dart-subclass` / `dart-sub-<label>` attribute emitted, `subclass_report` is `None`; the always-on unit `data-dart-pages` rollup is a SEPARATE unconditional provenance change, not gated here). Truthy (`1`/`true`/`yes`/`on`, case-insensitive) → after the chapters render, each Tier-2 composite unit (`<section data-dart-unit>`) is handed to a light local model that picks ONE finer subclass from the lexicon SEED vocabulary (`schemas/taxonomies/semantik_lexicon.json` `subclasses` section, per unit type — e.g. `worked_example`→`application-problem`/`symbolic-manipulation`) OR proposes a new kebab-case label. **Payload-only + strict + fail-safe:** the pass adds ONLY `data-dart-subclass="<label>"` + a `dart-sub-<label>` CSS class to the unit `<section>` open tag (never text / structure / sidecar); the model response is parsed strictly (single kebab-case token, ≤32 chars — else the unit stays unsubclassed); a model-proposed label within Levenshtein ≤2 / same-stem of a seed FOLDS into it, a genuinely-new label is accepted but LOGGED to a review sidecar (never canonized into the taxonomy); a unit whose member `data-dart-pages` are non-monotonic is SKIPPED (unreliable association); one `unit_subclass_assignment` DecisionCapture fires per unit with a dynamic rationale; and any client / parse error degrades that unit alone (render never breaks). The subclass RIDES the unit downstream: `Trainforge/parsers/html_content_parser.py` harvests `data-dart-subclass` onto the lead `ContentSection`, `Trainforge/chunker/chunker.py::aggregate_unit_subclass` pairs it with the resolved `composite_unit`, and the chunk emitters stamp the additive `unit_subclass` chunk_v4 field — adding the attribute NEVER changes chunk text or ids. An injected `subclass_client` (the `scripts/subclass_pilot.py` pilot / corpus-apply driver, mock in `--dry-run`) also forces the pass regardless of the flag. **Selects an LLM provider → has a `docs/LICENSING.md` row** — it reuses the EXISTING license-clean local reviewer seat (qwen2.5-7b via `LOCAL_SYNTHESIS_BASE_URL`/`_MODEL`/`_API_KEY` on an Ollama-style OpenAI-compatible server, Apache-2.0), env-resolved, no hardcoded endpoint. Falsey / unset / garbage → off (parse-with-fallback). |
| `SEMANTIK_SUBCLASS_SAMPLES` | `1` | **Build #25 Tier-3 self-consistency sampling knob** for the composite-unit subclassifier (`lib/semantik/subclassifier.py::resolve_subclass_samples`; consumed inside `annotate_html_subclasses`). Positive int, default `1`. `N=1` → **byte-identical** to the single-call behavior (one call per unit, the model's own self-reported confidence rides the assignment). `N>1` → each unit draws `N` sampled calls (the default local seat gets a non-zero `SUBCLASS_SAMPLE_TEMPERATURE` + per-sample `seed` so the 7B genuinely varies; a client that doesn't accept those params gets `N` identical calls) and each sample runs the SAME strict kebab parse + fold-on-ingestion normalization — a failed parse (or a raising call) drops out of the vote. The dominant folded label WINS with `confidence = dominant_count / valid_samples` (agreement fraction, REPLACING the model-reported confidence); ties resolve to the first winner seen in sample order; all samples failing skips the unit exactly as a single failed call (`parse_reject` when some response came back, `error` when every call raised). The `unit_subclass_assignment` DecisionCapture rationale additionally interpolates the sample distribution (labels + per-label counts + `valid/N`) so a vote is replayable. The page-anomaly guard + bucket-collapse detector operate on the FINAL (voted) labels — unchanged semantics. Parse-with-fallback: empty / non-int / `<1` / garbage → `1`. Gate only — rides the SAME already-licensed local reviewer seat as `SEMANTIK_SEMANTIC_SUBCLASS` (selects a sample COUNT, not a provider/model), so no separate `docs/LICENSING.md` row. |
| `SEMANTIK_RENDER_TIKZ_FIGURES` | unset (off) | **Build #27 TikZ figure re-draw gate (real-figure wave, plan `plans/tikz-real-figures-2026-07.md`)** — opt-in that RE-DRAWS the narrow VLM-emitted TikZ shape grammar as accessible inline SVG instead of the round-10 placeholder (`lib/semantik/adapter.py::_resolve_render_tikz_figures` / `_render_tikz_figures`, running immediately BEFORE `_strip_tikz_figures`; parser + emitter in `lib/semantik/tikz_draw.py`). Default OFF → byte-identical to the strip-only path (the validated mid-pipeline corpus is unchanged). When truthy (`1`/`true`/`yes`/`on`), each delimited math span carrying a `\begin{tikzpicture}…\end{tikzpicture}` of supported statements (grid/help-lines, `\draw` segment paths with thick/dashed/dotted/`->`, `\filldraw`/`\fill` circle+rectangle, `\node` labels incl. inline `$math$` as plain text, `[scale=…]`) is re-drawn: a PURE-figure span whose notation parses is replaced whole by `<figure class="dart-figure"><svg class="dart-figure-svg" role="img" aria-label=…><title>…</title>…</svg></figure>`; a span outside the grammar (pgfplots `axis`/`\addplot`/`plot coordinates`, `\includegraphics`, unknown options/colors, curve operators, empty pictures) is left untouched so the following strip pass emits the placeholder (parse-with-fallback, never a hard fail); a MIXED math+figure span keeps its surviving math and gains the placeholder, closing the round-10 silent-drop gap where mixed-span figures vanished with no trace. HTML-ONLY (`raw_text`/`repaired_text`/sidecar/chunk text keep the plain fused TikZ); idempotent; CSS `.dart-figure`/`.dart-figure-svg` bind `currentColor`→`--text-color` so the SVG is legible in both schemes. Falsey / unset / garbage → off. Gate only — deterministic render-time markup, selects no provider/model → no `docs/LICENSING.md` row. |
| `SEMANTIK_SPECIALIST_REFINE` | unset (off) | Hybrid two-phase Stage-6 refine gate (`SemantiK/dart_semantic/qwen_specialists/runner.py::resolve_refine_mode`, ~L105). Default OFF. Truthy (`1`/`true`/`yes`/`on`) → Phase 1 drafts on the **Phase-1 seat** (default local GGUF specialists) THEN Phase 2 sends each region's (prompt + draft) + directive to the **Phase-2 seat** for a polish pass. Model-agnostic: the Phase-2 seat may be hosted (NVIDIA), a local OpenAI-compatible server, or local GGUF — so local-draft→local-large-refine makes zero hosted calls. One of the two EXPLICIT opt-ins (with `SEMANTIK_SPECIALIST_ENDPOINT_DISPLACE`) that enable Phase 2; without either, only Phase 1 runs (the dereliction default). **Note:** hybrid now genuinely runs Phase 1 on the Phase-1 (local-by-default) seat — set `SEMANTIK_SPECIALIST_PHASE1_PROVIDER` non-local for hosted-draft→hosted-refine. Falsey / garbage → off. Routes to the already-licensed specialist seat — no separate licensing row. |
| `SEMANTIK_SPECIALIST_ENDPOINT_DISPLACE` | unset (off) | **Phase-2-only (pure refine/generate) opt-in** for Stage 6 + gap-fill (`runtime.py::resolve_endpoint_displaces_local`; consumed by `runner.py` Phase-routing and `assembler/pass_9b.py`). **Default OFF — the dereliction fix.** With neither this nor `SEMANTIK_SPECIALIST_REFINE`, only Phase 1 (the local-by-default draft seat) runs. Truthy (`1`/`true`/`yes`/`on`) → SKIP Phase 1; all regions are generated directly on the **Phase-2 seat** (`SEMANTIK_SPECIALIST_PHASE2_PROVIDER` > the single seat). The Phase-2 seat is model-agnostic — typically the hosted large-model seat (the pre-fix pure-endpoint behaviour) but equally a local OpenAI-compatible server or local GGUF. Stage-6 routing truth table: neither opt-in → Phase 1 only (draft seat); + refine → Phase 1 (draft seat) + Phase 2 (refine seat); + displace → Phase 2 only (refine seat). gap_fill (Stage 9, single-pass) still routes to the endpoint only on this displace opt-in. Does NOT affect the explicit `make_runtime("endpoint")` Stage-5d reviewer / Stage-5e resegment seats (those are intentionally hosted). Falsey / unset / garbage → off (parse-with-fallback). Gate only — rides the already-licensed `SEMANTIK_SPECIALIST_PROVIDER` seat, no separate `docs/LICENSING.md` row. |
| `SEMANTIK_STAGE6_PROSE_PASSTHROUGH` | unset (off) | **Stage-6 prose GGUF-generation SKIP gate** (`runner.py::resolve_stage6_prose_passthrough`; consumed in `run_qwen_specialists`). Default OFF → byte-identical (every `AdapterID.PROSE` region runs the normal K-candidate GGUF generation path). Truthy (`1`/`true`/`yes`/`on`, case-insensitive) → each PROSE-routed region (paragraph / heading / list / definition_list / code_block / blockquote / figure / form / metadata_drop) skips GGUF generation entirely and receives ONE deterministic verbatim passthrough Candidate — the region's source text rendered by the canonical per-kind `assembler/fallbacks.py::emit_fallback` (the SAME emitter the Stage-9 assembler uses when a region's candidates all die at the gate), so the fragment is well-formed HTML5 and passes Stage-7 `text_preserve` (ratio ≈ 1.0) + Stage-9 assembly. This removes the ~3h/chapter local-7B prose-authoring cost on a full scan conversion. **TABLE and MATH are UNAFFECTED** (still generate) — their reconstruction is genuinely generative (grid/MathML structure the deterministic fallback cannot recover), whereas prose HTML is a thin wrapper around verbatim text. **Consumption map (why this is safe):** the Ed4All-consumed artifacts (`{stem}_accessible.html` + `_synthesized.json`) are rendered by `lib/semantik/cascade_ir.py::_block_html_for_kind` from `region_provenance[].raw_text` (feature-block text, explicitly "never post-model HTML"), NOT from Stage-6 candidates — prose candidates only feed the INTERNAL Stage-9 assembled doc → theta/WCAG/exit signals, which a verbatim `emit_fallback` fragment satisfies identically. Does NOT touch `runtime_mode` — a real run stays `runtime_mode='real'` (the R4 mock-trap is not tripped). Falsey / unset / garbage → off (parse-with-fallback). Gate only — deterministic skip, selects no provider/model, no separate `docs/LICENSING.md` row. |
| `SEMANTIK_SPECIALIST_CONCURRENCY` | `8` (per-region path) / used as the explicit override on the batched path | Thread-pool `max_workers` for concurrent endpoint POSTs (`endpoint_runtime.py::resolve_specialist_concurrency`). On the legacy per-region path it bounds `generate_batch`'s region fan-out (default 8). On the BATCHED endpoint lane (`SEMANTIK_SPECIALIST_BATCH` on, the default) it is honoured ONLY when explicitly set — otherwise the batched lane uses a LOW default of **2** (`runner.py::resolve_batched_concurrency`), because each batched POST already packs ~12 regions so a high fan-out would re-create the rate-limit pressure batching removes. Parse-with-fallback: non-int / non-positive / garbage → `8`. No provider/model selection — no licensing row. |
| `SEMANTIK_SPECIALIST_BATCH` | `1` (ON for the endpoint lane) | **Multi-region BATCHED Stage-6 endpoint gate** — defeats the hosted seat's ~40-requests/MINUTE rate cap (`runner.py::resolve_batch_mode`). Default **ON for the endpoint lane**: instead of one POST per region per candidate (~197×K POSTs that 429 on ~every call), the active regions are packed into a few large POSTs via `OpenAICompatibleRuntime.generate_multi` (one POST per batch — the limit is requests, not tokens). Each region travels in a delimited `<<<DART_REGION id="rN">>> … <<<DART_REGION_END id="rN">>>` block with its grounding payload verbatim; the response is split back per-region (a missing/malformed block → the same `None` fail-soft sentinel as `generate_batch`). The batched lane also caps candidate-K to **≤2** (see `SEMANTIK_SPECIALIST_BATCH_K`) and uses concurrency **2** by default, so a 16-page slice fires ~34 POSTs. Default-ON parse semantics: explicit falsey (`0`/`false`/`no`/`off`) → the EXACT legacy per-region `generate_batch` path (byte-stable); unset / truthy / garbage → on. Only consulted on the endpoint lane (Phase 2); the local Phase-1 path is unaffected (it always batches by adapter). A runtime lacking `generate_multi` falls back to the per-region path. No provider/model selection — no licensing row. |
| `SEMANTIK_SPECIALIST_BATCH_REGIONS` | `12` | Maximum regions packed into ONE batched endpoint POST (`endpoint_runtime.py::resolve_specialist_batch_regions`, consumed by `runner.py::_pack_batches`). A new batch is also started early when the running batch's summed per-region output budget would exceed the output-token cap, so big-output tables get smaller batches automatically. Larger values cut total POSTs (the rate-limit win) at the cost of a bigger per-call prompt. Parse-with-fallback: non-int / non-positive / garbage → `12`. No-op when `SEMANTIK_SPECIALIST_BATCH` is off. No provider/model selection — no licensing row. |
| `SEMANTIK_SPECIALIST_BATCH_K` | unset (→ cap at 2) | Candidate-K cap on the BATCHED endpoint lane (`runner.py::resolve_batched_endpoint_k`). The config prose default is K=4; running 4 candidate slots multiplies POSTs by 4. Unset → the caller's K is clamped down to **2**; a positive int pins the cap explicitly. Never raises K above what the caller requested. Parse-with-fallback: garbage / non-positive → the default-2 cap. No-op when `SEMANTIK_SPECIALIST_BATCH` is off (full K is used on the per-region path). No provider/model selection — no licensing row. |
| `SEMANTIK_SPECIALIST_TIMEOUT_SECONDS` | `120.0` | Per-request HTTP timeout (float seconds) for endpoint specialist/reviewer POSTs (`endpoint_runtime.py`, ~L135). Non-finite / non-positive / garbage → `120.0`. No licensing row. |
| `SEMANTIK_SPECIALIST_MAX_RETRIES` | `2` | Bounded **per-region retry count** on TRANSIENT endpoint errors only — ReadTimeout / ConnectionError / 5xx / 429 (`endpoint_runtime.py::resolve_specialist_max_retries`; the retry loop is `OpenAICompatibleRuntime._one_completion_with_retry`, used by `generate_batch`). The number of EXTRA attempts after the first, with a short linear back-off (attempt N → 0.5×N s). PERMANENT errors (missing key, 400/401/403, malformed response) are NEVER retried — re-asking a bad key/request just burns the timeout budget. Paired with the Stage-6 Phase-2 per-region **fail-soft** path (`runner.py`): a region whose endpoint call still fails after retries degrades alone — hybrid/`SEMANTIK_SPECIALIST_REFINE` mode keeps that region's Phase-1 LOCAL draft, pure-endpoint mode emits a degraded skip candidate stamped `endpoint_degraded` — instead of one item's timeout aborting the whole document; only a TOTAL failure (every active region fails) still fails loud. Parse-with-fallback: garbage / negative → `2`; `0` is honoured (single attempt, no retry). No provider/model selection → no licensing row. |
| `SEMANTIK_SPECIALIST_DISABLE_THINKING` | unset (off) | Suppresses chain-of-thought on a REASONING endpoint model (`endpoint_runtime.py::resolve_disable_thinking`; injected in `OpenAICompatibleRuntime._one_completion`). Default OFF → byte-identical body for the non-reasoning seats (no extra field). Truthy (`1`/`true`/`yes`/`on`, case-insensitive) → the endpoint request body carries `chat_template_kwargs={"thinking": False}` so a reasoning model (e.g. `deepseek-ai/deepseek-v4-pro`) returns the structured answer directly instead of spending the `max_tokens` budget on a `<think>` block; harmlessly ignored by models that do not read the field. Falsey / unset / garbage → off (parse-with-fallback). A request parameter only — selects no provider/model → no `docs/LICENSING.md` row. |
| `SEMANTIK_DROP_FRONTMATTER_ZONE` | `1` (on) | Deterministic **page-density front-matter-zone** detector gate (`lib/semantik/toc_frontmatter_detector.py::_zone_flag_enabled`, ~L73/446). Default ON → a page-density pass detects dense chapter-heading clusters on early pages and drops the front-matter zone (catches the preface-summary contamination defect). Default-on parse semantics: explicit falsey (`0`/`false`/`no`/`off`) → off; unset / truthy / garbage → on. No provider/model — no licensing row. |
| `SEMANTIK_DROP_FRONTMATTER_TOC` | `1` (on) | Deterministic **phantom-TOC** front-matter detector gate (`lib/semantik/toc_frontmatter_detector.py::_flag_enabled`, ~L66/170). Default ON → the phantom-TOC + chapter-index-cluster + boilerplate detection runs inside the front-matter zone. Default-on parse semantics identical to `SEMANTIK_DROP_FRONTMATTER_ZONE` (explicit falsey → off; else on). No licensing row. |
| `SEMANTIK_TABLE_STRUCTURAL_CONFIRM` | `1` (on) | **Load-bearing structural-confidence table confirm** (`SemantiK/dart_semantic/council/cross_reranker.py::resolve_table_structural_confirm` / `_structurally_confident_table` / `_table_confirmed`). Default ON → a pdfplumber table that is STRUCTURALLY confident (a real grid: ≥2 rows AND ≥2 cols, OR bordered/ruling-line evidence with ≥2 rows) is confirmed onto the table track REGARDLESS of Structure's `table_region` BERT head — the deterministic geometric detector is load-bearing and the BERT becomes a SECONDARY signal that can only ADD confirmations (a structurally-weak candidate it rates ≥0.5) and can no longer DROP a structurally-strong one. Fixes the 0-tables regression (the head fires <0.5 on every real pdfplumber table → all dropped). The structural guard REJECTS page-layout false-positives (a 1×N / borderless single-cell "table"). Default-on parse semantics (explicit falsey `0`/`false`/`no`/`off` → the legacy BERT-gated-only path; unset / truthy / garbage → on) mirror `deterministic_structure.resolve_structure_clean_mode`. No provider/model — no licensing row. |
| `SEMANTIK_STRUCTURE_CLEAN` | `1` (on) | **Load-bearing deterministic structure-clean pass** (`SemantiK/dart_semantic/qwen_specialists/deterministic_structure.py::resolve_structure_clean_mode`, ~L77; consumed in `cascade.py` ~L766 BEFORE the off-by-default Stage-5d large-model reviewer + the per-kind cap, so BOTH the assembled HTML and `region_provenance` derive from the corrected region list). Default ON → a CPU-only, model-free pass re-tags leading front-matter/TOC heading regions to `metadata_drop` (incl. the fused-TOC drop — a heading carrying ≥2 "N.M Title" tokens), demotes textbook pedagogical-label headings ("EXAMPLE N"/"Solution"/"Step N", with a `css_class` semantic hint), and demotes bullet/list-item headings the council mis-typed as headings. Reuses the reviewer's admission + token-conservation invariant harness and **fails closed** (reverts the whole pass to the input region list on any conservation failure); the FB partition is immutable + text is preserved verbatim. Default-on parse semantics: explicit falsey (`0`/`false`/`no`/`off`) → byte-identical pass-through; unset / truthy / garbage → on (mirrors `toc_frontmatter_detector._flag_enabled`). No provider/model — no licensing row. |
| `SEMANTIK_PROMOTE_SECTION_HEADINGS` | unset (off) | **Section-heading PROMOTION** gate (defect 3(c)) — `SemantiK/dart_semantic/qwen_specialists/deterministic_structure.py::resolve_promote_section_headings_mode`, ~L84 (truthy-set parse-with-fallback: only `1`/`true`/`yes`/`on`, case-insensitive, enables; unset / falsey / garbage → off). Default OFF because a paragraph→heading promotion is behaviour-changing (it re-shapes the chapter structure), so it follows the house flag pattern rather than the always-on `SEMANTIK_STRUCTURE_CLEAN` bug-fix path. Gates TWO sites, both no-ops when off: **(1)** clean_structure **sub-pass E** (`_detect_section_promotions`, ~L1097) — a STANDALONE paragraph region whose WHOLE text is a clean `"N.M Title-Case"` section heading (matched by `_STANDALONE_SECTION_RE` only — no apparatus-opener arm) that the council mis-typed as body prose is re-tagged back to an `<h2>` via `_promote_to_heading`, re-enabling the `cascade_ir` section-number chapter derivation (anything already dropped/demoted by earlier sub-passes is excluded — a dropped region is furniture, not a section title); off → `section_promote` is the empty set, so the corrected region list is byte-identical. **(2)** lane B's **post-Stage-5e fused-title promotion hook** (`cascade.py` ~L1554 → `deterministic_structure.promote_fused_title_children`), gated on BOTH this flag AND `SEMANTIK_SPLIT_FUSED_SECTION_TITLES` — it promotes the title child (`child_index==0`) that the Stage-5e fused-title SPLIT just carved off (sub-pass E runs at Stage-5d-det, BEFORE the split, so it never sees that child); split-on/promote-off leaves the title child a `<p>` (still an improvement; promotable later by rerender), promote-on/split-off is byte-identical (no fused-title child exists → natural no-op). Both sites are re-tags (kind change only, partition-immutable) riding the clean_structure token-conservation invariant with **fail-closed whole-revert** — the promotion never mints/drops FBs or sourceIds, so unlike the fused-title SPLIT it has NO sourceId blast radius (the id SET is unchanged). Falsey / unset / garbage → off (parse-with-fallback). Gate only — deterministic re-tag, selects no provider/model → no separate `docs/LICENSING.md` row. |
| `SEMANTIK_READING_ORDER_FIX` | `1` (on) | **SUPERSEDED by `SEMANTIK_REGION_ORDER` (ITEM5; legacy alias honored).** Stage-5 **region reading-order re-sort** gate — now the `fb` MODE of the single ordering owner. `resolve_reading_order_fix()` (`SemantiK/dart_semantic/structure_graph.py`) is KEPT as a SHIM returning `resolve_region_order_mode() != "off"` (load-bearing for the ITEM1 `SEMANTIK_UNIT_REGROUP` guard + gold-absorb mutual exclusion); the explicit-falsey `SEMANTIK_READING_ORDER_FIX=0` revert lever still resolves `off` via the region-order resolver's alias (one-shot deprecation log), so its behavior is unchanged. Default ON → a final **stable** sort of the region list by `min(feature_block_indices)` at the exit of `build_structure_graph` (after the coverage invariant, before return) restores document reading order, so the assembled HTML + `region_provenance` are in reading order (fixes a live textbook-corpus defect where `build_structure_graph` appended regions in PASS order — Pass-2 all headings, Pass-3 tables/math, Pass-5 paragraphs, ... — never re-sorting, leaving the flat Region list SEGREGATED by kind: EXAMPLE labels at byte-4% and their bodies at byte-40%). The just-verified coverage invariant guarantees disjoint FB sets, so the sort key is a total order with no ties → deterministic, and a permutation `assert` fails closed on a key bug. The WIRE contract is preserved (`first_raw_block_index` / `data-dart-block-id` are FB-derived → multiset byte-stable under the reorder; only the LIST ORDER + `region_index` integers + the reviewer `block_id` move). Default-on parse-with-fallback semantics (explicit falsey `0`/`false`/`no`/`off` → the legacy segregated pass-order list, the revert lever; unset / blank / truthy / garbage → on) mirror `deterministic_structure.resolve_structure_clean_mode`. No provider/model — no `docs/LICENSING.md` row. |
| `SEMANTIK_REGION_ORDER` | `fb` (Phase-B shadow default) | **ITEM5 single Stage-5 region-ORDER owner** — a three-valued flag `geom` \| `fb` \| `off` (`SemantiK/dart_semantic/region_order.py::resolve_region_order_mode`; `apply_region_order` runs ONCE at the `build_structure_graph` exit, after the coverage invariant + the ITEM2 `page_bboxes` stamp). Subsumes `SEMANTIK_READING_ORDER_FIX` as its `fb` mode. **Modes:** `off` → the legacy segregated PASS-order list (byte-identical to the `SEMANTIK_READING_ORDER_FIX=0` revert lever — the pass never runs); `fb` → the min-FB stable sort restoring document reading order (byte-identical to the historic default-ON reading-order fix), with the geometric divergence tripwire MEASURED in SHADOW (order NOT changed); `geom` → the geometry-grounded per-page order `(page → column via reading_order.column_ids_for_bboxes → y0 → x0 → min_fb)` derived from the ITEM2 Region `page_bboxes` (FB-derived fallback), with figure/table/math floats excluded from seeding, a full-width straddler pinned to column 0, bbox-less regions re-inserted after their FB-order predecessor, and a per-page divergence CEILING (`_GEOM_ORDER_PAGE_CEILING`, initial 0.45) that falls the page back to `fb` order when the geometry is pathological (garbled OCR bboxes). **Divergence tripwire:** normalized inversion distance (Kendall-tau / C(n,2)) between FB-index order and geometric order, per page + doc-aggregated (region-count-weighted), computed whenever mode != `off`; WARN floors (page > 0.10, doc > 0.02 initial) fire a `logger.warning` in `geom` mode. **Audit:** an additive operator/tooling-facing `result["geom_order"]` dict (`{mode, applied, doc_divergence, pages:[{page, n_regions, n_columns, divergence, fallback}], bboxless_regions, warnings}`) forwarded across the cross-venv bridge (`run_cascade_json._resolve_geom_order`), parallel to `block_resegment`; `None` in `off` mode. **Legacy alias:** when `SEMANTIK_REGION_ORDER` is unset/blank AND `SEMANTIK_READING_ORDER_FIX` is explicitly falsey → resolves `off` (one-shot deprecation log); an explicit mode value wins; unknown/garbage → the default. **Order-only — the WIRE contract + sourceId multiset are UNCHANGED** (`data-dart-block-id` / `first_raw_block_index` are FB-derived → byte-stable under any permutation; only LIST ORDER + `region_index` integers move — same posture as the READING_ORDER_FIX reorder, strictly safer than merge/split). **NO LLM call site → NO DecisionCapture** (a deterministic pass). Phase-B default `fb` is the byte-identical SHADOW; the flip to `geom` is Phase C (calibration-gated, owner sign-off). **Divergence tripwire calibration constants carry `# TODO(calibration Phase C)` markers.** Parse-with-fallback. No provider/model — no `docs/LICENSING.md` row. |
| `SEMANTIK_CONTAINMENT` | `1` (default ON — deviation) | **ITEM4 materialized-containment-tree gate** (`SemantiK/dart_semantic/containment.py::resolve_containment_mode`). A single deterministic, CPU-only pass (`build_containment_tree`) derives the document's containment structure — an explicit parent/child **forest over the flat Region list** (heading→section / unit-anchor→body / figure-table→caption ownership, plus an intra-region `payload['dl_pairs']` stamp) — so the Stage-9 assembler can WALK that tree instead of re-deriving containment three ways at emit time. The tree is a DERIVED sidecar (a frozen `ContainmentTree`, rebuilt at every `run_pass_9a` entry so it is immune to later region-list mutations — Stage-5e MERGE/SPLIT/REGROUP, the ITEM3 MOVE, the second-pass re-assembly — by construction, never a stored `parent_region_id` field that would go stale). **NO LLM call site** is added (a pure function → no `DecisionCapture` obligation; flagged loudly per the campaign — no exceptions in ITEM4). **Phase 1 (metadata-only, landed):** the tree is built + stashed in `pass_9a` `sub_task_log["containment"]`, and `cascade._build_region_provenance` exports it as an ADDITIVE optional per-region `containment: {parent_region_index, edge_kind}` key (absent when the flag is off — the adapter ignores unknown keys, the `review`-block precedent); render bytes are UNCHANGED (proven byte-identical A/B: HTML + whole `region_provenance` identical after stripping the additive key). **Default ON** (default-on falsey-set parse mirroring `SEMANTIK_READING_ORDER_FIX`: explicit `0`/`false`/`no`/`off` → the byte-identical legacy path — no build, no stash, no provenance key; unset / blank / truthy / garbage → on). **Regroup interplay:** the builder needs reading order; when `SEMANTIK_READING_ORDER_FIX` is OFF it degrades to the trivial all-roots forest (a walk == flat concat), so legacy behavior is preserved without a special case. **TREE-COVER invariant:** every region appears exactly once in `roots ∪ ⋃children`; the pre-order emission is the IDENTITY permutation of the region list (any backward candidate edge — the caption-ABOVE case — is dropped to metadata-only); a hard cover/order check **fails closed** to the trivial forest (never a broken render). Unit arm reuses the `pedagogical_units` boundary SoT verbatim (subsuming the narrow-table absorb's FB-adjacency arm unconditionally). Falsey / unset / garbage → on (parse-with-fallback). Gate only — deterministic containment derivation, selects no provider/model → no separate `docs/LICENSING.md` row. |
| `SEMANTIK_BERT_AUTHORITATIVE` | unset (off) | **Council-head precedence flip + band-aid retirement (Phase 5)** master gate (`SemantiK/dart_semantic/structure_graph.py::resolve_bert_authoritative`). Default OFF → byte-identical to today (the council `structural_role` head is a non-authoritative recommendation; the Stage-5d LLM reviewer re-types unconditionally; the deterministic band-aids run). Truthy (`1`/`true`/`yes`/`on`, case-insensitive) flips the contract: **(1) precedence flip** — a gated post-pass in `build_structure_graph` (after the coverage invariant, before the reading-order sort) maps each prose region's representative-FB `structural_role` argmax to `Region.kind` via `_ROLE_TO_REGION_KIND` (the 9-role taxonomy; table/math/figure passthrough + headings excluded — heading membership/level stay deterministic), stamps `payload['role_confidence']` from the head's top-1, and stamps `payload['semantic_class']` from the AXIS-2 `pedagogical_role` head (routing pedagogical kinds through the gold `_wrap_semantic_class` wrapper); **(2) reviewer demotion** — `reviewer._content_block_dispatch_gate` delegates to `_authoritative_dispatch_gate` (dispatch IFF `structural_role` top-1 < `_AUTHORITATIVE_DISPATCH_TAU=0.5` OR top1−top2 margin < `_AUTHORITATIVE_DISPATCH_DELTA=0.15` OR an `is_heading`↔`structural_role` cross-head heading conflict — the tau/delta constants are now calibratable offline against the ITEM6 persisted `region_provenance[].role_top_k` distributions via `SemantiK/scripts/calibrate_dispatch_thresholds.py` (CPU-only, no LLM/GPU), pending an owner-reviewed measured run; the constants stay 0.5 / 0.15), the unconditional `_UNCONDITIONAL_DISPATCH_KINDS` code_block/table blanket is bypassed, and headings pass through the same gate (no longer always-in-scope); **(3) band-aid retirement** — `deterministic_structure.clean_structure` sub-pass B (pedagogical-label demotion + `css_class` stamping) is skipped and `assembler/fallbacks._css_class_attr` returns `""` (the `<p class="…">` band-aid is retired; the authoritative head emits the pedagogical kind directly). KEPT in BOTH modes: clean_structure sub-pass A (front-matter/phantom-TOC/OCR drop), the per-kind cap + token-conservation/R-PART harness, Stage-5e resegment, and the heading-tree LEVEL normalizer. Falsey / unset / garbage → off (parse-with-fallback). Gate only — selects no provider/model (the reviewer rides the already-licensed specialist seat), no separate `docs/LICENSING.md` row. |
| `SEMANTIK_CONTENT_TYPE_GUARD` | unset (off) | **Deterministic content-type sanity guard** — a domain-agnostic positive-evidence veto AND-ed into the Stage-5 Pass-6 `code_block` / `definition_list` predicates (`SemantiK/dart_semantic/structure_graph.py::resolve_content_type_guard` + `_looks_like_code` / `_looks_like_definition_list`, consumed by the nested `_is_code` / `_is_definition` in `build_structure_graph`). Default OFF → **byte-identical** to today: Pass-6 typing trusts the council BERT + the confidence floor alone, so a wiki numeric TABLE row / table header / GPO printer furniture / bare label can still ship as `code_block` and a mid-sentence regulatory PROSE fragment can still ship as `definition_list`. Truthy (`1`/`true`/`yes`/`on`, case-insensitive) → a region is kept `code_block` only if the council said so AND its text ACTUALLY looks like code (positive signals: STRONG code operators `== != <= >= => -> := ++ -- && || :: /* */ #include </tag /> ){` brace-at-EOL / leading `//`; a lowercase line-initial programming keyword `def`/`import`/`for`/`return`/…; a leading-assignment `ident = expr`; or ≥2 lines with ≥2-space indentation — bare `()` `.` `,` `;` `%` `|` are deliberately NOT counted so legal citations `See section 3(a)(2) …` and wiki pipe-rows never trip it), and kept `definition_list` only if it has a real term–definition shape (a short leading term + `:`/spaced en/em dash + a definition, or is a bare short term / capitalized definition-body sentence — CONSERVATIVE, only vetoing a clear running-prose fragment with no delimiter). A vetoed region loses its Pass-6 claim and falls through to the unclaimed-FB **paragraph fallback** (Pass ordering List→Paragraph→Code), so **text is preserved** (no FB lost; the FB partition + coverage invariant are untouched). **ITEM6 runner-up fallback:** a vetoed FB now consults the council's runner-up `structural_role` label (floor + shape-predicate gated, D3a table — `blockquote`/`code_block`/`definition_list`/`list`) via `_guard_runner_up_region` before degrading to the paragraph fallback; `heading` is never minted, and the flag-off byte-identical guarantee is unchanged (`guard_vetoed` only populates under this flag). Default OFF (not the always-on `SEMANTIK_STRUCTURE_CLEAN` / `SEMANTIK_TABLE_STRUCTURAL_CONFIRM` bug-fix path) because a positive-evidence code check CAN reject a genuinely-minimal code snippet (a keyword-free / operator-free one-liner) → to stay strictly byte-identical on genuine code / def-list corpora the guard is opt-in behind the house flag pattern rather than default-ON. Falsey / unset / garbage → off (parse-with-fallback), mirroring `resolve_bert_authoritative`. Gate only — deterministic region re-typing, selects no provider/model → no `docs/LICENSING.md` row. |
| `SEMANTIK_COUNCIL_BATCH_SIZE` | `32` | **VRAM-bounded council-backbone batch size** (`SemantiK/dart_semantic/council/structure.py::resolve_council_batch_size`, ~L124; `_DEFAULT_COUNCIL_BATCH_SIZE = 32`). The shared ModernBERT backbone forward is tokenized + run in slices of this size with intermediate tensors freed between slices, so peak VRAM is bounded by the slice rather than the full document's block count (an 8 GB OOM mitigation, complementing the by-adapter Qwen batching). Parse-with-fallback: non-int / non-positive / garbage → `32`; the resolved value is floored at 1 (`max(1, int(...))`). No provider/model selection — no licensing row. |
| `SEMANTIK_WORKER_MAX_TASKS` | `50` | **Pool-worker recycle interval** (tasks-per-worker) for the offline data-build worker pool (`SemantiK/dart_semantic/worker_pool.py::resolve_worker_max_tasks`, ~L28; consumed by `run_in_pool`, ~L137). `run_in_pool` processes items in GENERATIONS — a fresh `ProcessPoolExecutor` per `max_tasks × workers` items, torn down before the next — so each worker handles ~this many tasks then dies, handing back its native glibc malloc arenas (the tracebackless RSS ratchet that per-page pypdfium2/pdfplumber render+extract churn leaves fragmented and never returns to the OS — ~25 MB/pair on render-heavy docs), its Chromium child (the worker `_teardown` kills the Playwright node+Chromium child tree then `os._exit`s — graceful Playwright stop deadlocks pool shutdown), and its per-process `lru_cache`. This is the PRIMARY fix for the `data/build_structure_data.py --aligner global` OOM (a leak that is glibc-arena fragmentation, not Python object growth, so tracemalloc cannot see it). **NB:** generational pools are used INSTEAD OF `ProcessPoolExecutor(max_tasks_per_child=...)` because that kwarg is forbidden with the default `fork` start method, and on this Python build the `forkserver`/`spawn` variants stall after the first worker generation (replacement workers never pick up remaining tasks — reproduced with a no-op initializer, so not Playwright-specific). Complemented by `MALLOC_ARENA_MAX=2` (a std glibc env — NOT a SemantiK flag — `setdefault` in `build_structure_data.main()` so spawned workers inherit it; caps per-process arenas to attack the fragmentation directly, including the synchronous `--workers 1` in-process path that recycling cannot cover) + a best-effort `libc malloc_trim(0)` at the end of `process_pair_global` + a per-pair `data.structure_align._sim_cached.cache_clear()` (the unbounded `@lru_cache(maxsize=None)` on the aligner `sim()` grew ~750 B/entry with ~0 cross-pair reuse; cleared after each pair, preserving the within-pair DP memoization). Parse-with-fallback: blank / non-int / non-positive / garbage → `50` (mirrors `SEMANTIK_BLOCK_REVIEW_WINDOW`). Selects no provider/model — no `docs/LICENSING.md` row. |
| `SEMANTIK_DETECT_FIGURES` | unset (off) | **Figure-detection path gate** (Part F; `SemantiK/dart_semantic/cascade.py::resolve_detect_figures`, extract-side mirror `extract_shared._detect_figures_enabled`). Default OFF → byte-stable: no per-page `images` list is extracted, no `ImageCandidate` forms, no synthetic image FeatureBlock enters the stream, no figure region is emitted (every existing snapshot is unchanged). When truthy (`1`/`true`/`yes`/`on`), the cascade extracts PDF IMAGE page-objects via pypdfium2 (`_pypdfium2_page_images`, Y-flipped to top-left), filters spacer/rule images, synthesizes one image FeatureBlock per candidate INTERLEAVED into the FB stream in reading order, routes each `ImageCandidate` DETERMINISTICALLY to the figure track (Structure's `is_image_block` head is a secondary confirmation signal only), forms a load-bearing figure Region (Pass-3a in `build_structure_graph`), renders the bbox to a PNG (Stage 5c, per-region fail-soft), writes a deterministic sidecar `{stem}_figures/fig-{first_fb}.png` + stamps `image_src` (Stage F), and fills the previously-empty `<img src="">` in both emitters. The figures flag is part of the extract disk-cache key so a flip invalidates the cache. Falsey / unset / garbage → off (parse-with-fallback). No provider/model selection → no `docs/LICENSING.md` row (the optional SmolVLM2 captioner rides the existing figure-captioner path; the hosted large-model seat is unaffected). |
| `SEMANTIK_FIGURE_CAPTION` | `auto` | Tri-state figure-captioning sub-gate (`cascade.py::resolve_figure_caption_mode` / `_figure_captioning_active`). `auto` (default; unset / `auto` / garbage) → caption via SmolVLM2 (Stage 6b) only when a CUDA device is present; otherwise DEFER (skip the Stage-6b call so the figure ships the honest type-level `"Figure."` alt via the assembler's `guard_figure_alt` / `TYPE_LEVEL_ALT`). Explicit truthy (`1`/`true`/`yes`/`on`) forces captioning on; explicit falsey (`0`/`false`/`no`/`off`) forces deferral. Only consulted when `SEMANTIK_DETECT_FIGURES` is on; the legacy `image_block_demoted` figure path always captions. Selects no provider/model → no licensing row (the captioner model is the existing SmolVLM2 figure-captioner). |
| `SEMANTIK_OCR_RENDER_SCALE` | `2.0` | **Tesseract OCR raster-DPI scale** (`SemantiK/dart_semantic/extract_shared.py::resolve_ocr_render_scale`; consumed at call time by `_tesseract_page_blocks`, which passes it to `_pypdfium2_render_page_to_image(scale=…)`). A pypdfium2 page renders at 72 DPI × scale, so the default `2.0` ≈ 144 DPI on a 612×792pt page — adequate for typical body text but MARGINAL for ~10pt print on a rasterized / image-only scan (which OCRs best at ~300 DPI, scale ≈ `4.0`). Raise it for such a corpus without a code change; higher scales cost render time + RAM. Read at CALL time (not import), so tests / Docker can flip it without a re-import (mirrors `_detect_figures_enabled` / `SEMANTIK_HOME`). Parse-with-fallback: unset / blank / non-float / NaN / ±Inf / non-positive (`<= 0` would raster an empty image) → `2.0`; a valid positive float → that scale. Byte-identical to the historic hardcoded `scale=2.0` when unset. **Cache caveat:** the render scale is NOT part of the extract disk-cache key (`EXTRACT_CACHE_VERSION` / the `fig_key` salt in `extract_shared.py`), so flipping this env var does NOT invalidate a prior OCR extraction — clear the extract cache (default `<SemantiK>/data/extract_cache`, relocatable via `SEMANTIK_DATA_DIR` / `SEMANTIK_HOME`) or use fresh inputs after a change, else a stale-scale cached extraction is served. Selects no provider/model — deterministic render knob, no `docs/LICENSING.md` row. |
| `SEMANTIK_TESSERACT_CONFIG` | `""` (unset) | **Tesseract CLI config fragment** threaded into `pytesseract.image_to_data(config=…)` (`SemantiK/dart_semantic/extract_shared.py::resolve_tesseract_config`; consumed at call time by `_tesseract_page_blocks`). Default `""` → **byte-identical** to the historic call shape: the `config=` kwarg is OMITTED entirely (so the existing `test_ocr_render_scale.py` mock stays valid). A non-empty value (e.g. `--oem 1 -c preserve_interword_spaces=1`) is passed verbatim (stripped; embedded newlines collapsed to spaces — it is one CLI fragment). Composes with `SEMANTIK_TESSERACT_USER_WORDS` (both may be set). Read at CALL time (mirrors `resolve_ocr_render_scale`) so tests / Docker can flip it without a re-import. Parse-with-fallback: unset / blank → `""` (freeform string, so it is trivially total — a run never crashes on a malformed flag). **Cache caveat:** like `SEMANTIK_OCR_RENDER_SCALE`, this flag is NOT part of the extract disk-cache key (`EXTRACT_CACHE_VERSION` / `fig_key`), so flipping it does NOT invalidate a prior OCR extraction — clear the extract cache (default `<SemantiK>/data/extract_cache`, relocatable via `SEMANTIK_DATA_DIR` / `SEMANTIK_HOME`) or use fresh inputs; `scripts/ocr_recall_ab.py` isolates per-config caches itself. Deliberately kept out of the cache key (matches the render-scale precedent, avoids invalidating every existing cache). Selects no provider/model — a request parameter, no `docs/LICENSING.md` row. |
| `SEMANTIK_TESSERACT_USER_WORDS` | `None` (unset) | **Tesseract `--user-words` domain-lexicon path** (`SemantiK/dart_semantic/extract_shared.py::resolve_tesseract_user_words`; consumed at call time by `_tesseract_page_blocks`). Default unset → `None`, byte-identical (no `--user-words` appended). A set path is `expanduser`ed and, when it `is_file()`, appended to the effective config as `--user-words <shlex.quoted path>` (POSIX quoting survives a path with spaces — pytesseract `shlex.split`s the config); composes with `SEMANTIK_TESSERACT_CONFIG`. A set-but-missing file logs a **de-duplicated** warning (once per distinct path, so a 400-page doc does not log 400 times) and degrades to `None` — never raises (parse-with-fallback). A generic domain lexicon ships at `SemantiK/data/config/algebra_user_words.txt` (~100 generic algebra terms, one lowercase word per line — NOT course data); code never defaults to it, the operator points the flag at it explicitly (relocatable under `SEMANTIK_CONFIG_DIR`). Read at CALL time. **Cache caveat + kept-out-of-key** identical to `SEMANTIK_TESSERACT_CONFIG` above. **Honest caveat:** `--user-words` is primarily a LEGACY-engine feature — under the default LSTM engine (`--oem 1`) its effect is weak-to-none on most 4.x/5.x traineddata; treat it as a measured experiment (`scripts/ocr_recall_ab.py` is the arbiter), not a promised win. Selects no provider/model — a request parameter, no `docs/LICENSING.md` row. |
| `SEMANTIK_VLM_EXTRACT` | unset (off) | **P0 VLM extraction-source master gate** (plan `plans/scan-conversion-improvements-2026-07.md` Phase 4; `SemantiK/dart_semantic/extract_shared.py::resolve_vlm_extract_mode`). Default OFF → **byte-identical**: no VLM source is fused, no `vlm_hint` is minted or carried, `RawBlock`/`FeatureBlock.vlm_hint` is `None` everywhere, and no `region_provenance` gains a `vlm_*` key. Truthy (`1`/`true`/`yes`/`on`, case-insensitive) → the Qwen2.5-VL per-page markdown transcription is fused as a FOURTH extraction source (VLM text on Tesseract bbox via the `data/structure_align` DP aligner) and the P2 hint channel is live. **Cache posture (fig_key / `fuse_key` shape-changing-flag precedent, IN the key — NOT the render-scale kept-out-of-key one):** the flag SALTS the extract disk-cache key (`extract_shared_cached`, **asymmetric append-only** `|vlm1` when on), so flipping it ON NEVER serves a stale non-VLM extraction; the flag-OFF key is **byte-identical to the historic `v{VER}|{fig_key}|...` key** (no `vlm0` salt — every existing extract cache survives when off). Read at CALL time. Falsey / unset / garbage → off (parse-with-fallback). Gate only — no provider/model selection, no `docs/LICENSING.md` row (the seat selectors below carry it). |
| `SEMANTIK_VLM_FUSION` | unset (off) | **P1 deterministic VLM↔Tesseract fusion gate** (plan `plans/scan-conversion-improvements-2026-07.md` Phase 4 P1; `SemantiK/dart_semantic/vlm_fusion.py::resolve_vlm_fusion_mode`, seam `extract_shared.py::_apply_vlm_fusion` — after the tesseract + P0 `page['vlm']` branches, BEFORE `_merge_page`). **Separate from `SEMANTIK_VLM_EXTRACT` (P0)** so the VLM source can run measure-only (source cached, merged output unchanged); this flag is a **NO-OP without `SEMANTIK_VLM_EXTRACT`** (there is no `page['vlm']` to fuse — mirrors the `SEMANTIK_SEMANTIC_CLASS` requires-`SEMANTIK_BLOCK_REVIEW` dual-gate). Default OFF → **byte-identical** (no fusion, no `fusion`/`fusion_sim`/`vlm_md`/`vlm_coverage` block keys, no `page['vlm_divergence']`/`vlm_fusion_stats`, zero LaTeX anywhere). Truthy (`1`/`true`/`yes`/`on`, case-insensitive) → the P0 VLM markdown lines are aligned to the tesseract line blocks by REUSING the `data/structure_align` DP aligner's public API (`align_blocks` + `sim` — the same 5-move MATCH/SPLIT/MERGE/GOLD_GAP/PDF_GAP lossless alignment; a VLM logical line MERGEs `k` wrapped OCR print-lines, which a greedy matcher cannot express), then `page['tesseract']['text_blocks']` is rewritten IN PLACE: **aligned** → VLM text on the tesseract bbox union (`fusion='vlm+tesseract'` + `fusion_sim=<DP confidence>`; bbox / font / confidence / 20-dim-layout geometry all survive for the council); **VLM-only** → a flagged low-confidence insert (bbox interpolated in tesseract IMAGE-PIXEL space between aligned neighbors, `confidence=0.30`, `fusion='vlm-only-flagged'` — the anti-hallucination tripwire is STRUCTURAL); **tesseract-only** → kept byte-verbatim (`fusion='tesseract-only'`, VLM-drop protection). Two deterministic math-garble mitigations (the highest-value lines: OCR `Vab = Va-Vb` vs VLM `\sqrt{ab}=\sqrt{a}\cdot\sqrt{b}` score near-zero on raw token sim): a fusion-LOCAL sim pre-normalization (LaTeX-strip + √/l/O confusable folds for SCORING ONLY — MUST NOT migrate into `structure_align.normalize_for_match` or it silently changes the training-data aligner) + a gap-pairing positional RESCUE bounded by aligned anchors on both sides. Per-page `page['vlm_divergence']` = raw gap lines / total (the tripwire) + `page['vlm_fusion_stats']`. Provenance INVARIANCE: `_merge_page` forces `provenance=['tesseract']`, so the `features.py` `prov=tesseract` council-input marker is unchanged and the `fusion` key never reaches the classifier input. **Math text-form contract (documented representation change):** the VLM's inline LaTeX is kept VERBATIM as text (`$`-delimiters preserved); only markdown STRUCTURAL syntax (`#` headings, list markers, `**bold**`) is stripped — so chunk / gold text WILL carry LaTeX where OCR had garble (flag off ⇒ zero LaTeX). P2 hint channel: the fused blocks carry `vlm_md` (RAW markdown, markers preserved) + `vlm_coverage` (`whole_block` for a single VLM line = MATCH/MERGE/insert; `prefix` for a multi-line SPLIT — the load-bearing distinction that stops a fused-title alignment from re-typing a prose block into an `<h2>`), consumed by `_attach_vlm_struct_hints` under `SEMANTIK_VLM_STRUCT_HINTS`. **Cache posture (fig_key / `vlm_key` shape-changing-flag precedent, IN the key — NOT the render-scale kept-out-of-key one):** fusion changes the merged shape, so the flag SALTS the extract disk-cache key (`extract_shared_cached`, append-only `|vlmfuse1` when on) — the flag-OFF key stays byte-identical to the current P0 key. **sourceId blast radius (honest):** fused text changes content hashes and VLM-only inserts shift raw FB indices, so a flag FLIP changes which `data-dart-block-id` / `sourceId` exist — deterministic per run (same PDF + same flags → same ids), but downstream chunk refs / `source_module_map.json` / citation deep-links from a PRIOR run must be re-derived after a flip (matches the `SEMANTIK_UNIT_REGROUP` / `SEMANTIK_SPLIT_FUSED_SECTION_TITLES` posture). **Pathological-page guard:** `n_tess * n_vlm` over a fixed `250×250` ceiling skips fusion deterministically (tesseract verbatim, `divergence=1.0`, `stats['skipped_too_large']`) — fail-open, never a wall-clock cliff. The unbounded aligner `_sim_cached` lru_cache is cleared per page (the `SEMANTIK_WORKER_MAX_TASKS` per-pair-clear precedent). Fail-soft: fusion NEVER aborts extraction (any failure degrades to the unfused tesseract blocks). Falsey / unset / garbage → off (parse-with-fallback). Gate only — deterministic fusion, selects no provider/model → no separate `docs/LICENSING.md` row (the P0 `SEMANTIK_VLM_MODEL` seat carries that). |
| `SEMANTIK_VLM_STRUCT_HINTS` | unset (off) | **P2 VLM structural-hint ATTACH + CONSUME gate** (`extract_shared.py::resolve_vlm_struct_hints_mode`; extract-side consumption mirror `structure_graph._vlm_struct_hints_enabled`). **Subordinate to `SEMANTIK_VLM_EXTRACT`** — live only when BOTH are truthy (there is no VLM markdown to mint from unless the extract source ran), so the fusion can ship text-only with hints independently revertible (mirrors the SPLIT_FUSED_SECTION_TITLES / PROMOTE_SECTION_HEADINGS dual-gate precedent). Default OFF → byte-identical (no `vlm_hint` attached; no consumption). Truthy → hints are minted onto merged blocks (`mint_vlm_hint`) and consumed as **NON-AUTHORITATIVE heading-candidate CORROBORATION only** — a candidate that has already cleared EVERY deterministic gate (structure_graph Pass-2) gets a `payload['vlm_corroborated']` audit flag + a bounded `payload['confidence']` lift + a SECONDARY `_level_hint` tiebreak (consulted only when the deterministic font/number inference is ambiguous); it NEVER mints a candidate, lowers a threshold, bypasses a gate, or vetoes a clean_structure demotion. Sub-pass E promotion is set-invariant with hints on/off (audit breadcrumb only). **De facto subordinate to `SEMANTIK_VLM_FUSION` too** — the hint is minted from the `vlm_md` / `vlm_coverage` keys that ONLY the P1 fusion stamps, so with fusion off no `vlm_md` is ever present and attachment is a byte-identical no-op even with this flag on. **Cache posture (fig_key / append-only precedent, IN the key):** `_attach_vlm_struct_hints` runs INSIDE `_merge_page`, so the `vlm_hint` payload is BAKED into the cached merged blocks — this flag therefore SALTS the extract disk-cache key on its own (`_compute_extract_cache_key`, append-only `|vlmhints1` when on) so a cache warmed with hints OFF is never served hint-less after the operator flips hints ON (the "ship fusion text-only, flip hints later" flow), and the reverse never serves hint-carrying blocks with the flag off; the flag-OFF key stays byte-identical to the fusion key (there is no `vlmhints0` salt). Falsey / unset / garbage → off (parse-with-fallback). Gate only — no `docs/LICENSING.md` row. |
| `SEMANTIK_VLM_STRIP_FURNITURE` | `1` (on) | **Document-level repeated page-furniture strip** for the VLM fusion path (`SemantiK/dart_semantic/vlm_furniture.py::resolve_strip_furniture_mode`; consumed by `extract_shared._strip_document_furniture`, run in the deferred second loop when `SEMANTIK_VLM_FUSION` is on). Defect 1 (2026-07-03 audit): the VLM emits a per-page running FOOTER (copyright / "available for free at <url>") + page-top HEADER ("Chapter N Title" +/- page number) as their OWN markdown lines; the P1 DP fusion then glues them into body prose (tesseract used to garble them so they never aligned — the VLM reads them cleanly). Detects line signatures (whitespace-normalized, **digit-runs masked** so "Chapter 9 … 803" == "… 815") recurring at the page-top / page-bottom position on >= `SEMANTIK_VLM_FURNITURE_MIN_SHARE` of the SAME document's pages (the `deterministic_structure._detect_repeated_chapter_furniture` repeat-count precedent) and drops every matching line from each page's VLM lines + any surviving clean tesseract line BEFORE fusion. **Default ON** because it is a CORRECTNESS fix, not a feature — but it is a NO-OP without `SEMANTIK_VLM_FUSION` (which is itself off by default), so the flag-off / no-fusion pipeline is byte-identical (default-on justified). Signatures are detected from BOTH sources — the VLM lines and the y-sorted tesseract blocks (Defect A follow-up: tesseract reads the footer cleanly on many pages, surviving as a standalone last-position block the DP fusion aligns the VLM footer onto) — and the union is stripped from both sides. The change to what `fuse_page` emits is covered by the `|vlmfuse4` extract-cache salt bump; this escape-hatch flag is deliberately kept OUT of the cache key (render-scale kept-out-of-key precedent) — flip it off after a warm cache and clear the extract cache. Default-on parse semantics: explicit falsey (`0`/`false`/`no`/`off`) → off; unset / truthy / garbage → on. Read at CALL time. Selects no provider/model → no `docs/LICENSING.md` row. |
| `SEMANTIK_VLM_FURNITURE_MIN_SHARE` | `0.35` | **Recurrence threshold** for `SEMANTIK_VLM_STRIP_FURNITURE` (`vlm_furniture.py::resolve_furniture_min_share`) — the minimum share of a document's pages a number-masked line signature must recur at a margin on to be treated as furniture. 0.35 catches running headers that alternate recto/verso (so appear on ~half the pages) while a varied body line never recurs verbatim at a margin across a third of the pages. Parse-with-fallback: blank / non-float / NaN / out-of-`(0, 1]` → `0.35`. No-op when the strip is disabled / fusion is off. Selects no provider/model → no `docs/LICENSING.md` row. |
| `SEMANTIK_VLM_DROP_GARBAGE_TAILS` | `1` (on) | **Conservative OCR-garbage drop for unrescued tesseract-only blocks** on the VLM fusion path (`SemantiK/dart_semantic/vlm_fusion.py::resolve_drop_garbage_tails_mode`; predicate `_looks_like_ocr_garbage`, consumed in `_reconstruct`'s tesseract-only loop). Defect (2026-07 OpenStax Algebra ch01 scan audit): on a cleanly-VLM-transcribed page an unrescued **tesseract-only** block (the DP aligner matched it to NO vlm line AND the gap-pairing rescue skipped it) is almost always OCR noise the VLM correctly omitted, yet it shipped byte-verbatim into the HTML as a `<p>` (`TRY Tiss ® ©`, `©) obi Dom -19`, `© Solution No.`, `2,791 © 2,795`). Default ON → the pass drops the UNAMBIGUOUSLY-garbage ones via a CONSERVATIVE, domain-agnostic predicate — a SHORT line (≤40 chars AND ≤6 tokens) that carries ≥1 junk glyph (`®™©|~^` backtick `¬°`, brackets/parens deliberately excluded) AND either has no plausible word (no `[A-Za-z]{3,}` run), OR opens with a junk glyph, OR is junk-symbol-dominated (≥0.20). A line with no junk glyph, a `$…$`/`\(…\)` math span, a real multi-word phrase, or a clean numeric answer row is NEVER dropped (errs toward KEEPING — a false-negative is safer than deleting real content). Every drop is tallied in `stats['garbage_tail_dropped']` (mirrors `vlm_furniture`'s `vlm_furniture_dropped`); `stats['tesseract_only']` stays the RAW ledger gap count (divergence invariant), so the tally is ADDITIVE. **Default ON** because it is a CORRECTNESS fix, not a feature — but it is a NO-OP without `SEMANTIK_VLM_FUSION` (itself off by default), so the flag-off / no-fusion pipeline is byte-identical (default-on justified). **`=0` is the operator escape hatch** (unrescued tesseract-only blocks stay byte-verbatim — and its output is byte-identical to the pre-change `vlmfuse5` behavior). **Cache posture (append-only `|gtail1` salt, IN the key):** dropping blocks changes what `fuse_page` emits (fewer tesseract-only blocks → which FBs / sourceIds exist differs), so the flag SALTS the extract disk-cache key (`_compute_extract_cache_key`) with a NEW append-only `|gtail1` gated on fusion-on AND drop-on — NOT a `vlmfuse6` bump, precisely because a `=0` run is byte-identical to `vlmfuse5` and must key back to it (its warm cache stays valid) while the default drop-on run re-extracts once; the flag-OFF (no-fusion) key stays byte-identical (no `fuse_key` → no `gtail_key`). Default-on parse semantics (explicit falsey `0`/`false`/`no`/`off` → off; unset / truthy / garbage → on) mirror `SEMANTIK_VLM_STRIP_FURNITURE`. Read at CALL time. Selects no provider/model → no `docs/LICENSING.md` row. |
| `SEMANTIK_VLM_ORDER_AUTHORITATIVE` | unset (off) | **VLM-authoritative reading-order repair for dense multi-column pages** (`SemantiK/dart_semantic/vlm_fusion.py::resolve_vlm_order_authoritative_mode`; reorder helper `_apply_vlm_order`, applied at the tail of `_reconstruct`). **NO-OP without `SEMANTIK_VLM_FUSION`** (there is no fused list to reorder). Default OFF → byte-identical to the legacy fusion path on EVERY page (the entire existing fusion snapshot is unchanged). Truthy (`1`/`true`/`yes`/`on`, case-insensitive) → on a page whose fusion `divergence` ≥ `SEMANTIK_VLM_ORDER_DIVERGENCE` (Tesseract geometry unreliable — a fractured multi-column answer grid / 3-column exercise bank) the fused blocks are re-emitted in VLM (gold) order over synthetic MONOTONIC single-column bboxes, the unmatched tesseract-only garbage tails are DROPPED, and `stats['vlm_authoritative']=True`. This makes the VLM's known-good linear order survive the downstream `_merge_page` `(y0,x0)` re-sort + the region-level `min(feature_block_indices)` sort (which would otherwise re-scramble the columns). **Divergence-gated even when ON** → clean low-divergence (single-column) pages stay byte-identical, so enabling it repairs only genuinely-fractured pages. sourceId/`region_index` blast radius matches the reorder posture (order-only; FB-derived `data-dart-block-id` byte-stable, but DROPPED garbage tesseract-only lines shrink the id set on a repaired page). Falsey / unset / garbage → off (parse-with-fallback). Gate only — deterministic reorder, selects no provider/model → no `docs/LICENSING.md` row. |
| `SEMANTIK_VLM_ORDER_DIVERGENCE` | `0.25` | **Per-page divergence floor** at/above which `SEMANTIK_VLM_ORDER_AUTHORITATIVE` treats Tesseract order as unreliable and makes VLM order authoritative (`vlm_fusion.py::resolve_vlm_order_divergence_floor`). `divergence = (tesseract_only + vlm_only)/(n_tess + n_vlm)` — a clean single-column page aligns almost every line (well under 0.1); a fractured multi-column grid runs far higher. Float parse-with-fallback in `[0.0, 1.0]`: blank / non-float / NaN / Inf / out-of-range → `0.25`. No-op when `SEMANTIK_VLM_ORDER_AUTHORITATIVE` is off. Selects no provider/model → no `docs/LICENSING.md` row. |
| `SEMANTIK_VLM_PROVIDER` | unset → `local` | **Provider-agnostic VLM seat selector** (`extract_shared.py::resolve_vlm_provider`), mirroring the `SEMANTIK_SPECIALIST_PROVIDER` normalization. Blank / `local` / `gguf` / `ollama` → `"local"` (the local ollama seat — no credential); any other non-empty value is the lower-cased provider name — a new provider is env config, never a subclass, and the SAME seat serves the local ollama GPU and a Spark endpoint later. A non-local hosted (non-loopback) provider without a key FAILS LOUD at dispatch (`VLMSeat.require_ready` → `VLMSeatError`; the P0 HTTP dispatch calls it before any POST — never a silent stub). Parse-with-fallback. **Selects an LLM provider → has a `docs/LICENSING.md` row** (the Qwen2.5-VL-7B seat). |
| `SEMANTIK_VLM_BASE_URL` | unset → `http://localhost:11434` | VLM seat base URL (`extract_shared.py::resolve_vlm_base_url`). Resolution: `SEMANTIK_VLM_BASE_URL` (explicit) > the local ollama default. Mirrors `endpoint_runtime._resolve_base_url` but with a machine default because the seat's default IS local (OpenAI-compatible). URL only — no `docs/LICENSING.md` row. |
| `SEMANTIK_VLM_API_KEY` | unset → `None` | VLM seat bearer token (`extract_shared.py::resolve_vlm_api_key`). Resolution: `SEMANTIK_VLM_API_KEY` (explicit) > `None`. The local / loopback seat needs no credential; a non-local hosted (non-loopback) provider without a key fails closed before dispatch (`VLMSeatError`). Credential, never hardcoded — no `docs/LICENSING.md` row (the seat it unlocks is covered by the `SEMANTIK_VLM_PROVIDER` / `_MODEL` row). |
| `SEMANTIK_VLM_MODEL` | `qwen2.5vl:7b` | **VLM seat model id** (`extract_shared.py::resolve_vlm_model`). Resolution: `SEMANTIK_VLM_MODEL` (explicit) > the local Qwen2.5-VL-7B default (`qwen2.5vl:7b`, Apache-2.0). Parse-with-fallback (blank / garbage → the default). **Selects an LLM model → has a `docs/LICENSING.md` row** (Qwen2.5-VL-7B-Instruct, Apache-2.0 — the 7B VL, NOT the Qwen-licensed 72B). |
| `SEMANTIK_VLM_TIMEOUT_SECONDS` | `180.0` | **Per-request HTTP timeout (float s) for the P0 VLM image-chat POST** (`extract_shared.py::resolve_vlm_timeout`; consumed by the P0 client `vlm_extract.py::extract_page_markdown`). Defaults HIGHER than the specialist seat's 120 s because the tier-0 probe measured a 64 s cold-load + 12 s/page warm on ollama 0.31.1 — the FIRST scanned page of a document eats load+inference in one request, and the per-page fail-soft keeps one wedged page from killing a chapter. Read at CALL time. Parse-with-fallback: non-float / non-finite / non-positive / garbage → `180.0`. **P0 client lives in `vlm_extract.py`** (the OpenAI-compatible `/chat/completions` image-chat client + per-page markdown disk cache `data/vlm_extract_cache/` [glm-ocr-cache layout, keyed on `sha256(pdf_sha \| page \| model \| prompt_version \| render_px)`] + the best-effort local-model `keep_alive:0` unload fired once per document when ≥1 live POST was made on a `local` seat). A request parameter only — selects no provider/model → no `docs/LICENSING.md` row. **Min-version note:** ollama < 0.31 refused GPU placement for the vision estimator (CPU-only wall-clock trips this timeout); use ≥ 0.31.1. **Data-build caveat:** do NOT set `SEMANTIK_VLM_EXTRACT` for the offline `worker_pool.py` data builds — per-worker VLM load/unload would thrash the card. |
| `SEMANTIK_DEPLOY_PROFILE` | unset (off) | **Bundled deploy-profile gate (W7.1 + W7.2 + column extraction)** — the SINGLE opt-in that repairs the deploy/render seam's default-off accessibility regressions AT ONCE (`SemantiK/dart_semantic/reading_order.py::resolve_deploy_profile`). On a deploy-like run figure detection AND column reading-order both default OFF, so every image is silently dropped (WCAG 1.1.1 content loss) and multi-column reading order is line-interleaved across the gutter. Default OFF → **byte-identical current deploy output** (the resolver reads unset/blank/falsey/garbage → off; parse-with-fallback, mirroring `resolve_column_order_mode`). Truthy (`1`/`true`/`yes`/`on`, case-insensitive) → turns ALL THREE on together by composing into the existing resolvers: `cascade.resolve_detect_figures` + the extract-side mirror `extract_shared._detect_figures_enabled` (W7.1 — the full Part-F figure path: image extraction → figure Region → PNG sidecar → `<img src>` fill → SmolVLM2 alt-text) AND `reading_order.resolve_column_order_mode` (W7.2 — column-major `(page, column_index, y0, x0)` re-sort) AND `reading_order.resolve_column_extract_mode` (column-aware EXTRACTION — see the `SEMANTIK_COLUMN_EXTRACT` row). Each underlying flag (`SEMANTIK_DETECT_FIGURES` / `SEMANTIK_COLUMN_ORDER` / `SEMANTIK_COLUMN_EXTRACT`) still works independently; this bundle is the OR of them. **Bundle blast radius (column-extract):** because the profile now composes column-aware extraction, flipping the bundle ON also (a) salts the extract disk-cache key (the append-only `|col2` salt — bumped from `|col1` when ITEM5 prong 1 made the Tesseract path column-aware; never serves a stale column-fused extraction; and because the bundle turns column-ORDER on too, the `|ord1` order-salt is subsumed by `|col2` here — extract-on implies order-on) and (b) re-segments genuinely multi-column pages, changing their text-block lists → FB indices → which `data-dart-block-id` / `dart:{slug}#{block_id}` sourceIds exist; downstream chunk refs / `source_module_map.json` / citation deep-links from a PRIOR run must be re-derived after a flip — full posture in the `SEMANTIK_COLUMN_EXTRACT` row. **W7.2 single-column guard:** the column re-sort's gutter detector previously regressed genuinely single-column docs (a paragraph indent / centered figure / right-aligned page number spawned a spurious column that reordered the page). The reorder call sites now pass `single_column_guard=True` (`reading_order.column_ids_for_x0s` / `column_ids_for_bboxes`, guard `_is_genuine_multicolumn` — each carved column must clear an absolute ≥2-seed AND relative ≥15%-share floor), so a genuine single-column page is a strict no-op/identity (no invented gutter); the council BERT-feature path (`council/merge_or_split._column_ids_for_page`) keeps the default `single_column_guard=False` → byte-identical. A high-value WCAG recovery kept opt-in so current deploy output is unchanged by default. Falsey / unset / garbage → off (parse-with-fallback). Selects no provider/model → no `docs/LICENSING.md` row (the SmolVLM2 captioner rides the existing figure-captioner path; the hosted large-model seat is unaffected — mirrors `SEMANTIK_DETECT_FIGURES`). |
| `SEMANTIK_COLUMN_EXTRACT` | unset (off) | **Column-aware EXTRACTION pass gate** (`SemantiK/dart_semantic/reading_order.py::resolve_column_extract_mode`; consumed in `extract_shared.py::_pdfplumber_page` AND `_tesseract_page_blocks` (ITEM5 prong 1)). Fixes the dominant cross-genre generalization defect: a multi-column born-digital PDF (Federal Register 3-col, CFR 2-col, IRS W-9 2-col) was extracted by bucketing each physical line across the FULL page width then x-sorting + space-joining, FUSING unrelated columns into one block string (a single region's `raw_text` interleaves page-number, agency-masthead, contact-line, and footnote fragments from all three columns — the "column salad"). The existing `SEMANTIK_COLUMN_ORDER` re-sort only reorders already-fused blocks — it cannot un-join per-word data that is gone by then — so the fix lives BEFORE the space-join. Default OFF → **byte-identical** (the legacy full-page-width `_assemble_text_blocks_singlewidth` loop runs verbatim; no extraction changes anywhere). Truthy (`1`/`true`/`yes`/`on`, case-insensitive) → each page is segmented into column BANDS before line assembly (`_assemble_text_blocks_columnaware`): (1) column-start words (each physical line's first word + any word opening a new column past a gutter-sized gap; table-cell words excluded so a wide data table can't seed a spurious gutter) seed the **REUSED** `reading_order.column_ids_for_x0s` single-linkage gutter detector with `single_column_guard=True`; (2) every word is assigned to the band whose left margin is the greatest `≤ word.x0` (a word belongs to the column it starts within — NOT the detector's left-edge-midpoint split, which misassigns wide interior words); (3) a full-width **header/masthead** whose word-run STRADDLES an interior column left-edge (a word sits in the gutter x-range) stays ONE block leading column 0, NOT scattered across bands; (4) emit column 0 top→bottom, then column 1, … . **Composition:** `resolve_deploy_profile` also enables it (the W7 bundle), and `resolve_column_order_mode` returns True whenever column-extract is on — column-aware extraction implies the column-major block sort, so the two never disagree. **Single-column safety (regression-critical):** `single_column_guard=True` + the genuine-multicolumn share floor mean a genuinely single-column page (indents / centered figures / justified prose) collapses to one band and takes the byte-identical single-width path — the load-bearing algebra-corpus no-op holds even flag-ON (second safety net atop default-OFF + the append-only cache salt). **Conservatism note (v1 honest limitation):** because the reused `_is_genuine_multicolumn` guard rejects the WHOLE page unless EVERY carved column clears the ≥15%-seed-share floor, a real multi-column page whose columns are unbalanced (a masthead / full-width heading / left-column-only run inflates column 0, pushing an interior column below the floor) degrades to the fused single-width output — no regression (byte-identical to today), but not yet segmented. Measured on the (gitignored) eval-corpus Federal Register fixture: the balanced 3-col body page segments cleanly and the salad is eliminated; its col-0-inflated masthead pages currently stay fused. A follow-up (word-share balance check / x-coverage projection-profile detector) would widen coverage. **Cache posture (append-only `|col2` salt, IN the key):** the column-band assembly changes the extracted text-block SHAPE, so the flag SALTS the extract disk-cache key (`_compute_extract_cache_key`, append-only `|col2` when on; `EXTRACT_CACHE_VERSION` NOT bumped) — flipping ON never serves a stale column-fused extraction, and the flag-OFF key stays byte-identical to the historic key (no `col0` salt). The salt was bumped `|col1`→`|col2` when ITEM5 prong 1 made the Tesseract path column-aware (the extracted OCR shape changed under the SAME flag, so a `|col1`-warmed cache must re-extract once). **sourceId blast radius (honest):** re-segmenting a page changes its text-block list → FB indices → which `data-dart-block-id` / `dart:{slug}#{block_id}` sourceIds exist. Deterministic per run (same PDF + same flags → same ids), off by default; downstream chunk refs / `source_module_map.json` / citation deep-links from a PRIOR run must be re-derived after a flip — same posture as `SEMANTIK_VLM_FUSION` / `SEMANTIK_UNIT_REGROUP`. **OCR path (ITEM5 prong 1 — now column-aware):** the Tesseract analog `_tesseract_page_blocks` re-keys each `(block, par, line)` word group to `(col, block, par, line)` via the SHARED `reading_order.column_edges_from_lines` gutter engine (the same engine the pdfplumber path now calls — one gutter engine, two callers) in the IMAGE-PIXEL space of the OCR bboxes (`page_w = image.width`), so a scanned multi-column page's OCR lines are no longer fused across the gutter; a full-width header word STRADDLING an interior edge stays ONE block (column 0), and a genuine single-column scan collapses via the guard → byte-identical grouping (the anti-regression posture). **`_merge_page` pixel-space gutter fix:** the column-major merged-block re-sort now scales its gutter by `page['tesseract_width']` (image pixels) on the OCR path (`not text_layer_ok`) instead of the PDF-point `page['width']`, fixing a verified latent bug (`0.06*612pt` vs a 1224px page) that mis-scaled the gutter by the render scale and neutered column-major ordering on the OCR path. Falsey / unset / garbage → off (parse-with-fallback). Gate only — deterministic segmentation, selects no provider/model → no `docs/LICENSING.md` row. |
| `SEMANTIK_MATH_RECONSTRUCT` | unset (off) | **Deterministic MathML reconstruction (T4)** gate (`SemantiK/dart_semantic/math_reconstruct/policy.py::resolve_math_reconstruct_mode`). Default OFF → byte-identical: no Stage-5 stamping (`structure_graph.py::build_structure_graph` math-region payload is unchanged), `assembler/pass_9a.py` runs the legacy candidate selection, and `assembler/fallbacks.py::fallback_math` emits the legacy raw `<span class="math-source">` glyph-soup fallback. Truthy (`1`/`true`/`yes`/`on`, case-insensitive) → the **DETERMINISTIC-FIRST** policy fires: Stage 5 recovers per-glyph geometry (`math_reconstruct/glyph_extract.py` — pdfplumber `page.chars` re-extracted in the math region bbox when the cascade threads `pdf_path`; else the flat `src_text`, which still carries unicode super/subscripts), infers the 2-D structure (`structure_infer.py` — super/subscripts from size+baseline shift, built-up fractions from fraction-bar geometry, bracket/row grouping), emits presentation-MathML with `alttext` (`mathml_emit.py`), validates EVERY candidate through `gates/mathml_check.py::validate_mathml`, and stamps a gate-valid deterministic `<math>` on `region.payload['math_reconstruct']`. The chain is **deterministic → local QLoRA math-specialist generative fallback (on-device, license-clean — NEVER the hosted large-model seat; `policy.build_local_math_generative_fn` forces `make_runtime("real", force_local=True)`) → accessible-uniform `<math><mtext>`/`<merror>` last resort** (source as `alttext`), so the raw glyph soup is ELIMINATED (zero raw soup ever ships when the feature is on). `pass_9a` prefers the stamped deterministic candidate over the Stage-6 generative one. Shapes the deterministic reconstructor declines (matrices, multi-line/aligned systems, big-operator limits `∑∏∫`, roots, nested/stacked fractions, or a blind linearisation where the council saw 2-D structure) self-flag low-confidence and route to the generative tier (Stage-6's local-by-default math specialist). Falsey / unset / garbage → off (parse-with-fallback). Gate only — selects no provider/model (the optional generative tier rides the already-licensed LOCAL specialist seat), no separate `docs/LICENSING.md` row. **Caveat:** the directive "never the hosted large-model seat for math" holds for the policy's own generative tier (forced local); keep the Stage-6 math seat local (the byte-stable default) so a `SEMANTIK_SPECIALIST_ENDPOINT_DISPLACE` run does not route the residual generative math to the cloud seat — documented follow-up. |
| `SEMANTIK_THETA_DEVICE` | `cpu` | Torch device for the theta cross-encoder (DeBERTa-v3-small + LoRA semantic-preservation head; `SemantiK/dart_semantic/theta/semantic_preservation.py`, `ENV_THETA_DEVICE`, ~L74). Resolution: explicit `device` arg > `SEMANTIK_THETA_DEVICE` > the back-compat `DART_THETA_DEVICE` alias > `cpu`. Accepts `cpu` / `cuda` / `cuda:N`; empty / garbage → `cpu`. Graceful CUDA-unavailable fallback to CPU at load time (never crashes a GPU-less box). Mirrors `ED4ALL_NLI_DEVICE`. Device knob, not a provider/model — no licensing row. |
| `SEMANTIK_EXPANDABLE_SEGMENTS` | unset (off) | CUDA-allocator opt-in for the out-of-process bridge (`MCP/tools/pipeline_tools.py::_semantik_expandable_segments_enabled`, ~L6061). Truthy (`1`/`true`/`yes`/`on`) → the bridge subprocess env gets `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True` (OOM mitigation; opt-in because the setting is driver/torch-version dependent). Falsey / unset / garbage → off (unset). No licensing row. |
| `SEMANTIK_BRIDGE_TIMEOUT_SECONDS` | `3600.0` | Subprocess timeout (float seconds) for the out-of-process SemantiK cascade JSON bridge (`MCP/tools/pipeline_tools.py::_resolve_semantik_bridge_timeout`, ~L6068). Non-float / non-positive / garbage → `3600.0`. No licensing row. |
| `SEMANTIK_PYTHON` | unset (required for bridge) | Absolute path to the SemantiK runtime venv's python interpreter, used when the cascade runs out-of-process via the JSON bridge (`MCP/tools/pipeline_tools.py`, `_SEMANTIK_PYTHON_ENV` ~L6041/6134/6579). No machine default — when the in-process SemantiK deps are absent and this is unset, the bridge fails closed with operator guidance. Exec path, not a provider/model — no licensing row. |
| `SEMANTIK_RUNTIME_DIR` | unset (required for bridge) | SemantiK runtime repo root, used as the subprocess `cwd` for the out-of-process cascade so model/cache dirs resolve relative to it (`MCP/tools/pipeline_tools.py`, `_SEMANTIK_RUNTIME_DIR_ENV` ~L6042/6135). No machine default; fail-closed when the bridge is needed and this is unset. Path knob — no licensing row. |
| `SEMANTIK_HOME` | unset (repo-relative) | **Relocatable data root** (`SemantiK/dart_semantic/paths.py`, ~L80). When set, every SemantiK data dir defaults to `<SEMANTIK_HOME>/<basename>` (`models`, `data`); the per-dir overrides below keep higher precedence (per-dir env > `SEMANTIK_HOME` > package-relative `<SemantiK>/<basename>`). Mirrors `ED4ALL_HOME`. Read at call time (tests/Docker can flip it without re-import). Byte-stable to the package-relative default when unset. No licensing row. |
| `SEMANTIK_MODEL_DIR` / `SEMANTIK_CACHE_DIR` / `SEMANTIK_DATA_DIR` / `SEMANTIK_CONFIG_DIR` | `<SemantiK>/models` (model) · `<SemantiK>/data` (cache/data/config) | Per-dir relocation overrides for the SemantiK data roots (`SemantiK/dart_semantic/paths.py::_resolve`, ~L108/118/127/137). Precedence per dir: the per-dir env var > `SEMANTIK_HOME/<basename>` > package-relative. `SEMANTIK_MODEL_DIR` points at model weights / LoRA council adapters / Qwen GGUFs / theta head; the cache/data/config roots hold extract/glm-ocr/prerender caches, eval reports/datasets/labels, and calibration/config artifacts respectively. No provider/model selection — no licensing row. (Counted as four flags in the prefix-ownership tally.) |
| `SEMANTIK_STOP_SENTINEL` | unset (no seams) | **Declared-env plumbing, NOT an operator flag** — the graceful-stop ("checkpoint on command") sentinel path(s) the Ed4All boundary injects into the cascade so it can pause between chapters. The Ed4All-side `MCP/tools/pipeline_tools.py` boundary (`_SEMANTIK_STOP_SENTINEL_ENV`, ~L7116/7209) exports the run-scoped **and** global sentinel paths — `os.pathsep`-joined — into both the in-process cascade call and the out-of-process JSON-bridge subprocess env; the SemantiK-local `dart_semantic/stop_seam.py` (zero Ed4All imports, cross-venv twin) parses them per call and probes for existence. When ANY listed path exists the cascade seams raise `CascadeStopRequested` (translated to `GracefulStopRequested` at the boundary): the seams sit at post-Stage-5e/pre-Stage-6, pre-Stage-13 offline-retry, and the Stage-6 adapter-batch boundary. **No-op when unset / blank** (zero paths → the seams never fire → byte-identical legacy behaviour) — operators set `ed4all stop`, not this var. Selects no provider/model — no `docs/LICENSING.md` row. |

**Dev-scripts-only flag (kept out of the table above to avoid noise, mirroring the root `ED4ALL_*` test-discovery posture):** `SEMANTIK_ARXIV_REPO` (default `~/arxiv-repo/papers`) points the offline eval-corpus builders under `SemantiK/scripts/` (`build_eval_corpus_manifest.py`, `pair_from_arxiv.py`, `measure_stage5_heading_rate.py`) at a local arxiv paper repo. Not a production pipeline path; selects no provider/model.

## Honest constraints

- **Council VRAM on 8 GB.** The full cascade is GPU-flaky on an 8 GB card —
  council BERTs share one ModernBERT-base backbone (one-resident LoRA adapter
  swap) and the Qwen specialists batch **by adapter** rather than fanning out,
  precisely because parallel adapter contexts + a concurrent Chromium/axe-core
  process poison CUDA on 8 GB. Mitigated, not eliminated; gate GPU-heavy work
  on a dev box. The DGX Spark-class deployment target is the real fix.
- **Structure quality is council-bound.** Block-ID quality of *pedagogical*
  elements is only as good as BERT-Structure's `structural_role` / `is_heading`
  heads; heading over-detection is patched defensively (the always-on
  deterministic front-matter detector is load-bearing; the Stage-5d large-model
  reviewer is conservative and off by default).

See [`architecture.md`](architecture.md) §14 for the full limitations section.

## License

Apache-2.0 (`LICENSE`, "Copyright 2026 Ed4All"). License-clean by
construction: the PDF stack is pypdfium2 (Apache-2/BSD-3) + pdfplumber (MIT) +
pikepdf (MPL-2.0) + pytesseract/Tesseract (Apache-2); the ML stack is
transformers/peft/llama-cpp-python — **every dependency on the path is
permissively licensed** (see the licensing inventory in
`dart_semantic/extract_shared.py` and `image_extract.py`). Model weights
(council BERTs, Qwen GGUFs, theta head) are separate artifacts, not shipped in
this tree. The provider/model licensing for the opt-in hosted large-model endpoint seat
lives in `docs/LICENSING.md`.
