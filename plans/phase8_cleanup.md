# Phase 8 Detailed Execution Plan — Two-Pass Architecture Closing Cleanup

Closes the deferred-followup queue surfaced by the Phases 1-7c.5 + 5 closing review. Eight followups + one user-decision placeholder (Subtask 8.5: BERT SHA pinning). **Depends on:** HEAD `aad82f0` on `claude/dev0.3.0-courseforge-1o4fS`. Not a feature wave — every subtask is either a parity fix, portability fix, stale-test refresh, or doc/dep nit. **No production-code change ships outside the scope below.**

---

## Investigation findings (locked)

- **`@mcp.tool() async def archive_to_libv2`** is at `MCP/tools/pipeline_tools.py:1249-1489`. Its manifest emit at `:1448-1467` carries `libv2_version`, `chunker_version`, `slug`, `import_timestamp`, `classification`, `source_artifacts`, `provenance`, `features` — **and nothing else**. The three SHA fields the registry variant emits (`concept_graph_sha256`, `dart_chunks_sha256`, `imscc_chunks_sha256`) are absent both from the manifest dict AND from the function's parameter list (`:1250-1259` declares only `course_name`, `domain`, `division`, `pdf_paths`, `html_paths`, `imscc_path`, `assessment_path`, `subdomains`).
- **Registry variant `_archive_to_libv2`** at `MCP/tools/pipeline_tools.py:5287-5757` resolves all three SHAs via `kwargs` (`:5347-5358`), recomputes `concept_graph_sha256` from on-disk JSON when the kwarg is malformed (`:5666-5687`), and conditionally adds `dart_chunks_sha256` / `imscc_chunks_sha256` to the manifest when each kwarg matches `^[0-9a-f]{64}$` (`:5709-5727`). Phase 7c.5 commit `e5603ac` landed this only in the registry variant — the divergence is real.
- **`Trainforge/process_course.py:1303`** calls `chunks = self._chunk_content(parsed_items)` from inside `process()` (the 6-stage pipeline entry point at `:1276-1349`). The output `chunks` list feeds `_write_chunks` (`:1307`), `_generate_concept_graph` (`:1311`), `_generate_pedagogy_graph` (`:1319`), `_generate_quality_report` (`:1324`), `_generate_semantic_concept_graph` (`:1331`), and `_write_metadata` (`:1338`). The wrapper at `:1502-1533` already delegates to `ed4all_chunker.chunker.chunk_content` (Phase 7a ST 6), so the redundancy is the IMSCC chunkset emit — `imscc_chunking` workflow phase (`config/workflows.yaml:1422-1457`, `_run_imscc_chunking` at `MCP/tools/pipeline_tools.py:6799-7087`) writes the canonical `imscc_chunks/chunks.jsonl` BEFORE `trainforge_assessment` runs. The only consumer that re-runs the chunker is `CourseProcessor.process()` invoked from `_run_trainforge_assessment` at `MCP/tools/pipeline_tools.py:4845-4868`. The migration path: thread `imscc_chunks_path` via the workflow runner's `inputs_from` chain (mirroring Phase 7b ST 14.5's `dart_chunks_path` consumption pattern in `_run_concept_extraction` at `:6216-6249`), accept it as a `CourseProcessor` constructor kwarg, and short-circuit the `_chunk_content` call when the path is provided + readable.
- **Three hardcoded `_PROJECT_ROOT / "LibV2"` sites at HEAD**: `MCP/tools/pipeline_tools.py:6401` (`_run_concept_extraction`), `:6714` (`_run_dart_chunking`), `:7039` (`_run_imscc_chunking`). All three follow the literal pattern `course_dir = _PROJECT_ROOT / "LibV2" / "courses" / course_slug`. Two parallel sites at `:1280` and `:5364` are inside `archive_to_libv2` variants — different surface, out of scope here. The three Phase 6/7 helpers are the portability blocker for ops topologies that mount LibV2 at a non-default location (Docker volume / NFS / ConfigMap).
- **`lib/classifiers/bloom_bert_ensemble.py::_DEFAULT_ENSEMBLE_MEMBERS`** at `:63-76` carries three entries; each `revision: "main"` is a placeholder per the module docstring at `:24-32`. The `kabir5297/bloom_taxonomy_classifier` model (`:65`) currently 401s on HuggingFace. The other two (`distilbert-base-uncased-finetuned-sst-2-english`, `MoritzLaurer/DeBERTa-v3-base-mnli-fever-anli`) are non-gated and resolvable via `huggingface_hub.HfApi().model_info(repo_id).sha`.
- **`config/tests/test_workflows_source_mapping.py::TestCoursePlanningUpdated::test_course_planning_depends_on_source_mapping`** at `:87-89` asserts `"source_mapping" in phase["depends_on"]`. The current value at `config/workflows.yaml:785` is `depends_on: [concept_extraction]` — the Phase 6 insert moved `concept_extraction` between `source_mapping` and `course_planning` (workflow chain: `source_mapping → concept_extraction → course_planning`). The dep `concept_extraction` itself depends on `[source_mapping, chunking]` (`config/workflows.yaml:735`), so source_mapping is still a transitive predecessor — but the test asserts a direct edge that the architecture no longer carries.
- **`lib/tests/test_workflow_runner_meta_schema.py::test_get_phase_param_routing_matches_legacy_structure`** at `:241-255` iterates `_LEGACY_PHASE_PARAM_ROUTING.items()` and asserts each phase's YAML-resolved routing matches the legacy in-memory dict tuple-for-tuple. The legacy dict at `MCP/core/workflow_runner.py:83-96` does NOT include the Phase 6 ST 16 addition `"concept_graph_path": ("phase_outputs", "concept_extraction", "concept_graph_path")`, but the YAML at `config/workflows.yaml:820-823` DOES route this. Same divergence on `libv2_archival` (legacy at `:156-163` doesn't carry `concept_graph_sha256` / `dart_chunks_sha256` / `imscc_chunks_sha256`; YAML routes all three). Test was a Wave 6 transition net — Phases 6 + 7c.5 widened the YAML without updating the legacy fallback dict.
- **`build_pedagogy_graph` at `Trainforge/pedagogy_graph_builder.py:593`** reads `cid = c.get("id")`. `_create_chunk` at `Trainforge/process_course.py:1713` emits canonical `"id"` keys. The Phase 6 inline-projection in `_run_concept_extraction` at `MCP/tools/pipeline_tools.py:6327-6328` STILL emits `"chunk_id"`-keyed dicts (verified at HEAD; W14.5's reconciliation gated it as a fallback path that runs only when `dart_chunks_path` is absent or unreadable, but did NOT migrate the inline-projection itself to canonical id-keyed emit). On the forward path the chunkset is loaded from JSONL (`:6231-6243`) and chunks carry canonical `id` from the chunker package. **Residual scope**: any workflow run where `dart_chunks_path` is missing, malformed, or where the upstream `chunking` phase was skipped falls into the inline-projection path and feeds `build_pedagogy_graph` with chunks `build_pedagogy_graph` silently drops.
- **Phase 7b ST 12 commit `626a53b`** subject reads `feat(schemas): add dart_chunks_manifest.schema.json for Phase 7b dual-chunkset architecture`; the on-disk filename is `schemas/library/chunkset_manifest.schema.json` (verified). Cosmetic-only — the schema landed correctly with the symmetric DART+IMSCC `chunkset_kind` discriminator.
- **`pyproject.toml::[project.optional-dependencies].dev`** at `:50-58` already pins `pytest-asyncio>=0.21.0,<1.0.0`. The dependency itself is correctly declared. The actual blockers are: (a) the dep is in `[dev]` extras, so `pip install -e .` (without the extras) leaves the test env without `pytest-asyncio` — the user complaint surfaces this; (b) `pytest.ini` at `:25-33` does NOT register `asyncio` as a known marker AND `--strict-markers` is set at `:6`, so `@pytest.mark.asyncio` triggers strict-marker rejection independent of plugin availability; (c) no `asyncio_mode` config — `pytest-asyncio>=0.21` requires either `auto` mode or per-test fixture decoration. Repo carries 111 `@pytest.mark.asyncio` decorations across 21 files (`grep -rn "@pytest.mark.asyncio"`); the failure surface is wider than the "3+ false test failures" estimate.
- **Hardcoded LibV2 path is structurally parallel** to the Phase 6 ST 18 + Phase 7c.5 SHA-threading already wired into `_archive_to_libv2`'s registry variant. An ops topology change here lands consistently across the four helpers.

---

## Pre-resolved decisions

1. **Subtask 1 fix shape.** Bring the `@mcp.tool()` variant of `archive_to_libv2` to feature-parity with the registry variant's three-SHA emit. Add `concept_graph_sha256`, `dart_chunks_sha256`, `imscc_chunks_sha256` parameters to the function signature (Optional[str], default None — preserve back-compat for legacy MCP clients calling without them). Persist each into the manifest gated on the same `^[0-9a-f]{64}$` regex check the registry variant uses (`pipeline_tools.py:5667-5687, :5720-5727`). DO NOT replicate the registry variant's recompute-from-disk fallback for `concept_graph_sha256` — the `@mcp.tool()` surface is called directly by external MCP clients that pass paths explicitly; recomputation belongs to the workflow-runner-driven path. Net divergence after the fix: `@mcp.tool()` requires explicit kwargs (no recompute); registry variant retains both the kwarg + on-disk recompute fallback. This asymmetry is intentional — the surfaces have different caller contracts.
2. **Subtask 2 fix shape.** Promote `imscc_chunks_path` to a workflow-routed input that `_run_trainforge_assessment` consumes and passes to `CourseProcessor`. Add `imscc_chunks_path: Optional[str] = None` constructor kwarg to `Trainforge/process_course.py::CourseProcessor.__init__`; when populated, `process()` reads chunks from the upstream JSONL instead of calling `self._chunk_content(parsed_items)`. Mirrors the Phase 6 ST 13 + Phase 7b ST 14.5 upstream-consumption pattern (`_generate_pedagogy_graph` falls back to in-process build when no path is threaded; `_run_concept_extraction` falls back to inline-projection). Best-effort upstream consumption with warning-severity log on missing / corrupted upstream chunks; legacy callers (no `imscc_chunking` phase wired, e.g. `python -m Trainforge.process_course` standalone) keep working unchanged.
3. **Subtask 3 fix shape.** Thread a `libv2_root` resolution helper through `_run_concept_extraction`, `_run_dart_chunking`, `_run_imscc_chunking`. Resolution chain (high → low): explicit `libv2_root` kwarg > `ED4ALL_LIBV2_ROOT` env var > `_PROJECT_ROOT / "LibV2"` legacy default. New helper `_resolve_libv2_root()` co-located with the three call sites; one call replaces each of the three hardcoded literals. Workflow runner threads via `inputs_from: [{param: libv2_root, source: workflow_params, key: libv2_root}]` — the param surfaces as an optional `--libv2-root` CLI flag on `cli/commands/run.py`. Default resolution unchanged for every existing run.
4. **Subtask 4 framing.** **RESOLVED: operator selected `cip29/bert-blooms-taxonomy-classifier`** (per USER decision logged 2026-05-03 — the BERT-named candidate with the strongest usage signal at 414 downloads, apache-2.0 license, BERT-based "generated_from_trainer" provenance). The two non-Bloom members (`distilbert-base-uncased-finetuned-sst-2-english`, `MoritzLaurer/DeBERTa-v3-base-mnli-fever-anli`) get their concrete SHAs pinned independently — their revisions are unaffected by this choice. Subtask 4 lands the SHA-pinning work for all three (replace `revision: "main"` with concrete commit SHAs via `huggingface_hub.HfApi().model_info(repo_id).sha`). **`cip29` requires a `_CIP29_TO_BLOOM` label-map translation table** mirroring the existing `_SST2_TO_BLOOM` pattern at `bloom_bert_ensemble.py:100-103` if its label schema diverges from the canonical 6-level Bloom enum (`remember`, `understand`, `apply`, `analyze`, `evaluate`, `create`). The translation table contents are determined by reading the model's actual label schema at SHA-pin time (`huggingface_hub.HfApi().model_info("cip29/bert-blooms-taxonomy-classifier").config` or by loading the model and inspecting `id2label`). The two rejected candidates were `Malithi200/bloom-taxonomy-classifier` (lower download count, no "bert" semantic match) and `phoenix28/bloom-taxonomy-classifier` (newest with only 18 downloads / 3 likes — insufficient usage signal for a production-pinned audit trail member).
5. **Subtask 5 fix shape.** Update both stale tests to reflect Phase 6's new dep chain. `test_course_planning_depends_on_source_mapping` becomes `test_course_planning_depends_on_concept_extraction` asserting `"concept_extraction" in phase["depends_on"]`; the test docstring carries a one-line note that `source_mapping` is now a transitive predecessor via `concept_extraction.depends_on`. `test_get_phase_param_routing_matches_legacy_structure` either (a) updates `_LEGACY_PHASE_PARAM_ROUTING["course_planning"]` + `["libv2_archival"]` to mirror current YAML routing or (b) softens the assertion to "legacy is a subset of YAML" with a one-line note explaining the post-Phase-6 widening contract. Pre-resolved decision: option (a) — keep the legacy dict as a true mirror so the test continues to catch silent YAML widening that doesn't update the legacy fallback path. (The legacy dict is still consulted by `_get_phase_param_routing` at `MCP/core/workflow_runner.py:476` as a fallback when YAML lookup misses; the dict drift is real risk.) Treat both tests as architectural-truth assertions, not stale-data updates — they document the dep chain.
6. **Subtask 6 fix shape.** Migrate the inline-projection in `_run_concept_extraction` at `MCP/tools/pipeline_tools.py:6327-6336` to emit canonical `"id"` keys instead of `"chunk_id"` keys. Single-line dict-key rename. Adds a regression test in `MCP/tests/` that exercises `_run_concept_extraction` with `dart_chunks_path=None` (forces fallback path) and asserts the produced chunks pass through `build_pedagogy_graph` without silent drop. Closes the Phase 6 W14.5 latent.
7. **Subtask 7 framing.** Cosmetic-only: amend nothing on disk. Document the disagreement in this plan file (commit message vs filename) for the audit trail and move on. No subtask body — the existing on-disk filename `chunkset_manifest.schema.json` is the canonical name; the commit message stands as historical artifact.
8. **Subtask 8 fix shape.** Three additions: (a) move `pytest-asyncio` from `[dev]` to default `dependencies` (so a bare `pip install -e .` install can run the test suite that the project's CI exercises); (b) add `markers = asyncio: ...` registration to `pytest.ini::[pytest].markers` table; (c) add `asyncio_mode = auto` to `pytest.ini::[pytest]` so `@pytest.mark.asyncio` decoration triggers without per-test fixture wiring. Net effect: 111 currently-skipped/failing async test invocations (across 21 files) flip to genuinely running. The (a)-vs-keep-in-`[dev]` decision is operator-level — the plan recommends (a) because the project's CI exercises async-test paths even on default installs (sample: `MCP/tests/test_executor_dispatch_routing.py` is collected by `pytest.ini::testpaths = MCP/tests`).

---

## Atomic subtasks

Estimated total LOC: ~600 (90 archive parity + 130 chunker retire + 80 libv2-root threading + 80 BERT SHA pin + 50 stale-test refresh + 30 chunk_id audit + 0 cosmetic nit + 70 pytest-asyncio + ~80 tests + ~20 docs). Smaller than Phase 7's ~3,600 because every subtask is closure work, not feature build.

### A. HIGH severity — archive parity + chunker retire (2 subtasks)

#### Subtask 1: Bring `@mcp.tool() async def archive_to_libv2` to three-SHA parity with the registry variant
- **Files:** `/home/user/Ed4All/MCP/tools/pipeline_tools.py:1249-1489`
- **Depends on:** none
- **Estimated LOC:** ~90
- **Change:** Extend the function signature (`:1250-1259`) with three new optional params `concept_graph_sha256: Optional[str] = None`, `dart_chunks_sha256: Optional[str] = None`, `imscc_chunks_sha256: Optional[str] = None`. Inside the manifest-build block (`:1448-1467`), after the `manifest = {...}` literal, add three conditional inserts gated on the same `^[0-9a-f]{64}$` regex shape the registry variant uses at `:5667-5687, :5720-5727`. Keep the recompute-from-disk fallback OUT of the `@mcp.tool()` variant per pre-resolved decision #1 — external MCP callers pass paths explicitly; recomputation is the workflow-runner-driven surface's job. Update the docstring at `:1260-1278` with a one-line callout naming the three new params (mirror the registry variant's docstring at `:5288-5318`). Capture the divergence in a code comment near the manifest-build block: `# @mcp.tool() variant: kwarg-only SHA emit (no on-disk recompute); registry variant carries the recompute fallback for workflow-runner-driven calls.`
- **Verification:** New test `MCP/tests/test_archive_to_libv2_mcp_tool_parity.py` asserts (a) the @mcp.tool() variant accepts all three SHA kwargs and persists each into the manifest when well-formed; (b) malformed SHAs (non-64-hex) are silently dropped (mirrors registry variant's `INVALID_*` fall-through behavior); (c) absent SHAs result in absent manifest fields (back-compat with legacy callers). Run: `pytest MCP/tests/test_archive_to_libv2_mcp_tool_parity.py -v` reports ≥3 PASSED.

#### Subtask 2: Retire in-process `_chunk_content` call in `Trainforge/process_course.py::process` when upstream `imscc_chunks_path` is wired
- **Files:** `/home/user/Ed4All/Trainforge/process_course.py:1276-1349, :1303`; `/home/user/Ed4All/MCP/tools/pipeline_tools.py:4845-4868` (CourseProcessor instantiation site); `/home/user/Ed4All/config/workflows.yaml:1459-1499` (trainforge_assessment phase `inputs_from`); `/home/user/Ed4All/MCP/core/workflow_runner.py:147-155` (legacy routing dict for `trainforge_assessment`)
- **Depends on:** none (independent of Subtask 1)
- **Estimated LOC:** ~130 (constructor kwarg + short-circuit branch + workflow YAML routing + legacy dict mirror + warning-log path)
- **Change:** (a) Add `imscc_chunks_path: Optional[str] = None` to `CourseProcessor.__init__` (around `Trainforge/process_course.py:1100-1200`); persist as `self.imscc_chunks_path`. (b) In `process()` at `:1301-1303`, branch: when `self.imscc_chunks_path` is non-None and the path exists + is readable, replace the `chunks = self._chunk_content(parsed_items)` call with `chunks = self._load_chunks_from_jsonl(self.imscc_chunks_path)` (new helper that streams the JSONL into a List[Dict]). On read failure, emit a warning-severity log mirroring the Phase 6 ST 13 fallback pattern (`_generate_pedagogy_graph` upstream-load best-effort) and fall through to the in-process `_chunk_content` build. (c) In `_run_trainforge_assessment` at `MCP/tools/pipeline_tools.py:4845-4868`, accept `imscc_chunks_path` from `kwargs` (add to the `kwargs.get(...)` block above the `CourseProcessor(...)` call) and pass through. (d) In `config/workflows.yaml::textbook_to_course::trainforge_assessment.inputs_from`, add `{param: imscc_chunks_path, source: phase_outputs, phase: imscc_chunking, output: imscc_chunks_path}`. (e) Mirror in `_LEGACY_PHASE_PARAM_ROUTING["trainforge_assessment"]` at `workflow_runner.py:147-155`: add `"imscc_chunks_path": ("phase_outputs", "imscc_chunking", "imscc_chunks_path")`.
- **Verification:** (i) `Trainforge/tests/test_course_processor_imscc_chunks_consumption.py` (new) asserts: a `CourseProcessor` constructed with `imscc_chunks_path=<path-to-fixture-chunks.jsonl>` skips `_chunk_content` and uses fixture chunks (count + first-id assertion); a `CourseProcessor` constructed with `imscc_chunks_path=None` falls through to legacy in-process chunking (back-compat); a `CourseProcessor` constructed with a malformed path emits warning + falls back. Run: `pytest Trainforge/tests/test_course_processor_imscc_chunks_consumption.py -v` reports ≥3 PASSED. (ii) Workflow-routing regression: `lib/tests/test_workflow_runner_meta_schema.py::test_get_phase_param_routing_matches_legacy_structure` continues to pass after the legacy-dict mirror update (Subtask 5 also touches this test file; coordinate ordering — Subtask 2 lands the routing addition, Subtask 5 cleans up the related stale assertions). (iii) `pytest Trainforge/tests/ -k "chunk" -v` keeps existing tests green.

### B. MEDIUM severity — portability + libv2-root parameter threading (2 subtasks)

#### Subtask 3: Thread `libv2_root` parameter through three Phase 6/7 helpers
- **Files:** `/home/user/Ed4All/MCP/tools/pipeline_tools.py:6401, :6714, :7039` (the three hardcoded sites); `/home/user/Ed4All/cli/commands/run.py` (CLI flag); `/home/user/Ed4All/config/workflows.yaml::textbook_to_course` (route param into the three phases)
- **Depends on:** none
- **Estimated LOC:** ~80
- **Change:** (a) At module level in `pipeline_tools.py` (near `_PROJECT_ROOT` definition), add `def _resolve_libv2_root(explicit: Optional[str] = None) -> Path:` that follows the resolution chain in pre-resolved decision #3: explicit kwarg > `ED4ALL_LIBV2_ROOT` env var > `_PROJECT_ROOT / "LibV2"` default. (b) At each of the three call sites, replace `course_dir = _PROJECT_ROOT / "LibV2" / "courses" / course_slug` with `course_dir = _resolve_libv2_root(kwargs.get("libv2_root")) / "courses" / course_slug`. (c) In `cli/commands/run.py`, add `--libv2-root <path>` flag (Optional, default None) that surfaces as `workflow_params.libv2_root`. (d) In `config/workflows.yaml::textbook_to_course`, add `{param: libv2_root, source: workflow_params, key: libv2_root}` entries to the `inputs_from` blocks of `concept_extraction`, `chunking`, and `imscc_chunking` phases. Mirror the additions in `_LEGACY_PHASE_PARAM_ROUTING` for the same three phases.
- **Verification:** `MCP/tests/test_libv2_root_resolution.py` (new) asserts the resolution chain — explicit > env > default — and that all three helpers (`_run_concept_extraction`, `_run_dart_chunking`, `_run_imscc_chunking`) write to the resolved root when `libv2_root` is threaded. Test fixture sets `ED4ALL_LIBV2_ROOT=<tmp_path>`, invokes each helper, asserts `(<tmp_path>/courses/<slug>/concept_graph|dart_chunks|imscc_chunks).exists()` is true. Backward compat: same test with no env / no kwarg falls through to `_PROJECT_ROOT / "LibV2"` default. Run: `pytest MCP/tests/test_libv2_root_resolution.py -v` reports ≥6 PASSED.

#### Subtask 4: Pin BERT ensemble revisions to concrete HuggingFace commit SHAs (BERT SHA pin)
- **Files:** `/home/user/Ed4All/lib/classifiers/bloom_bert_ensemble.py:63-76` (`_DEFAULT_ENSEMBLE_MEMBERS`)
- **Depends on:** **USER decision on Bloom-classifier replacement** (see Open Questions §1)
- **Estimated LOC:** ~80 (40 LOC for SHA resolution + capture-emit; 40 LOC for label-map translation table when a non-canonical Bloom model is chosen)
- **Change:** (a) Replace the three placeholder `revision: "main"` entries with concrete commit SHAs. Resolution: `huggingface_hub.HfApi().model_info(repo_id).sha` against each of the three `name` values; record the resolved SHA in the entry. (b) Capture the resolved revision in the existing `bert_ensemble_member_loaded` decision event so the audit trail records each classification's source commit (per the module docstring at `:24-32`). (c) **Conditional on user choice**: if the operator selects a non-canonical Bloom model (e.g. `cip29/bert-blooms-taxonomy-classifier` with non-`remember`/`understand`/.../`create` labels), add a translation table (`_CIP29_TO_BLOOM` or `_MALITHI_TO_BLOOM` or `_PHOENIX_TO_BLOOM`) mirroring the existing `_SST2_TO_BLOOM` pattern at `:100-103`. The translation runs inside `_aggregate` against the model's raw output. Operator selects the table contents from the model's documented label schema. (d) Module docstring update at `:24-32` to reflect the resolution (remove the "Phase 4 followup" callout; replace with a line describing the SHA-pinning method).
- **Verification:** `lib/tests/test_bloom_bert_ensemble.py` (extend existing): assert each of the three entries' `revision` fields matches the regex `^[0-9a-f]{40}$` (HuggingFace commit SHA shape). When the operator selects a non-canonical Bloom model, also assert the translation table has all six canonical Bloom levels in its values. Run: `pytest lib/tests/test_bloom_bert_ensemble.py -k "revision" -v` reports ≥1 PASSED.

### C. LOW severity — stale tests, chunk_id audit, commit nit, pytest-asyncio dep, ST 8.5 placeholder (5 subtasks)

#### Subtask 5: Refresh two stale Phase 6 transition tests + mirror Phase 6/7c.5 routing into legacy dict
- **Files:** `/home/user/Ed4All/config/tests/test_workflows_source_mapping.py:81-89` (rename test method + update assertion); `/home/user/Ed4All/lib/tests/test_workflow_runner_meta_schema.py:241-255` (no change to test body); `/home/user/Ed4All/MCP/core/workflow_runner.py:83-96, :156-163` (extend legacy dict to mirror current YAML routing for `course_planning` + `libv2_archival`)
- **Depends on:** Subtask 2 (which adds `imscc_chunks_path` routing — that addition must also land in the legacy dict here for the test to pass) AND Subtask 3 (which adds `libv2_root` entries to the same `_LEGACY_PHASE_PARAM_ROUTING` dict; sequencing ST 3 → ST 5 prevents merge conflicts on adjacent dict entries — see Execution sequencing for the wave-B ordering rationale)
- **Estimated LOC:** ~50
- **Change:** (a) `test_workflows_source_mapping.py:87-89`: rename `test_course_planning_depends_on_source_mapping` to `test_course_planning_depends_on_concept_extraction`; update assertion to `assert "concept_extraction" in phase["depends_on"]`. Add a one-line docstring noting that `source_mapping` is a transitive predecessor via `concept_extraction.depends_on: [source_mapping, chunking]`. (b) `MCP/core/workflow_runner.py:83-96`: extend `_LEGACY_PHASE_PARAM_ROUTING["course_planning"]` with `"concept_graph_path": ("phase_outputs", "concept_extraction", "concept_graph_path")`. (c) `:156-163`: extend `_LEGACY_PHASE_PARAM_ROUTING["libv2_archival"]` with `"concept_graph_sha256": ("phase_outputs", "concept_extraction", "concept_graph_sha256")`, `"dart_chunks_sha256": ("phase_outputs", "chunking", "dart_chunks_sha256")`, `"imscc_chunks_sha256": ("phase_outputs", "imscc_chunking", "imscc_chunks_sha256")`. (d) `_LEGACY_PHASE_PARAM_ROUTING["trainforge_assessment"]` already gets the `imscc_chunks_path` addition from Subtask 2.
- **Verification:** (i) `pytest config/tests/test_workflows_source_mapping.py::TestCoursePlanningUpdated -v` PASSES. (ii) `pytest lib/tests/test_workflow_runner_meta_schema.py::test_get_phase_param_routing_matches_legacy_structure -v` PASSES. (iii) The companion `test_get_phase_output_keys_matches_legacy` at `:258-269` continues to pass (no output-key drift expected — these are inputs).

#### Subtask 6: Migrate `_run_concept_extraction` inline-projection from `chunk_id` to canonical `id` key
- **Files:** `/home/user/Ed4All/MCP/tools/pipeline_tools.py:6327-6336` (single key rename inside the inline-projection fallback branch)
- **Depends on:** none
- **Estimated LOC:** ~30 (1 LOC change + ~25 LOC regression test + 5 LOC comment)
- **Change:** At `:6327-6336`, the chunk dict literal currently emits `"chunk_id": chunk_id`. Rename to `"id": chunk_id`. Add a code comment at the rename site: `# Phase 8 ST 6: emit canonical 'id' key (build_pedagogy_graph at Trainforge/pedagogy_graph_builder.py:593 reads c.get('id'); pre-Phase-8 chunk_id-keyed emit was silently dropped). Forward path (upstream dart_chunks_path JSONL load at :6231-6243) already emits canonical id via the chunker package; this fixes the fallback path that runs when dart_chunks_path is absent or unreadable.`
- **Verification:** `MCP/tests/test_concept_extraction_inline_projection_id_key.py` (new): construct a fixture staging dir with one `*_synthesized.json`, invoke `_run_concept_extraction` with `dart_chunks_path=None` (forces fallback), assert the emitted chunks have `"id"` keys and zero `"chunk_id"` keys. Pass the chunks to `build_pedagogy_graph(chunks=..., objectives=[], course_id="TEST")` and assert the returned graph has at least one chunk node (`Chunk` class) — pre-Phase-8 the count would have been zero due to silent drop. Run: `pytest MCP/tests/test_concept_extraction_inline_projection_id_key.py -v` reports ≥2 PASSED.

#### Subtask 7: Document Phase 7b ST 12 commit-message-vs-filename disagreement (cosmetic only — no on-disk change)
- **Files:** none — this subtask is a no-op except for documenting the disagreement in this plan file (above, in Investigation findings) for the audit trail
- **Depends on:** none
- **Estimated LOC:** 0
- **Change:** None on disk. The on-disk filename `schemas/library/chunkset_manifest.schema.json` is canonical (the symmetric DART+IMSCC name with the `chunkset_kind` discriminator). The Phase 7b ST 12 commit subject line `feat(schemas): add dart_chunks_manifest.schema.json ...` (commit `626a53b`) is historical artifact; renaming it would require a force-push to the merged commit, which violates the plan's "do NOT touch" boundary.
- **Verification:** None. (Subtask exists for completeness — the closing-review surfaced it explicitly. Documenting + dismissing it here closes the audit loop.)

#### Subtask 8: Promote `pytest-asyncio` to default dependency + add `asyncio_mode` + register marker
- **Files:** `/home/user/Ed4All/pyproject.toml:14-36` (move pin from `[dev]` extras to default `dependencies`); `/home/user/Ed4All/pyproject.toml:50-58` (remove from `[dev]` to avoid duplicate-pin); `/home/user/Ed4All/pytest.ini:25-33, :6` (add `asyncio_mode = auto` + register `asyncio` marker)
- **Depends on:** none
- **Estimated LOC:** ~70 (3 LOC pyproject.toml move + 2 LOC pytest.ini + ~60 LOC test changes if any per-test-file `asyncio_mode = strict` overrides surface during the rebuild)
- **Change:** (a) `pyproject.toml` — move `"pytest-asyncio>=0.21.0,<1.0.0"` from `[project.optional-dependencies].dev` to default `[project].dependencies`. Append a code comment explaining the project's CI exercises async-test paths even on default installs (sample: `MCP/tests/test_executor_dispatch_routing.py`), so leaving the dep in `[dev]` was a packaging bug. (b) `pytest.ini::[pytest]` — add `asyncio_mode = auto` (so `@pytest.mark.asyncio` decoration triggers without per-test fixture wiring). (c) `pytest.ini::[pytest].markers` — add line `asyncio: marks tests as async (managed by pytest-asyncio)` so `--strict-markers` accepts the marker. (d) Run a full test sweep to surface any tests that currently silently skip via the missing-plugin path; document any test-side cleanup needed in a Subtask 8.5 followup if scope balloons beyond the 70 LOC budget.
- **Verification:** (i) `pip install -e . && python -c "import pytest_asyncio; print(pytest_asyncio.__version__)"` exits 0 — verifies the dep is in default install. (ii) `pytest --collect-only MCP/tests/test_executor_dispatch_routing.py` lists all 12 async tests as collected (no strict-marker rejection). (iii) `pytest MCP/tests/test_executor_dispatch_routing.py -v` passes (or surfaces real test failures, which were previously hidden by plugin absence — those become Subtask 8.5 followups).

#### Subtask 9: Phase 7a ST 8.5 placeholder — BERT replacement-model decision capture
- **Files:** none — this subtask is a USER-DECISION capture record
- **Depends on:** USER input on Open Question §1
- **Estimated LOC:** 0 (Subtask 4 lands the actual code change; this subtask is the decision-capture artifact)
- **Change:** None on disk. When the user picks a replacement, log the choice + rationale to `training-captures/decisions/phase8_st_4_bert_replacement.jsonl` via the standard `DecisionCapture` interface with `decision_type="bert_ensemble_replacement_selection"`, `decision="<chosen_repo_id>"`, `rationale=<≥20 char operator rationale>`, `alternatives_considered=[{"option": "cip29/bert-blooms-taxonomy-classifier", "reason_rejected": "..."}, {"option": "Malithi200/bloom-taxonomy-classifier", "reason_rejected": "..."}, {"option": "phoenix28/bloom-taxonomy-classifier", "reason_rejected": "..."}]`. Subtask 4 then consumes the capture's selection.
- **Verification:** Decision capture file exists at the path above and validates against `schemas/events/decision_event.schema.json`. (No automated test — operator-driven.)

### D. Optional documentation (1 subtask)

#### Subtask 10: Add Phase 8 closing-review entry to root `CLAUDE.md` (optional — author when subtasks 1-9 land)
- **Files:** `/home/user/Ed4All/CLAUDE.md` (append a "Phase 8 cleanup" line in the existing wave / phase history if such a section exists; otherwise add a one-sentence note in the relevant section header)
- **Depends on:** Subtasks 1-9 all merged
- **Estimated LOC:** ~20
- **Change:** Single short paragraph noting Phase 8 closed the deferred-followup queue from the closing review of Phases 1-7c.5 + 5. Reference the eight followups + the Subtask 8.5 user-decision item. Cross-link this plan file. NO recap of code changes — those live in the per-subtask commit messages and PR descriptions.
- **Verification:** Manual review of the CLAUDE.md edit; no automated test.

---

## Execution sequencing

Recommended wave structure — three waves, ordered by dependency + severity:

**Wave A (HIGH severity, parallel-safe).** Subtasks 1, 2, 6, 7. Each is independent of the others; can land in any order. Subtask 7 is a no-op (cosmetic dismissal). Subtask 6 closes the W14.5 latent identified in the investigation.

**Wave B (MEDIUM severity + dep-chained from Wave A).** Subtasks 3, 5. **Sequencing within Wave B: ST 3 lands BEFORE ST 5.** Both subtasks edit `MCP/core/workflow_runner.py::_LEGACY_PHASE_PARAM_ROUTING` — ST 3 adds `libv2_root` entries to three phases (`concept_extraction`, `chunking`, `imscc_chunking`); ST 5 adds `concept_graph_path` / `concept_graph_sha256` / `dart_chunks_sha256` / `imscc_chunks_sha256` entries to `course_planning` + `libv2_archival` (different keys, different phases, but adjacent lines in the same dict). Landing ST 3 first lets ST 5's executor rebase cleanly without merge conflicts on the dict edits. ST 5 also depends on Subtask 2 (the legacy-dict mirror for `imscc_chunks_path` needs to land alongside the new YAML routing Subtask 2 added). Subtask 3 is independent of Wave A.

**Wave C (LOW severity + USER-blocked).** Subtasks 4, 8, 9, 10. Subtask 4 depends on USER decision (Open Question §1). Subtask 9 is the operator-driven decision-capture artifact. Subtask 8 is independent — can land any time after Wave A. Subtask 10 (optional) lands last.

Total wall-time estimate: ~4-6 hours per wave at human-paced review (smaller per-subtask scope than Phase 7's 3,600 LOC budget). Subtasks 1, 2, 3, 6 carry the regression-test load; Subtasks 4, 5, 7, 8, 9, 10 are mostly no-test or one-test scope.

---

## Open questions

1. **~~BERT replacement-model choice~~ — RESOLVED.** Operator selected **`cip29/bert-blooms-taxonomy-classifier`** (logged 2026-05-03; see pre-resolved decision #4 for selection rationale). Subtask 4 lands the SHA pin + the `_CIP29_TO_BLOOM` label-map translation table (contents determined at SHA-pin time by reading the model's `id2label` config). Subtask 9's decision-capture artifact records the choice + alternatives_considered for `Malithi200/bloom-taxonomy-classifier` + `phoenix28/bloom-taxonomy-classifier` per the standard `DecisionCapture` interface.

2. **Scope of Subtask 8 cascade (gates Subtask 8 sizing).** Once `pytest-asyncio` is in the default install + `asyncio_mode = auto` is on, the 111 currently-decorated `@pytest.mark.asyncio` test invocations across 21 files start running for real. Existing test failures previously hidden behind the plugin-absence path will surface. The plan budgets 70 LOC for Subtask 8, but if a wide swath of those 111 tests fail on real-content assertions (e.g. `MCP/tests/test_executor_dispatch_routing.py`'s async dispatch happy-path tests assume specific tool-registry content), Subtask 8 splits into Subtask 8 (the dep + config landing) and Subtask 8.5 (the per-test cleanup pass). Estimated probability of split: medium.

3. **`@mcp.tool()` archive variant kwarg surface vs registry parity contract (gates Subtask 1 closure).** Pre-resolved decision #1 says the `@mcp.tool()` variant takes the three SHAs as explicit kwargs but does NOT replicate the registry variant's recompute-from-disk fallback. This intentional asymmetry assumes external MCP clients pass paths explicitly and never expect the tool to compute hashes for them. If post-Subtask-1 telemetry shows external clients DO call the tool with empty SHA kwargs and expect recomputation, the asymmetry becomes a Subtask-followup. Resolution: leave the asymmetry as the default; revisit only if telemetry surfaces real callers in the recompute pattern.

---

## Boundaries — explicit scope confirmation

**Phase 8 is closure work only.** No new feature wave hides inside this plan. Each subtask's verification command is a single test invocation; total new test LOC is ≤300 across all 9 subtasks. The plan deliberately avoids any architectural change beyond the eight followups + the Subtask 8.5 USER-decision placeholder. If a reviewer surfaces a NEW followup during execution that wasn't in the closing review's queue, the new item gets its own subtask + dependency entry in this file — not a creeping scope expansion of an existing subtask.

**Cross-reference contract.** Each subtask's "Files" section cites at least one absolute path with line numbers — the executor reads only what's cited and modifies only what the change spec describes. No subtask is allowed to silently widen its own scope mid-execution; if the change reveals a new touch surface, the subtask returns to plan-update before execution continues.
