{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://libv2.local/schema/packaging_report.schema.json",
  "title": "IMSCC Packaging Report",
  "description": "Per-build sidecar emitted by `Courseforge/scripts/cartridge/package_multifile_imscc.py::package_imscc` next to the IMSCC archive. Records authored and packaged page counts plus exclusion reasons so promotion checks can account for every omitted page. The artifact is optional for compatibility with packages created before report emission was supported.",
  "type": "object",
  "required": ["schema_version", "course_code", "package_path", "source_coverage"],
  "additionalProperties": false,
  "properties": {
    "schema_version": {
      "type": "string",
      "pattern": "^v\\d+$",
      "description": "Schema-contract version. Bumped only when the emit shape changes."
    },
    "course_code": {
      "type": "string",
      "description": "Canonical course code (`COURSE_NAME`-style identifier passed into the packager)."
    },
    "course_title": {
      "type": "string",
      "description": "Human-readable course title (best-effort — derived from project_config.json upstream)."
    },
    "package_path": {
      "type": "string",
      "description": "Absolute path to the emitted `.imscc` archive."
    },
    "outline_only": {
      "type": "boolean",
      "description": "True when the build was packaged with `--outline-only` (drops content/example/assessment pages from the zip). Lets the master aggregator tell intentional outline pruning from quality failures."
    },
    "source_coverage": {
      "type": "object",
      "description": "Canonical W3.H source_coverage block. Drop reason codes for the H3 packaging arrow: `missing_lo` (page lacked any learningObjectives JSON-LD), `gate_block` (page violated per-week LO contract or another critical gate), `outline_filter` (page excluded by the `--outline-only` filter — intentional pruning, not a quality failure). The `internal_drop_reason_missing` bucket fires when the silent-gaming check trips.",
      "required": ["consumed_count", "emitted_count", "dropped_count", "drop_reasons", "coverage_pct"],
      "additionalProperties": false,
      "properties": {
        "consumed_count": {"type": "integer", "minimum": 0},
        "emitted_count": {"type": "integer", "minimum": 0},
        "dropped_count": {"type": "integer", "minimum": 0},
        "drop_reasons": {
          "type": "object",
          "additionalProperties": {"type": "integer", "minimum": 0}
        },
        "coverage_pct": {"type": "number", "minimum": 0, "maximum": 1}
      }
    }
  }
}
